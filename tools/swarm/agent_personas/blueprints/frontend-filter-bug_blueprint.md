# Blueprint: frontend-filter-bug

Generated by `tools/swarm/invent_personas.py` from problem file `reports\filter_bug_investigation_2026_05_03.md` using design engine `cerebras`.

## Personas in this blueprint

- `data-drift-specialist` -> `data_drift_specialist.md` — Detects and quantifies mismatches between data feeds that drive filtering logic.
- `overlay-ui-specialist` -> `overlay_ui_specialist.md` — Evaluates UI overlay implementation that stamps next‑month badges over React‑rendered dates.
- `filter-logic-specialist` -> `filter_logic_specialist.md` — Audits imperative filter guards in index.html for null‑eventData paths and date‑parsing fallbacks.
- `filter-bug-coordinator-synthesizer` -> `filter_bug_coordinator_synthesizer.md` (coordinator)

## Phases

### Phase 1: plan

Define investigation objectives, assign personas, and create a shared plan document.

**Outputs:** `plan.md`

### Phase 2: parallel_specialists

Run each specialist concurrently on the repository and live site, producing individual reports.

**Outputs:** `data_drift_specialist_report.md`, `overlay_ui_specialist_report.md`, `filter_logic_specialist_report.md`

### Phase 3: cross_critique

Each specialist reviews the others' reports, flags contradictions, and adds supplemental notes.

**Outputs:** `cross_critique_notes.md`

### Phase 4: coordinator_synthesis

Coordinator synthesizes all inputs into the final FINDINGS.md and a ranked implementation plan.

**Outputs:** `FINDINGS.md`, `plan_ranked.md`


## Cycle count

**2** cycle(s), minimum **3** engines per cycle.

## Verification required

`playwright_trace`

## Ship-today threshold

All critical root causes must have a concrete fix snippet and an automated verification step ready for immediate PR.

## Recommended invocation

```bash
# Phase 2 — parallel specialists (fan out to N engines, one per persona):
python tools/swarm/swarm_run.py \
    --prompt-file <your_prompt.md> \
    --engines deepseek,xai,kilo,cerebras \
    --persona data-drift-specialist \
    --max-parallel 4

# Phase 4 — coordinator synthesis on the run dir:
python tools/swarm/worker_runner.py \
    --engine claude \
    --prompt-file swarm_runs/<run>/_coordinator_input.md \
    --persona filter-bug-coordinator-synthesizer \
    --out-file swarm_runs/<run>/FINDINGS.json
```
