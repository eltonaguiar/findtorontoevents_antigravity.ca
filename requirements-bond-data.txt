# Opt-in dependencies for the BOND asset-class data + pricing sidecar
# (see alpha_engine/fred_data_fetcher.py, alpha_engine/bond_pricer.py)
#
# Install with:
#   python -m pip install -r requirements-bond-data.txt
#
# Both wrappers soft-fail on ImportError, so production never breaks if
# these are not installed. They are only needed when running the
# fred_data_fetcher / bond_pricer modules or the run_bond_signal_demo.py
# driver script.
#
# Licenses (all permissive, see PR body):
#   fredapi              — MIT
#   QuantLib-Python      — BSD-modified
#   pyarrow              — Apache 2.0

fredapi>=0.5
QuantLib-Python>=1.32
pyarrow>=14.0
