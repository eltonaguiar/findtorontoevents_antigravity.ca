{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: check_active_picks",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/alpha_engine/check_active_picks.py",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
