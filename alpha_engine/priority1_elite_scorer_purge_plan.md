# Priority 1: Purge Bad Picks in elite_scorer.py\n\n## Changes Made\n- Extended the existing `from config import CRYPTO_SYMBOLS` to include BLACKLISTED_STRATEGIES, BLACKLISTED_EXCHANGES, MIN_COPY_TRADER_PF, MIN_CLOSED_TRADES.\n\n## Next Changes\n1. Add `import re` after `import json`:\n   ```python\n   old: \"import json\\nimport os\"\n   new: \"import json\\nimport re\\nimport os\"\n   ```\n\n2. Add `should_skip_pick` function before `compute_elite_score`:\n   old: \"    return max(-6.0, min(10.0, bonus))\\n\\n\\n def compute_elite_score(\"\n   new: \"    return max(-6.0, min(10.0, bonus))\\n\\n\\ndef should_skip_pick(pick: dict) -> tuple[bool, str]:\\n    ... full function ...\\n\\n\\ndef compute_elite_score(\"\n\n3. Add filter check in `compute_elite_score` after `_flatten_extra_json`:\n   old: \"pick = _flatten_extra_json(pick)\\n\\nscore = 0.0\"\n   new: \"pick = _flatten_extra_json(pick)\\n\\nskip, reason = should_skip_pick(pick)\\nif skip:\\n    print(f\\\"[ELITE_FILTER] Purged {{pick.get('strategy', '?')}} {{pick.get('symbol', '?')}}: {{reason}}\\\")\\n    return {\\n        \\\"elite_score\\\": 0,\\n        \\\"elite_breakdown\\\": {\\\"_filtered_out\\\": reason},\\n        \\\"elite_grade\\\": \\\"F\\\"\\n    }\\n\\nscore = 0.0\"\n\n## Full should_skip_pick function\n```python\ndef should_skip_pick(pick: dict) -> tuple[bool, str]:\n    \"\"\"Check config filters and return (skip, reason).\"\"\"\n    strategy = pick.get('strategy', '').strip()\n    symbol = str(pick.get('symbol', '')).upper()\n    exchange = str(pick.get('exchange', '')).lower().strip()\n\n    if strategy in BLACKLISTED_STRATEGIES:\n        return True, f'blacklisted_strategy: {strategy}'\n\n    if not re.match(CRYPTO_SYMBOL_REGEX, symbol):\n        return True, f'non_crypto_symbol: {symbol}'\n\n    if exchange in BLACKLISTED_EXCHANGES:\n        return True, f'blacklisted_exchange: {exchange}'\n\n    # Copy trader check\n    strategy_lower = strategy.lower()\n    if any(prefix in strategy_lower for prefix in [\n        'copy_hl_', 'clone_hl_', 'bitget_copy', 'okx_copy', 'okx_futures_', 'hs_', 'binance_smart_money'\n    ]):\n        pf = float(pick.get('profit_factor', pick.get('pf', 0)) or 0)\n        closed_trades = int(pick.get('closed_trades', pick.get('trades', 0)) or 0)\n        if pf <= MIN_COPY_TRADER_PF or closed_trades < MIN_CLOSED_TRADES:\n            return True, f'bad_copy_trader: PF={pf:.2f}, trades={closed_trades}'\n\n    return False, ''\n```\n\n## Expected Result\nEnforced config blacklists in elite_scorer.py to purge bad picks. Added filter function, integrated into scoring. Changes: config import extension, should_skip_pick function, early return in compute_elite_score. Priority 1 complete.