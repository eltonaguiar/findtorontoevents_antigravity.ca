# Alpha Engine - Quantitative Research Platform
# Install: pip install -r requirements.txt

# Core
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# High-Performance Backtesting (NEW)
vectorbt>=0.28.0
bayesian-optimization>=1.2.0

# Data Sources
yfinance>=0.2.30
pandas-datareader>=0.10.0
requests>=2.31.0
beautifulsoup4>=4.12.0
feedparser>=6.0.0

# Machine Learning
scikit-learn>=1.3.0
lightgbm>=4.0.0
xgboost>=2.0.0
shap>=0.42.0
hmmlearn>=0.3.2

# Database
# 2026-05-12 fix (Q4 swarm consensus): both drivers used in repo.
# mysql-connector-python — legacy callers (alpha_engine/audit_sync.py, etc).
# pymysql — newer additions (active_picks_sync, anti_overfit_audit_sidecar,
# cot_paper_pilot, top_n_rank_backtest, cot_step7_risk_of_ruin_mc,
# zero_pnl_detector, verify_system_pf, cot_step7_friction_adjusted_mc).
# Prior absence here caused 5 sidecars to silently no-op in audit-dashboard
# workflow with "ERROR: pymysql not installed" until per-step pip-install
# patches landed (62c323578b1, fd04540cda2).
mysql-connector-python>=8.2.0
pymysql>=1.1.0

# Hurst Exponent
hurst>=0.0.5

# Visualization & Reporting
matplotlib>=3.7.0
seaborn>=0.12.0
jinja2>=3.1.0
plotly>=5.15.0

# Utilities
tqdm>=4.65.0
joblib>=1.3.0
pyyaml>=6.0
rich>=13.0.0
tabulate>=0.9.0

# Optional: sentiment
textblob>=0.17.0
vaderSentiment>=3.3.2

# Bootstrap inference (required by tools/block_bootstrap_ci.py via tools/feed_risk_metrics.py)
arch>=7.0.0
