{
  "_comment": "Copy to scanner_calibration_config.json and set enabled:true after validating buckets. Claude MCP path was .tmp-validation-mcp/scanner_calibration_config.json (not committed).",
  "enabled": false,
  "ml_score_buckets": [
    {
      "lo": 0.7,
      "hi": 1.0,
      "delta": 0.07,
      "note": "Example only — replace with exported isotonic bins (0–1 ml_score scale)"
    }
  ]
}
