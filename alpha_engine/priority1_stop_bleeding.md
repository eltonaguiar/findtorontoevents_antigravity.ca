# Priority 1 Enhancements: Stop the Bleeding\n\n## Changes Made\n\n### config.py\nAdded the following config vars after `TRAIL_ACTIVATE_PCT`:\n\n```python\n# ---------------------------------------------------------------------------\n# Priority 1: Stop the Bleeding - Junk Signal Filters\n# ---------------------------------------------------------------------------\nBLACKLISTED_STRATEGIES = ['binance_smart_money']\nBLACKLISTED_EXCHANGES = ['bitget']\nMIN_COPY_TRADER_PF = 10.01\nMIN_CLOSED_TRADES = 50\nCRYPTO_SYMBOL_REGEX = r'^[A-Z0-9]+USDT$'\n```\n\n### copy_trader_bridge.py\n- Added `import re`\n- Added `from . import config`\n- Added new skipped counters: `skipped_non_crypto`, `skipped_blacklisted`, `skipped_bitget`, `skipped_low_trades`, `skipped_low_pf`\n\n## Filter Logic to Add (insert after confidence filter)\n\nInsert this block after the `continue` of the `elif direction not in ("LONG", "SHORT"):` block:\n\n```python\n        # Priority 1: Hard filters - crypto-only, blacklists, PF>10, closed trades\n        symbol = pick.get("symbol", "").upper()\n        if not re.match(config.CRYPTO_SYMBOL_REGEX, symbol):\n            skipped_non_crypto += 1\n            continue\n\n        orig_strategy = pick.get("strategy", "")\n        strategy_lower = orig_strategy.lower()\n\n        # Blacklist strategies\n        if any(blacklisted.lower() in strategy_lower for blacklisted in config.BLACKLISTED_STRATEGIES):\n            skipped_blacklisted += 1\n            continue\n\n        # Block Bitget traders\n        if strategy_lower.startswith("copy_bitget_") or any("bitget" in src.lower() for src in pick.get("consensus_sources", [])):\n            skipped_bitget += 1\n            continue\n\n        # Closed-trade verification\n        forward_trades = int(pick.get("forward_trades") or 0)\n        if forward_trades < config.MIN_CLOSED_TRADES:\n            skipped_low_trades += 1\n            continue\n\n        # Hard-kill low-PF traders\n        strat_meta = strat_lookup.get(orig_strategy)\n        if not strat_meta:\n            for prefix in ("copy_hl_", "copy_hl_lb_", "copy_okx_", "copy_bybit_", "copy_bitget_", "copy_bingx_"):\n                if orig_strategy.startswith(prefix):\n                    label = orig_strategy[len(prefix):]\n                    strat_meta = strat_lookup.get(label)\n                    if strat_meta:\n                        break\n        perf = strat_meta.get("expected_performance", {}) if strat_meta else {}\n        pf = float(perf.get("profit_factor") or 0)\n        if pf <= config.MIN_COPY_TRADER_PF:\n            skipped_low_pf += 1\n            continue\n```\n\n## Update Print Statement\nUpdate the print to include new skipped:\n\n```python\n    print(f"  [COPY TRADER BRIDGE] {len(filtered)} picks passed filters "\n          f"(from {len(raw_picks)} total | "\n          f"skipped: low-conf LONG:{skipped_low_conf}, SHORT:{skipped_short}, "\n          f"incomplete:{skipped_incomplete}, non-crypto:{skipped_non_crypto}, "\n          f"blacklisted:{skipped_blacklisted}, bitget:{skipped_bitget}, "\n          f"low-trades:{skipped_low_trades}, low-pf:{skipped_low_pf})")\n```\n\n## Syntax Check\nRun:\n```bash\npython -m py_compile alpha_engine/config.py\npython -m py_compile alpha_engine/copy_trader_bridge.py\n```\n\nFiles ready for next priorities.