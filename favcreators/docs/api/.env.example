# Copy to .env and set your DB credentials. Do not commit .env.
# db_config.php reads this file first for MYSQL_PASSWORD, then getenv() / $_ENV.
#
# Password must match your MySQL user (e.g. in phpMyAdmin).
# Complex passwords (with # $ " ' & ! or spaces): use double quotes.
#   MYSQL_PASSWORD="your#p@ss$word"
# Simple passwords: unquoted is fine.
#   MYSQL_PASSWORD=testing123
#
MYSQL_HOST=localhost
MYSQL_USER=ejaguiar1_favcreators
MYSQL_PASSWORD=
MYSQL_DATABASE=ejaguiar1_favcreators

# Optional: debug logging DB (ejaguiar1_debuglog). Used when admin enables "Debug logging" in sign-in modal.
DEBUGLOG_MYSQL_HOST=localhost
DEBUGLOG_MYSQL_USER=ejaguiar1_debuglog
DEBUGLOG_MYSQL_PASSWORD=debuglog
DEBUGLOG_MYSQL_DATABASE=ejaguiar1_debuglog

# Events DB (ejaguiar1_events) - for /stats sync history. Set in .env.events or server env (EVENTS_MYSQL_PASSWORD).
# EVENTS_MYSQL_HOST=localhost
# EVENTS_MYSQL_USER=ejaguiar1_events
# EVENTS_MYSQL_PASSWORD=
# EVENTS_MYSQL_DATABASE=ejaguiar1_events
