# Funding-rate archive — populated by tools/funding_rate_collector.py
# (opt-in passive collector feeding hypothesis H-006; see PATH_TO_PROVEN_EDGE).
# Per-symbol <SYMBOL>.jsonl files are appended daily by the
# .github/workflows/funding-rate-collector.yml cron.
