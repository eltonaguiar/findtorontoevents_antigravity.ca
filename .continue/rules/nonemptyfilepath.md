---
description: Ensures that file creation commands include a valid filepath,
  preventing errors caused by missing or empty path arguments.
alwaysApply: true
---

Always provide a non-empty, non-whitespace-only filepath string when using the create_new_file tool.