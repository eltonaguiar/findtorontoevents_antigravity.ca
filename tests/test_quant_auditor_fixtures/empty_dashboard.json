{
  "_fixture_meta": {
    "name": "empty_dashboard",
    "rule_under_test": "ci:input-validation",
    "expected_verdict": "INSUFFICIENT_DATA",
    "expected_rule_id": "ci:input-validation",
    "notes": "Required keys are missing. Agent should emit INSUFFICIENT_DATA with input_validation.dashboard_data_ok=false and missing_keys populated. No claims should be evaluated."
  }
}
