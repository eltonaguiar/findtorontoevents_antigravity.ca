{
  "$schema": "https://app.kilo.ai/config.json",
  "mcp": {
    "redis": {
      "type": "local",
      "command": [
        "uvx",
        "mcp-server-redis",
        "--url",
        "redis://localhost:6379"
      ]
    }
  }
}
