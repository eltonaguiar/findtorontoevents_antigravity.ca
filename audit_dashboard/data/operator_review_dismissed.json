{
  "_note": "Audit trail of operator accept/reject decisions on items previously shown in audit_dashboard/data/operator_review_today.json. tools/build_dismissed_audit.py (TODO) appends entries on operator click. The dashboard's renderOperatorReviewToday() reads this file as an INSTANCE cache of 'last week's decisions' for the operator's mental model — without this, picks re-surface daily even after the operator already decided.",
  "schema": {"id":"string","decision":"ACCEPTED|REJECTED","reason":"string","decided_at":"iso8601","decided_by":"operator_id"}
}
