# MD Review Pass — 2026-04-13 23:00 UTC

## Scope

- Window: files modified in the last 4 hours (since ~2026-04-13T19:27Z).
- Excludes: `.git/`, `.claude/worktrees/`, `.kilo/worktrees/`, `.tmp-*`, `.pr41-review/`, `_codex_*`, `node_modules/`, `venv/`, `__pycache__/`, `.planning/archive/`, `.local/share/opencode/worktree/`.
- Tooling: Python stdlib walker against filesystem mtime (git log since=4h showed no matching commits).
- Files found in window: **1**.
- Files skipped (already reviewed from prior run): 0 (first run, no prior tracking file).
- Review cap: 30. Not hit.

## Summary

- ACTION_NEEDED: 1
- ALREADY_HANDLED: 0
- INFORMATIONAL: 0
- DANGEROUS: 0

## Files by classification

| Classification | File | One-line summary |
|---|---|---|
| ACTION_NEEDED | `MERCURY_APRIL132026.MD` | Mercury agent audit of `alpha_engine/adaptive_tp_sl.py` flagging missing `asset_class` labels on ~99.7% of closed picks, tiny calibration samples, and a list of concrete fixes (labeling, sample-size gate, edge-ratio guard, unit tests, atomic cache writes). |

## Actionable items extracted

Sourced from `MERCURY_APRIL132026.MD`:

1. Populate `asset_class` in the pick-generation pipeline (symbol-suffix heuristic: `USDT`->crypto, `USD`->forex, ticker list -> equity, etc.) so the adaptive TP/SL optimizer stops falling back to crypto defaults for non-crypto instruments.
2. Gate `refresh_adaptive_levels()` in `alpha_engine/adaptive_tp_sl.py` so per-strategy / per-symbol calibrations only kick in above a larger sample threshold (Mercury suggests 500 closed picks; current floor is 10/8).
3. Add an edge-ratio guard inside `refresh_adaptive_levels()` so `has_edge` only flips true when `optimal_tp_pct / optimal_sl_pct >= 1.2` (or tuned threshold).
4. Expand `DEFAULTS` and `DEFAULT_ATR_PCT` to cover `forex`, `equity`, `stock`, `commodity`, and `on-chain` instead of crypto-biased defaults.
5. Add a unit-test suite under `tests/test_adaptive_tp_sl.py` for `_extract_mfe_mae_pct`, strategy/symbol blending, and LONG vs SHORT handling.
6. Harden cache writes in `_ensure_cache()` with an atomic `os.replace` or file lock to avoid race corruption of `adaptive_tp_sl.json`.
7. Investigate the three open questions raised at the bottom of the file: signal generator producing picks with negative expected R, possible look-ahead / leakage, and whether live-time features still match the training distribution (references PR #145 `feature_drift.py` flagging 23/32 features as dead or drifted).
8. Follow through on the observed edge asymmetry: CRYPTO SHORT 73.7% (14/19) vs CRYPTO BUY 28.2% (905/3213) — worth validating independently before any sizing change, but is a concrete direction for crypto-short refinement.

## Red flags

None rising to DANGEROUS. Caveats worth tagging when acting on the file:

- The headline "Win-rate ~91.7% (11W/1L)" is from a 12-trade slice and Mercury itself calls it unreliable; must not be quoted as a live-system WR.
- The symbol-based `asset_class` heuristic in the recommendations is illustrative, not production code — needs to be reconciled with the existing Session-3 crypto-tagging fix at `dashboard_generator.py:4836-4851` so we don't regress the 1,142-pick recovery.
- The "CRYPTO SHORT 73.7%" number is on n=19 — do not feed into sizing or auto-deploy without a larger-n validation per `feedback_mutate_before_kill.md` / the kill-protocol docs.
- CLAUDE.md forbids running dashboard generators locally; any test harness added for adaptive TP/SL must stay away from the generator pipeline.

## Cross-reference to shipped PRs

Session PRs listed in the task prompt: #120, #123, #124, #133, #135, #136, #137, #138, #139, #142, #143, #144, #145, #146, #147, #149, #157.

| Actionable item | Likely overlap | Notes |
|---|---|---|
| #1 `asset_class` enrichment | Partial — Session 3 crypto-tagging fix (commit `e5f438505d`) already patched `_normalize_pick` for CEX-sourced crypto, but does not cover forex / equity / commodity. Mercury is asking for a broader generalization. |
| #7 feature drift / leakage | PR #145 (`feature_drift.py`) already flagged 23/32 features; Mercury is asking to act on those flags, not to re-run the audit. |
| #2 #3 #4 #5 #6 | Not obviously covered by the listed PRs — these are new asks against `alpha_engine/adaptive_tp_sl.py`. Verify before claiming "already handled". |
| #8 crypto-short edge | Adjacent to `feedback_long_source_bias.md` (luxalgo/dna_winner shorts on red BTC 4h) but n is far too small to act on from this doc alone. |

## Recommendation

Top 3 items worth doing next, in order:

1. Generalize the `asset_class` enrichment beyond the Session-3 crypto-only patch so `alpha_engine/adaptive_tp_sl.py` stops burning forex / equity / commodity picks on crypto defaults. This is the biggest unlock Mercury identifies and is the prerequisite for every other recommendation in the file.
2. Add the edge-ratio guard and sample-size floor in `refresh_adaptive_levels()` (items #2 + #3). Small, localized, and directly stops the optimizer from committing on 10-trade "edges".
3. Wire the tests Mercury asks for (`tests/test_adaptive_tp_sl.py`). Adaptive TP/SL is load-bearing across every asset class and currently has zero regression coverage — cheap to add and unblocks safely iterating on items 1 and 2.

Explicitly NOT recommended from this file: acting on the 73.7% CRYPTO SHORT number, or on the 91.7% headline win-rate. Both are n<25 slices.

## Process notes

- `.planning/md_review/reviewed_files.json` created alongside this report for incremental future passes (mtime + sha256 keyed).
- Future passes should load `reviewed_files.json`, skip files whose mtime+sha256 have not changed, and append new entries.
