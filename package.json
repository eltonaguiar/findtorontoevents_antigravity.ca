{
  "name": "findtorontoevents-verify",
  "private": true,
  "scripts": {
    "test:events": "playwright test events-loading.spec.ts",
    "test:events:ui": "playwright test events-loading.spec.ts --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.0.0"
  }
}
