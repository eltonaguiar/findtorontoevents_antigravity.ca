# PROTECT updates/index.html — DO NOT OVERWRITE

**CRITICAL: The file `updates/index.html` is a 600KB+ page with 150+ manually curated entries. It has been accidentally overwritten by agents MULTIPLE TIMES, replacing it with a tiny stub.**

## Rules

1. **NEVER regenerate `updates/index.html` from scratch.** Do not create a new file, do not use the Write tool to overwrite it, do not replace its contents wholesale.
2. **ONLY append new entries** to the existing file using StrReplace (find a known anchor point and insert above it).
3. **Before editing `updates/index.html`**, always read it first and confirm it is 500KB+ in size. If it is smaller than 100KB, something is wrong — do NOT save over it.
4. **If you need to update this page**, find the correct insertion point (e.g. above an existing `<!-- Feb` comment) and use StrReplace to add your new `<div class="update-entry">` block.
5. **NEVER use the Write tool on `updates/index.html`.** Always use StrReplace.

## Why This Rule Exists

This page has been overwritten at least 3 times by different agents who regenerated it from scratch, destroying 150+ entries of work. The local file at `e:\findtorontoevents_antigravity.ca\updates\index.html` is the source of truth — if the remote copy is small, redeploy the local copy using `python tools/upload_single.py updates/index.html`.
