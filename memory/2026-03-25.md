2026-03-25

- Enhanced copy-trader execution hygiene based on `docs/DEEPRESEARCH.txt`.
- Rewrote `alpha_engine/copy_trader_bridge.py` so it compiles again and now:
  - blocks `binance_smart_money`
  - blocks Bitget copy sources
  - blocks unverified clone feeds
  - requires score proxy >= 55
  - widens proven whale TP/SL to 8%/4%
  - only lets proven traders compete for live execution
- Hardened `alpha_engine/production_scanner.py` copy-trader ranking/gating:
  - blanket copy boost replaced by audited source tiers
  - vetted copy picks get priority
  - sentiment / Bitget / bad clones are penalized or rejected
  - portfolio copy quota now applies only to vetted copy sources
- Updated `alpha_engine/score_booster.py` to penalize `binance_smart_money` instead of boosting it.
- Fixed `copy_trader_intel/trusted_trader_tracker.py` to include `alpha_engine/data/closed_picks.json`, then re-ran it.
- Validation:
  - `py_compile` passed for the patched modules
  - `python alpha_engine/copy_trader_bridge.py` returned 0 vetted picks from the current 61-pick copy feed
  - current copy feed is still clones + Binance sentiment + Bitget, not proven Hyperliquid winners
  - re-run trust tracker now shows real closed performance for `copy_hl_NMTD_25M` and `copy_hl_whale_123M_87roi`

- Fixed `/audit` default-view regression after user reported "0 forex picks" and "11 crypto picks only".
  - Root cause: `audit_dashboard/template.html` and `audit_dashboard/index.html` had `f-score-tier` defaulted to `trade` (`score >= 50`), so the page loaded a curated crypto-only subset instead of the full visible universe.
  - Patched init to call `resetPickFilters()` on first load, set the score-tier default back to blank, and added a Playwright regression test that asserts default load matches `Clear All`.
  - Also replaced the browser-only RSI/volume kline fetches that hardcoded `data-api.binance.vision` with a Binance-host failover helper to avoid the visible CORS errors.
  - Rebuilt with `python -m audit_trail.dashboard_generator` and `python -m audit_dashboard.blueprint_generator`, then re-ran `tests/audit_metric_matrix_validation.spec.ts` successfully.

- Re-ran the live fix through the self-hosted GitHub Actions path to get the served site unstuck.
  - `Unified Audit Dashboard` workflow run `23530748431` completed successfully and redeployed the dashboard.
  - Live `https://findtorontoevents.ca/audit/` now serves `<option value="" selected>All Scores (no filter)</option>` instead of a preselected `trade` tier.
  - Browser smoke after deploy showed the live page at `410` active picks by default and `52` active forex picks when filtering `FOREX`, confirming the user-facing issue is fixed.
  - Remote Playwright still needs a timing-aware assertion update because live price refresh changes stale-pick visibility shortly after first paint (`404` at first capture vs `410` after refresh/clear), but that is a test stability issue, not the original forex/crypto visibility bug.

- Hardened `tests/audit_metric_matrix_validation.spec.ts` after the follow-up pass.
  - Replaced the stale shadow copy of dashboard filtering logic with calls into the dashboard’s real browser helpers (`getFilters`, `getBaseVisibleActivePicks`, `getBaseVisibleClosedPicks`, `matchFilter`, `getTrustTier`) so the test no longer drifts when blocked systems change.
  - Stabilized local Playwright by blocking external network mutation in local mode, which stops background live-price refreshes from changing active counts mid-assertion.
  - Tightened the remote default-load smoke to assert the actual invariant that matters on production: no hidden filter state and a self-consistent active count after `Clear All`, instead of recomputing backend aggregate summary fields from the recent-closed slice.
  - Validation after patch:
    - local `npx playwright test tests/audit_metric_matrix_validation.spec.ts --project="Desktop Chrome"` passed (`4/4`)
    - remote `VERIFY_REMOTE=1 ... -g "default load matches cleared filters and starts unfiltered"` passed
  - Pushed to GitHub `main` as commit `91d8694c33`.

- Reinvestigated the "Latest 100 Closed Picks QA" 33% WR alarm and found it was materially polluted by non-performance rows, not just weak trading.
  - The top-100 recent-closed slice in `audit_trail/data/dashboard_payload.json` was including `25` `FORCE_CLOSED_TOXIC` zero-PnL `yahoo_analyst_consensus` rows and active feed leakage from permanently killed `cta_tsmom_blend`.
  - `audit_trail/dashboard_generator.py` already had the resolved-pick filters, but the producer side was still emitting killed CTA / Yahoo rows, so the payload on disk could stay stale or be rebuilt from noisy inputs.
  - Patched `copy_trader_intel/cta_strategy_replicator.py` to hard-block `alpha_engine.auto_tuner.PERMANENTLY_KILLED` strategies before writing `cta_picks.json`, then re-ran:
    - `python copy_trader_intel/cta_strategy_replicator.py`
    - `python copy_trader_intel/multi_asset_copytrader_scraper.py`
    - `python copy_trader_intel/multi_asset_bridge.py`
    - `python -m audit_trail.dashboard_generator`
  - After rerun:
    - `copy_trader_intel/data/cta_picks.json`: `12` rows, `0` killed-strategy leaks
    - `copy_trader_intel/data/multi_asset_picks.json`: `32` rows, `0` Yahoo/killed leaks
    - latest 100 recent-closed now: `53W / 47L / 0 zero`, `53.0% WR`, `+315.75% total PnL`, `+3.158% avg`
    - active feed leak count for `cta_tsmom_blend` / `yahoo_analyst_consensus`: `0`
  - Also patched `copy_trader_intel/multi_asset_copytrader_scraper.py` summary printing to use the filtered post-kill-list result set; before that, the file on disk was clean but the operator console still printed the stale pre-filter total (`44`) instead of the saved total (`32`).
  - While rerunning the bridge, found that `alpha_engine/data/active_picks.json` had been in a malformed/conflicted local state earlier; `multi_asset_bridge.py` currently falls back to an empty baseline on JSON parse failure, so future hardening should treat malformed upstream active-pick files as a stop condition rather than silently replacing the baseline.
  - Remaining drag candidates after cleanup:
    - `funding_momentum`: `93` trades, `49.5% WR`, `-2.0%` total PnL
    - `claude_gainer_ml`: `10` trades, `50% WR`, `-7.96%` total PnL
  - Dashboard generator still logs concentration risk: capped stats say `115.4%` of total PnL is coming from `FETUSDT`, so future quality work should focus on anti-concentration guards rather than more kill-list cleanup.
