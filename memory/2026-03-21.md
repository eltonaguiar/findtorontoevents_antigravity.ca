## 2026-03-21

- Researched live crypto and forex copy-trading sources.
- Strongest crypto source found: BingX Copy Trading. Public leaderboard pages expose trader names, 30D PnL, max drawdown, Sharpe, AUM, and rank. The page also shows roughly 2,000 trader slots and a separate support article cites 5,500 active traders, which is enough for a 300+ candidate pool.
- Strongest forex source found: MQL5 Signals. The public `/en/signals` page exposes signal names, growth, reliability, algo-trading flags, copy price, and multiple category lists. It also states there are 8,000+ signals available.
- Bitget copy trading pages expose public trader profile IDs via article links, but the rendered profile pages are thin in this browser and often need JS to load.
- eToro, ZuluTrade, NAGA, and Darwinex all have public copy-trading or leaderboard pages with verified stats and large trader pools.
- The most useful forex names gathered came from MQL5 signal listings and eToro / Myfxbook profile pages.
- Local source audit: `copy_trader_intel/data/unified_trader_research.json` and `alpha_engine/data/unified_trader_research.json` are badly stale/under-populated (`total_raw: 5`, `total_deduped: 1`, only OKX contributing). The saved source coverage does not reflect the scraper surface area in `copy_trader_intel/`.
- Local data quality issue: `alpha_engine/data/portfolio_copytrader.json` currently contains unresolved merge markers and is not valid JSON.
- Current live-research conclusion: the best scalable crypto source is BingX, but it needs browser-derived signed requests; the best scalable forex source is MQL5 Signals, but direct crawling is intermittently blocked by anti-bot. Manual/source-level verification is strong; bulk extraction still needs hardened automation.
- Generated a resumable forex crawler scaffold at `tmp/mql5_forex_signal_crawler.py` with outputs `tmp/mql5_forex_signals.json` and `tmp/mql5_forex_signals.csv` (currently empty because the run hit MQL5 blocking).
- Follow-up instruction from user: for sources we cannot directly scrape, explicitly consider `scrapling` and its anonymous / stealth scraping features after the normal direct or browser-session route.
- Reworked copytrader lesson/mutation extraction to use realized `highscore_pick_history.json` outcomes and snapshot replay instead of proxy `avg_forward_wr` / unrealized-PnL heuristics.
- Cleaned variation generation so it only emits tradeable crypto symbols; invalid pseudo-symbols like `XYZ:CL`, `XYZ:GOLD`, and `XYZ:NATGAS` no longer contaminate copytrader variations.
- Fixed audit rebuild path so `audit_trail/dashboard_generator.py` now reapplies `alpha_engine.score_booster` before building `audit_dashboard/index.html`; previously the latest local dashboard render was silently using unboosted scores.
- Fixed `alpha_engine/score_booster.py` family classification to use the pick's own `strategy` / `source_system` only. Before the fix, unrelated systems could inherit copytrader boosts if `agreeing_systems` mentioned `ct_consensus_*`.
- Fixed `copy_trader_intel/copytrader_lesson_extractor.py` to use realized `CLOSED` history rows only when building `supporting_closed_history` and mutation replay sets. This removed mixed active/closed evidence from lesson scoring.
- Validation pass after the fixes:
  - `python copy_trader_intel/score_calibration_validator.py` passed
  - `python tmp/score_performance_validator.py` showed low-score hidden winners were mostly eliminated on the active payload
  - `npx playwright test tests/audit_copytrader_inspection.spec.ts --project="Desktop Chrome" --project="Desktop Edge" --project="Samsung Galaxy S21"` passed `9/9`
- Current copytrader browser stats after fixes:
  - `copy_trader_intel`: avg score ~68.1, WR ~58.2%, avg pnl ~+0.60%
  - `copy_trader_highscore`: avg score ~70.1, WR ~59.1%, avg pnl ~+0.66%
  - `copy_trader_consensus`: avg score ~81.6, WR ~60.9%, avg pnl ~+0.87%
  - closed copytrader `70+` bucket: 21 trades, WR 85.7%, avg pnl +2.23%
- User deployed a separate research agent to find transparent crypto prediction systems with verifiable results for study / possible reverse-engineering, while continuing to monitor live portfolios.
- Deployed a dedicated research agent (`James`) to hunt for transparent crypto prediction systems with verifiable, replayable results and direct source links; goal is to rank candidates by verification quality and reverse-engineerability.
- Research agent shortlist for transparent crypto systems with verifiable results:
  - Best target: BTC AutoTrader on MQL5 plus its official guide. Public signal page + rule writeup: BTCUSD only, H1, breakout from dynamic support/resistance, pending orders, trailing stop, news filter, spread control, no grid/martingale. Reported ~350 trades, 78% win rate, PF 1.26. High reverse-engineering value but broker/spread sensitive.
  - Second: Crypto Killer Infinity on MQL5. BTCUSD breakout system with automated risk management and explicit live signal stats. Reported ~361 trades, 57.06% winners, PF 1.36. Medium-high reverse-engineering value; still vendor-run and leverage-heavy.
  - Transparency benchmark: BingX Copy Trading / AI Arena surfaces public trader/model positions and visible entries/exits/PnL, but strategy logic is opaque and AI Arena Phase 1 ended on 2025-12-19, so current usefulness must be checked before deeper work.
  - Data-volume honorable mention: CRYPTO BTC ETH on MQL5 has large trade count and concentrated BTC/ETH exposure, useful for statistical reverse-engineering, but rule transparency is weak.
