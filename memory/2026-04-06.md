# 2026-04-06

- Created `docs/PREDICTIONS_DATABASE_SCORING_ANALYSIS_2026-04-06.md`.
- Key finding: the repo has two distinct systems that keep getting conflated:
  - `predictions/data/predictions.db` = raw social prediction ledger
  - `audit_trail/data/dashboard_payload.json` = live scored audit layer
- Live findings captured in doc:
  - social DB has 367 predictions / 246 predictors, but 0 active in exported leaderboard
  - all `asset_class` values are null
  - 37 rows still have invalid TP/SL geometry
  - 11 rows have impossible PnL below -100%, corrupting averages
  - live dashboard payload has 116 active picks, 17 zero-score picks, 2 smart picks at score floor 70, 11 at live code floor 60
- Implemented `predictions/historical_hygiene.py` and schema extensions in `predictions/db.py`.
- Applied hygiene pass:
  - backfilled all 367 prediction rows to `asset_class=crypto`
  - repaired/capped 11 out-of-range PnL rows
  - filled 4 missing resolution prices on resolved rows
  - flagged 37 rows as `is_invalid_execution=1`
- Updated predictor stats to use `valid_predictions` / `invalid_predictions`; leaderboard export now reflects cleaner predictor averages and win rates.
- Closed-pick scoring audit completed using `audit_dashboard/data/dashboard_data.json` `picks.recent_closed` (3500 rows, 2855 crypto).
- Added `docs/CLOSED_PICKS_SCORING_TWEAKS_2026-04-06.md`.
- Key findings from closed picks:
  - crypto score `60-69` is best band: 63.6% WR, PF 4.78
  - crypto `70+` is good but not better than `60-69`
  - `trust_score >= 5` is the strongest clean separator: 71.7% WR, PF 3.02
  - crypto `confidence >= 0.90` is toxic: 26.7% WR, PF 0.20
  - crypto `has_conflict=true` slightly outperforms `false`, so blanket conflict penalty looks too harsh
- Broadcast those findings on Redis bus as `codex-closed-audit`.
- Added `docs/DAY_OF_WEEK_TRADE_ANALYSIS_2026-04-06.md` using `audit_dashboard/data/dashboard_data.json` `picks.recent_closed` (3500 closed, 2855 crypto).
- Day-of-week findings:
  - Thursday is the worst large-sample day by far: overall 27.7% WR / PF 0.39, crypto 27.1% WR / PF 0.37
  - crypto Friday is strongest large-sample day: 62.7% WR / PF 3.16
  - Monday is strong; Sunday is weaker than Friday but near flat, not catastrophic
  - Thursday weakness is partly system-specific, especially `claude_gainer_st`
- Broadcast those findings on Redis bus as `codex-dow-audit`.
- Noted bus conflict: another agent pushed a Wednesday/weekend heuristic at 2026-04-06T20:28:12Z that does not match this larger closed-book analysis; future work should reconcile before hard-coding weekday penalties.
- Peer-claim validation completed and written to `docs/PEER_CLAIM_VALIDATION_2026-04-06.md`.
- Root cause of DOW inversion found:
  - correct close-day study uses `picks.recent_closed[].closed_at`
  - some peers likely used `timestamp` instead, which flips Thursday from worst to best
  - 302 rows have non-ISO `EST` timestamps; naive `fromisoformat()` drops them and changes counts
- Validated on `n=3500` with EST-aware `closed_at` parsing:
  - Thursday 27.0% WR / -1.246% avg
  - Monday 52.4% WR / +0.863% avg
  - Crypto Friday 62.7% WR / +0.772% avg
  - LONG beats SHORT (47.2% vs 31.4%)
  - trust>=5 remains strong (68.4% WR)
  - score 60-69 remains strong (58.0% WR)
- Broadcast methodology + results on Redis bus as `codex-peer-validate`.
- Reviewed external SQL dump `C:\Users\zerou\Downloads\ejaguiar1_stocks_apr62026_extract.sql` and wrote `docs/EJAGUIAR1_SQL_EDGE_REVIEW_2026-04-06.md`.
- Main SQL extract findings:
  - dump mixes upstream research tables (`alpha_*`, `algorithms`) with live execution tables (`at_*`)
  - algorithm descriptions materially overstate edge relative to realized `algorithm_performance`
  - live-style tables still contain invalid trade geometry, duplicated exposures, zeroed executable fields, and inconsistent outcome enums
  - strong immediate edge is stricter pre-score validation plus symbol-level and recency-weighted routing from `at_strategy_stats` / `at_strategy_symbol_performance`
- Posted file reference and summary to Redis bus as `codex-sql-review`; `broadcast` succeeded, while `announce` failed locally due a Windows paging-file error in the Python subprocess path.
