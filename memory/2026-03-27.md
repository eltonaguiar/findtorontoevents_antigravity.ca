2026-03-27

- Tightened `/audit` quality gates so active picks require a real final score, `score >= 50`, and no `LOW` trust-label or `BANNED` trust-tier rows.
- Added a narrow audited-PM exception: `prediction_market_consensus` rows can survive the score floor when they carry strong multi-source audited history.
- Smart Picks now respect concentration risk instead of ranking highly concentrated non-verified strategies as premium rows.
- Focused regressions passed:
  - `pytest -q tests/test_quality_gates.py`
  - `pytest -q tests/test_dashboard_generator.py tests/test_quality_gates.py`
- Full `python audit_trail/dashboard_generator.py` rebuild hung for >10 minutes, so I used the canonical payload fallback:
  - re-applied `passes_active_gate()` / `passes_smart_gate()`
  - refreshed summary counts and verified-alpha summaries
  - rebuilt `audit_dashboard/data/dashboard_data.json` and `audit_dashboard/index.html`
- Resulting rebuilt artifacts now show:
  - `30` active picks
  - `28` smart picks
  - `0` active rows with `score=0`
  - `0` active rows with score `<50`
  - `0` active rows with `LOW` trust or `BANNED` trust tier
- Generator-related artifacts drifted during the rebuild attempt (`dashboard_payload.json`, `dashboard_data.json`, `index.html`, `universal_resolved_picks.json`, `score_boost_log.json`), so treat this workspace as live and double-check before any commit/push.
