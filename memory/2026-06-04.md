# 2026-06-04

## FOREX pipeline kill-switched

**Rationale:** PF 0.47, WR 23%, FOREX destroying credibility. Full class-wide halt.

**Actions executed:**
- Set `FOREX_HARD_DISABLE=1` as GHA variable (explicit, previously relied on code default "1" only)
- Disabled `forex-agent.yml` and `forex-smart-picks.yml` workflows
- Verified: `passes_active_gate(pick)` returns `False` for FOREX picks with `FOREX_HARD_DISABLE=1`
- Verified: gate layered properly — directional gate also blocks FOREX LONGs independently

**State:**
- Config: `alpha_engine/config.py:329` (default "1", already ON)
- GHA variable: `FOREX_HARD_DISABLE=1` set 2026-06-04T00:17:46Z
- Workflows: both FOREX workflows disabled
- Existing active picks (15 + 20) will resolve naturally; no new emissions

**Re-enable criteria** (per `docs/FOREX_HARD_DISABLE_RATIONALE.md`):
- Carry-factor backtest PF>1.0, WR>45%, n>30 over 30d
- Set `FOREX_HARD_DISABLE=0` in GHA variables
- Re-enable workflows

## Compounded PnL investigation

**Dashboard headline -89.8% was a display artifact** from deprecated `_compound_equal_weight_capped_sequence()`
with a 2% per-trade cap that destroyed the edge at scale (wins lost 61% of value, PF dropped 1.38→1.06).
The honest metric `total_pnl_pct_compounded_rolling_100` = +0.97% (last 100 trades).

**Fixes applied:**
- Swapped dashboard headline `total_pnl_pct` from deprecated EW compound → rolling-100 metric
- Bumped compound cap from 2% → 10% (only 55 trades capped vs 1,154)

## Intrabar OHLC replay ported to universal_pick_resolver.py

- Unified OHLC pre-fetch for ALL symbols (yfinance for non-crypto, Binance klines for crypto)
- SL-first conservative ordering in `_check_tp_sl_intrabar` (mirrors tournament's `_scan_bars_for_touch`)
- Added `import time`, normalize-then-classify for forex detection
- `outcome_resolver.py` already had intrabar OHLC wired — no changes needed
