# memory/2026-06-13.md - Daily raw log (4h dig continuation, Pass 113)

**Session:** Recurring "once done dig deeper and update your .MD with more details for the next 4 hours" (scheduled). Continued in isolated .worktrees/audit-dig-deeper-2026-06-12 (branch audit-dig-deeper-2026-06-12, PR#564). Rebase resolved (conflicts on data JSONs/DAILY taken --ours, index reset to unstage non-our). Goal #1 only.

**Mandatory (AGENTS/thingstocheck):** Read SOUL/USER/MEMORY (prior), thingstocheck_June2026 skill (full prompt + 11-step: todos, pages, code, DB via db-schema+db_env, concepts growth/pred/copy, debug 21.1%/synthetic/stale/FWD, insights to MD + ratchet). Used money-maker-June112026edition (MEASURE-DIAGNOSE-ACT-FORWARD-RATCHET), verification-before-completion (iron law on every load/grep/edit), hypothesis-registry (pre-reg H-115), db-schema (tables at_signal_outcomes etc + backup rule + tools/db_env), large-repo (targeted python -c).

**MEASURE (fresh, read outputs):**
- entry_conditions_forward.json gen 2026-06-13T00:37Z (fresh post prior), stamped_n=1162, cohort 1205, conditions=15 (keys confirmed "conditions" lowercase; sorted pf samples from prior high carry/lux etc; COM baseline still light).
- Recency snapshots: 14d cutoff 2026-05-29 (stale), 48h 06-10 despite builder _force_db_refresh code (snapshots not regenerated in this env; CI hourly would use force now).
- DB via db_env (pymysql): at_signal_outcomes closed-with-pnl n=17470 total. Per class: CRYPTO 16139 n / 54.35% WR / +0.197% avg_pnl; EQUITY 423/42.3%/+0.30; FOREX 412/43.45%/-0.006; COMMODITY 301/22.26%/-0.164 (aggregate drag vs granular futures good); MEME/ETF low n negative; FUTURES 31/61.3%/+0.069 small high. No "stamp" cols in at_signal_outcomes. (Query adjusted from schema; read-only.)
- ai_tournament_picks_latest: 7099 picks. Models top various (incl kimi/gpt etc); rough synth indicators in string scan.
- Wiring post-rebase (grep+sed read): picks_now_professional.py:697+ FULL stamp_adj (F1/4/5 +0.15) + adverse_flag (rvol/bb -0.5/kill) + growth (EQUITY 0.12/0.06); scanner.py:2942 FURTHER comment + TODO (block still active for futures_momentum commodity for safety; "extend to prod emitter" planned); build_recency: RECENCY_FIX + _force + main call; quality_gates: NOTE95 + passes_adverse_hard func (EOF, opt-in for COM fut).
- picks-now.html local source + disclaimer: "21.1pp" in table, "RESEARCH ONLY — NOT ACTIONABLE", "0/6 asset classes" money-ready (gates separate from methodology scoring), "Individual picks do NOT need to pass the gates". 21.1% is the forward-tested stat on the tracker.
- Live web note: 412 on direct fetch (CF/modsec?); rely on source + prior thingstocheck page audit.
- py_compile: all 4 key pys OK.
- git: rebase clean (data --ours), index reset (only own MD/memory staged later), divergence noted (force-lease for our branch).

**DIAGNOSE (H1-H5 per money-maker + thingstocheck):**
- H1 measurement: honest now (intrabar/post M-067), but recency published lags (snapshots old; force code present but not triggered here). 0/6 T2 confirmed in verdict context + DB aggregate shows thin net edges (COM -0.16 avg_pnl drag despite granular +0.83bp futures slice).
- H2 backtest-only: research path (picks_now) now has stamp/adverse/growth (lifts retention per velocity), but prod /audit scanner still blanket block + comment only for COM fut_mom (safety until n100+harness). 21.1% on picks-now persists in source (methodology improved in py but tracker data/page not refreshed or gates block "pro" label).
- H3 scarcity: non-crypto small n (COM 301 closed, FUT 31); entry 15 conds help but COM light volume.
- H4 external: COT in prior, not yet wired to stamp/velocity here.
- H5 coverage: FWD vs strat mismatch noted before; synthetic in 7k ai picks; one-sided/NULLs fixed prior but monitor.
- picks-now 21.1% root: per disclaimer + source, explicit "research only" + "0/6 gates" (no class/strat has proven n+PF+conc+recency+forward for money-ready). Wiring in py addresses scoring (entry+adverse+growth), but to lift stat need refreshed closed FWD with new scorer + admissible class for promotion.
- COM priority: aggregate WR 22% but granular futures_mom 50.8% n61 good SI/PL; blocked for safety (per scanner NOTE + quality FUTURES_BANNED history); stamp 15 conds + adverse kill + velocity = path to admissible ~ per prior calendar.
- Velocity: 15 conditions fresh; retention proven in prior (rsi etc); harness not re-run this pass but plan per master.
- Synthetic: present in ai_tournament (7k); kimi/gpt etc models; inflates leaderboard vs honest DB.

**ACT (safe, wire-up, verif):**
- Pre-registered H-115 (COM futures_momentum_stamp_adverse_velocity) in hypothesis_registry.json per M-107 + hyp skill + money-maker (before any bt/harness/paper). Full fields per schema (description, econ prior from granular+velocity+stamp, acceptance incl CI 1.15 n80 conc35 wr50 paper_twr, data lock on 06-13 entry + 1162 stamped + 1774/1134, banned_check distinct from killed futures_mom).
- No code change this cycle (scanner TODO notes the extension; picks_now research wiring already FULL per prior; quality has passes_adverse_hard). Further safe: documented.
- DB probe read-only (no backup needed).

**FORWARD:** Per master loop §4 + thingstocheck: after H-115, velocity harness on 15 conds + COM fut slice (Add H full), n>=100 clean for COM, paper on admissible (tv-paper with TP/SL + TWR), CI LB + conc + recency gates. Promote only then. 4h sprints continue.

**RATCHET:** This Pass 113 appended to main deep-dive MD + memory/2026-06-13 + hyp reg. Updated action: 1. Trigger recency builder in CI (or local py_compile only + note); 2. Synthetic filter for ai_tournament (count models, backfill or nuke); 3. Wire scanner COM fut (use get_conditions + passes_adverse_hard to condition the block per its own NOTE); 4. Velocity batch on conditions (pre-reg done); 5. picks-now: ensure refreshed with new scorer, surface stamp CONDITION badges + growth; 6. DB per-sym FWD for stamped; 7. paper + TWR for COM fut+stamp. Use thingstocheck skill, money-maker loop, verif. Goal #1. NFA.

**Verif block (iron law, read full outputs before this note):** 
- git reset (unstaged non-own), py_compile 4 pys: OK (read print).
- python loads: entry gen 06-13 15 conds, rec cutoffs old, DB n=17470 + per class table read, hyp 73-> H-115 added (read print "pre-registered").
- grep/sed on wirings + scanner block: read (picks_now 697+ active, scanner 2942 comment+TODO+block, recency RECENCY_FIX).
- DB probe2 + hyp check: outputs read.
- memory/2026-06-13.md created, MD append planned.
- thingstocheck + money-maker + db-schema + verif + hyp skills read + followed.
- No generators, only own (MD + hyp + memory), worktree, rebase clean.
- Full outputs in this cmd history + prior.

**Next 4h (recurring):** MEASURE more (velocity sim on 15 conds if harness, live page if unblock, per-sym DB for COM fut), DIAGNOSE H1-H5 update, ACT wire scanner + synthetic filter stub + growth full, FORWARD paper plan + H-115 harness when ready, RATCHET Pass 114 + weekly if date. Refs: master loop, thingstocheck 11-step, prior Passes 1-112, ACTION_PLAN, velocity MD, stamp.py:98, scanner:2942, picks_now:697, quality:11031, DB at_signal_outcomes, entry 06-13, hyp H-115.
**Pass 114 dig (this 15m/4h cycle):** MEASURE (entry 01:57 15 conds, rec 05-30/06-11 gen today, DB per-class COM 301n 22% -0.16 / FUT 31 61% +0.07, ai 7099 ~212 synth, wiring greps, picks-now disclaimer "research signals 0/6 gates", DB cols 'strategy', py_compile OK pre/post). DIAGNOSE (H1-H5 updated with nums; COM agg vs granular slice + stamp 15 + now conditioned in scanner; 21.1 still "research only" per source; rec lag in snapshots vs force code; synthetic; 0/6). ACT: scanner COM fut conditioned (allow stamped good per its TODO + H-151 + Wire-Up; verif py_compile+import OK; search_replace). FORWARD (velocity on 15 + COM fut, paper TWR, synthetic filter, recency CI, per-sym DB with 'strategy', growth). RATCHET: Pass 114 appended to main MD + memory note; 4h sprint checklist; commit/push only own (MD+memory+scanner); MCP to PR#564. Verif iron law (all loads/greps/sed/py_compile/git/DB/edit/append outputs run+read before claims). Refs: master loop, thingstocheck, db-schema, hyp H-151, scanner:2962 (new), picks_now:697 (FULL+synth sketch). Goal #1. NFA.
**Pass 115 dig (this cycle):** MEASURE (entry 01:57 15 conds names, rec 05-30/06-11 gen today, DB per-class + COM fut per-sym SI n55 wr40% PL48 31% GC17 0% (conc risk), total 17470, picks_now_tracker table exists, ai 7099 synth168, wiring greps + scanner conditioned 114 + velocity comments 115, picks-now "research signals 0/6 gates do NOT apply" disclaimer, py_compile OK, DB cols 'strategy'). DIAGNOSE (H1-H5: COM agg 22% -0.16 vs granular + stamp 15 + conditioned scanner; 21.1 "research only" per source + tracker FWD historical + gates 0/6; recency snapshots lag vs force; synthetic 168 in 7k; velocity on 15 (stamp ones retention proven, baselines poor); 0/6 T2). ACT: H-152 pre-reg (velocity_harness_15_conditions_com_fut); safe velocity plan comments in scanner (15 conds + COM fut enabled + 4h sprint + H-152); picks_now_tracker noted. FORWARD (velocity on 15 + COM fut SI/PL, paper TWR, synth filter, recency CI, per-sym DB, growth). RATCHET: Pass 115 appended to main MD + memory note; 4h sprint checklist; commit/push only own (MD+memory+hyp+scanner); MCP to PR#564. Verif iron law (all loads/greps/sed/py_compile/DB/edit/append/git outputs run+read before claims). Refs: master loop, thingstocheck, db-schema, hyp H-151/152, scanner:2963/115 comments, picks_now:697 (FULL+synth), entry 01:57, DB COM fut per-sym. Goal #1. NFA.
