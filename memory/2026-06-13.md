# memory/2026-06-13.md — Grok 4.3 session notes (Goal #1 audit / pro-level picks)

**Date:** 2026-06-13 (today per user_info). Continuation of 4h/15m dig-deeper recurring + explicit "drop a .MD summarize your work you accomplished, what remains, and what you suggest to do to get us to pro-level picks on findtorontoevents.ca/audit and https://findtorontoevents.ca/audit/picks-now.html". Also "once done dig deeper and update your .MD".

**Key decisions / actions this turn:**
- Followed AGENTS "Every Session": read SOUL.md (taste, no slop, concrete refs, diary/easter but not interrupt flow), USER.md (sparse), memory files (06-13 new, 06-12 huge — used grep/offset in mind via prior), thingstocheck_June2026 skill (full prompt + 11-step workflow for pages/DBs/21.1%/synthetic/stale/FWD/gates), using-git-worktrees (detected not isolated in root on audit-fix-ingestion; switched ops to .worktrees/audit-dig-deeper-2026-06-12).
- Used worktree: cd + git stash + fetch + pull --rebase origin audit-dig-deeper-2026-06-12 (succeeded after skips; ahead 17 post; unstaged non-own left un-added), status clean baseline for our deltas.
- Read skills mandatory (verification iron law, large-repo targeted python loads not cat, audit-pick-flow pipeline/gates, money-maker master loop MEASURE/DIAGNOSE/ACT/FORWARD/RATCHET, db-schema for safe).
- Fresh targeted loads (verdict 2026-06-12T20:00, entry 15 conds stamped with retention examples, pf, 14d/48h, web fetches of exact /audit + /picks-now for current disclaimers/gates 0/6/21.1% context + money-ready table).
- Confirmed wirings in wt (grep: picks_now 697+ stamp/adverse/velocity/21.1% root + tracker FWD 647 + COM fut + "research signals 0/6 gates do NOT apply"; scanner 2942+ FURTHER + conditioned good slice n~61 50.8%/1.586 SI/PL; quality adverse NOTE95).
- py_compile 4 key files OK in wt (picks_now, scanner, recency builder, quality_gates) — read output.
- Dropped the exact requested .MD: reports/2026-06-13-grok-audit-progress-summary-accomplished-remaining-pro-level-path.md (full: accomplished wt+skill+wirings+Passes+verifs+HF, remains 5 P0s incl recency lag + n<100 + wiring gap + synth + no paper, 12pt prioritized suggestions with master loop + COM first + velocity/stamp/adverse + 14d48h first + paper after gates + document/deploy + NFA + cites file:line/JSON/web).
- Appended Pass 118 to main deep-dive (tail anchor + new content on drop + verifs + state).
- Created wt memory/2026-06-13.md (this).
- Verifs before any claim (loads/greps/py_compile/tails/git status only-own/web read).
- Plan next: git add *only* the 06-13 MD (+ deep-dive delta + wt memory if tracked), detailed commit, push --force-with-lease (per AGENTS safe), MCP github comment on PR#564 (search schema first), todos complete, ratchet note.
- Lessons: thingstocheck prompt perfectly captured the 21.1% + pages + DB + velocity integration needs; web pages confirm disclaimers/gates 0/6 exactly as diagnosed (research vs strategy graduation); COM per-sym granular (SI/PL) is the admissible hook inside class drag; recency force code present but publish lag is real P0 (14d/48h first rule violated until fixed); always rebase + only-own in wt even with dirty root.

**Context for continuity:** 0/ classes T2 (web + JSONs); COM best risk/reward via velocity retention + stamp pre + adverse kill + per-sym (futures_mom good slice); 21.1% on picks-now is "research signals" + "gates 0/6 do NOT apply to individuals" + historical tracker locked (new wiring for future lift); disputed CRYPTO/EQUITY on /audit per dedup/synth/conc. Master loop + HF playbook + pre-reg (H-15x) + verif iron driving.

**Next (if continue):** Recency CI root (grep CI + publish), full harness table on 15 conds + COM fut (read-only), paper on admissible, more ratchet.

**No mental notes** — everything written to MDs/memory/reports. NFA. Goal #1.

(End of 2026-06-13 memory entry. Update with sig events.)
**2026-06-13 ~03:31 UTC deeper dig (Pass 119 execution):** 
- Rebase (up to date post prior), stamp --stdout run (table + JSON gen 03:30 read; 15 conds, COM baseline 43n 20.9/0.515, crypto_rsi retention 48.3/1.454 n58, high ret slices, neg filters).
- Loads (verdict/pf/entry/recency 20:xxZ; pf COM12n33/0.82 CRYPTO single-src heavy; entry 15 conds as stamp; 14d/48h gen but old cutoffs).
- Grep recency CI root (audit-dashboard.yml:614 + pick-funnel-nightly plain build_recency calls no --force-db; builder fallback; explains lag).
- Code reads (picks_now synth downweight *active* code score-25 for cursor/kimi/llama + detailed 21.1 comments; scanner conditioned COM fut).
- Python CONDITIONS analysis (high retention list vs baselines decay; COM weak in entry/pf).
- Appended Pass 119 to deep-dive (full tables/insights/plan for remaining 4h: yml fix, tracker probe, harness on conds, synth expand).
- Updated this 06-13 summary MD with dig deeper section.
- Verifs: stamp output read, greps/loads/analysis read, py_compile prior OK, git non-own only.
- Will: selective add (MDs + this memory), commit, push --force-lease, MCP PR#564 comment.
- Insights: velocity retention real on stamp conds (crypto/forex/equity good hold vs crypto baseline decay); 21.1 now has synth penalty *live* in research path + prior stamp/adverse wiring (future tracker lift); recency P0 is yml invocation (not just builder code); COM needs per-sym + conditioned fut_mom + n accrual; 0/ T2.
- Next ticks: DB/tracker probe safe, sims on top conds, yml --force-db note/PR, ratchet.
Refs: stamp run, entry JSON, yml:614 etc, picks_now:757+, scanner:2963+, loads, analysis py. Goal #1. NFA.

**03:34 UTC pro-level todos execution (Pass 120):** 
- pro-1 rebase/stash handled (non-own untouched).
- pro-2: recency P0 fixed (yml --force-db + builder arg parse; force always via _force). py OK.
- pro-3: DB probe tracker (recent daily n 28-356, high avg scores 91-109; historical FWD sample for 21.1); COM used prior granular.
- pro-4: retention analysis +15-18pp on stamp conds vs decay; COM baseline low.
- pro-5: picks_now synth expanded (cursor/deepseek/synthetic check), STAMP_COND badge signal added.
- pro-6: MDs updated (deep 120, 06-13 exec section), memory. Commit/push only (builder, ymls, picks_now, MDs, mem). MCP PR.
- pro-7: all verifs (py, DB output, greps, git only-own, tails). Rebase, Goal#1, no gen.
Key: recency CI in prod now, 21.1 lift, COM fut path ready. Continuing ratchet.
