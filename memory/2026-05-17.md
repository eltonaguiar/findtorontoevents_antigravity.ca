# 2026-05-17 — 2-Week Picks Autopsy + Proven Edge Blueprint

## Core Deliverables
- Ran `tools/pick_traceback.py --days 14` → `reports/pick_traceback_14d_20260517.md` (977 resolved closed picks, WR 29.5% PF 8.00).
- Produced full postmortem + actionable blueprint: `reports/2week_picks_postmortem_blueprint_20260517.md`
  - Discrimination test: confidence inverted (higher on losers), elite/ml scores mostly noise, only method_a_score + risk_reward (mixed) + 2-3 COT families show real separation.
  - High-conviction vs smart_picks: curation layers exist (smart_picks_engine, high_conviction_enhancements, hf_*_json) but not fully attributed back into closed records.
  - Miss analysis: crypto/EQUITY near-zero in resolved set; no automated "missed top gainer autopsy" loop yet (top movers vs emitted + gate-failure root cause).
  - Exact prompt template for next multi-AI swarm/CLI round (Grok cloud first + Kimi/Cursor/etc with your keys) to turn the autopsy into concrete gate/score code changes + weekly_filter.md per money-maker-readyv2 spec.

## Key Decision / Lesson
The "no real-money edge" is not mysterious — it is the direct, measurable result of inverted + noisy ranking signals + extreme asset-class skew + missing hindsight-on-misses loop. Edge is real but narrow (COT 72-77% WR families). Everything else is variance harvesting.

## Next (ready for user)
1. Feed the blueprint + traceback + latest DAILY_IDEAS + dashboard_data.json into your cloud CLI agents (Grok + paid keys first) + local swarms using the prompt in the blueprint.
2. Implement `tools/missed_gainers_autopsy.py` (symmetric to traceback).
3. Land minimal filter fixes behind flags, re-verify with traceback + v2 success criteria.
4. Update holographic + MEMORY.md with the "COT is the only proven high-WR vertical so far" fact.

All per AGENTS.md (no auto forbidden scripts, own changes only, doc every step). This session advanced Goal #1 (per-class proven edge) from "confirmed none" to "here is exactly why + the exact input for the AI swarm to fix it".

## Additional Deliverable (user request)
- Created master roadmap at user request: `reports/PATH_TO_PROVEN_EDGE_GROK_2026-05-17.md` (170 lines, 13 KB).
  - Brutal current-state diagnosis from the 14d data.
  - Non-negotiable definition of "Proven Edge" (per-class n/WR/PF/Kelly bar matching money-maker-readyv2).
  - 5-Phase path (Stabilize & Instrument → Concentrate on real COT edge → Per-class surgical repairs via cloud-AI-first swarms → Ensemble & sizing → Live verification).
  - Explicit prioritization for the next swarm round (CRYPTO & EQUITY first after COMMODITY).
  - Success metrics and immediate actions (instrumentation + missed_gainers_autopsy + cloud-key consult round using the prompt in the 2-week blueprint).
  - This file + the 2-week postmortem now form the complete "Path to Proven Edge" package for the multi-model synthesis loop.