2026-03-29

- `/audit` frontend integrity fix: removed the GitHub raw `dashboard_data.json` runtime override from `audit_dashboard/index.html` and `audit_dashboard/template.html`, standardized the page on same-origin `data/dashboard_data.json`, added source badges (`SITE JSON` vs `EMBEDDED`), and added a visible red-flag banner when the all-assets active universe collapses far below the loaded feed without explicit filters.
- Pushed the fix safely to `origin/main` from a clean temporary worktree as commit `16b21b6699` because the main worktree had unrelated Monte Carlo result file changes and `origin/main` advanced during the first push attempt.
