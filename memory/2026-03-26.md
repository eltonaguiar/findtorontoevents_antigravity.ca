2026-03-26

- Investigated `/audit` crypto/forex win-rate mismatch.
- Confirmed live audit page reads GitHub raw JSONs, not MySQL directly:
  - `audit_trail/data/dashboard_payload.json`
  - `alpha_engine/data/smart_picks.json`
  - `alpha_engine/data/smart_picks_history.json`
- Root cause 1: Smart Picks was trusting raw copy-trader profile stats (`forward_wr`) and raw feeds (`copy_trader_binance`, `copy_trader_bitget`, `clone_hl_*`) instead of only vetted copy picks.
- Root cause 2: dashboard active payload was leaking resolved rows (`TP_HIT`, `SL_HIT`) and zero-entry prediction-market signals into the Active Picks / unrealized P&L feed.
- Realized closed performance that contradicted the optimistic feed labels:
  - `copy_trader_binance`: 8 closed, 37.5% WR, negative total PnL
  - `copy_trader_bitget`: 3 closed, 0% WR, negative total PnL
  - `polymarket_prediction`: 3 closed, 33.3% WR, slightly negative total PnL
- Local fixes applied:
  - `alpha_engine/smart_picks_engine.py`: block unvetted raw copy feeds, trust forward stats only for vetted copy picks, route copy picks through `copy_trader_bridge`
  - `audit_trail/dashboard_generator.py`: keep only open active rows with usable `entry_price`
- Verified locally after regeneration:
  - `alpha_engine/data/smart_picks.json`: 4 picks, no raw Bitget/Binance/clone entries
  - `audit_trail/data/dashboard_payload.json`: 149 active rows, all `OPEN`, zero zero-entry rows
- Production state still inconsistent after investigation:
  - live `smart_picks.json` updated cleanly
  - live `dashboard_payload.json` still showed resolved rows in active feed
  - live `smart_picks_history.json` still reported older 54.37% Smart WR from mixed historical batches
- Forex copy-trader lineage audit:
  - `copy_trader_intel/data/forex_copytrader_picks.json` is generated by `multi_asset_copytrader_scraper.py`
  - active forex copy picks are mirrored into `multi_asset/data/active_picks.json` by `multi_asset_bridge.py`
  - `/audit` ingests that mirror as source `multi_asset`, not `multi_asset_copytrader`
  - dashboard expects `multi_asset/data/multi_asset_closed.json` as the paired closed file, but that file is from the legacy `multi_asset/scanner.py` path, not the new forex copy-trader bridge
  - current realized forex copy closures instead land in `alpha_engine/data/closed_picks.json` through generic alpha-engine closure logic (TP/SL hits + stale closures)
  - observed current repo state: 16 open forex copy rows in `forex_copytrader_picks.json`, 9 realized forex copy closes in `alpha_engine/data/closed_picks.json` (33.3% WR, net negative), so active and closed lineage are only partially aligned
- Reviewed Grok's 2026-03-26 dashboard/repo feedback against the repo/runtime path.
- Integrated Grok review into TODO docs:
  - validated fast repo progress, reduced non-crypto bleeding, and honest outcome visibility as the next leverage point
  - corrected overstatements that dashboard regeneration alone is enough
  - promoted publisher unification, Smart WR history cleanup, and forex lineage repair above MySQL-only or regen-only fixes
- Tightened TODO wording so PM is treated as experimental rather than "completely dead," and regeneration is no longer listed as the first standalone next action.
- Reviewed Grok's additional PM integration patch.
- Findings:
  - most PM loader/UI wiring is already present in `audit_trail/dashboard_generator.py` and `audit_dashboard/template.html`
  - the proposed wildcard JSON source path is not compatible with the current loader shape
  - `consensus_outcomes.json` is cross-system, not PM-only, so it cannot be used as a clean PM 5+ conviction source
  - the real PM blocker is still zero-entry signal rows + weak outcome resolution + blunt dedup collapsing PM tiers
- Verified and completed the next highest-value pass: publisher unification for Smart Picks on `/audit`.
  - `audit_trail/dashboard_generator.py` now embeds `smart_picks_feed` and `smart_picks_snapshot_summary` into `dashboard_payload.json`
  - `audit_dashboard/template.html` / `index.html` now read Smart Picks + Smart WR from embedded payload instead of separate GitHub-raw `smart_picks.json` / `smart_picks_history.json` fetches
  - `.gitignore` now ignores `audit_trail/data/.generator.lock`, and the tracked lockfile was removed from the repo tip
  - committed/pushed repo tip observed at `16bbfbf7cec299da536e13d2d549dd8421d063d6`
- Re-verified after the push:
  - `origin/main` and `HEAD` match, but the working tree drifted again immediately with regenerated data/dashboard artifacts
  - current payload on disk is mechanically consistent for the runtime-unification change: active summary count matches active rows, embedded Smart Picks objects are present, and `index.html` no longer references raw `smart_picks.json` or `smart_picks_history.json`
  - current diagnostics still show weak alpha quality, not a publish-path bug:
    - `python alpha_engine/check_active_picks.py` -> 105 active picks, score-to-PnL correlation `-0.0661`, overall health `39/100`
    - `python alpha_engine/decile_test.py` -> overall moderate signal, but `SHORT` and `FOREX` remain broken and `CRYPTO` is only weak
  - current active feed includes one legitimate `pm_whale_signals` row with real `entry_price` and `rr_ratio=2.0`; this is not the earlier zero-entry PM signal leakage issue

- Completed source-side stale-ledger cleanup for the worst unresolved systems.
  - `predictions/audit_push.py` now runs `validation.price_validator.validate_all()` before export/push, and `predictions/local_scheduler.py` now refreshes the active ledger instead of only rewriting the leaderboard.
  - `quan_engine/audit_push.py` now validates and re-exports `active_signals.json` before push, `quan_engine/failover_system.py` now maps `KASUSDT`, and `quan_engine/forward_tracker.py` now caches per-symbol prices during validation so large duplicate clusters do not rate-limit themselves.
  - `alpha_engine/outcome_resolver.py` now time-exits stale `rapid_fire_data/now_picks.json` rows and writes `_resolved_*` metadata; `audit_trail/dashboard_generator.py` now reads that metadata so resolved rapid-fire history counts as real W/L/F instead of excluded shadow rows.
  - Verified after regeneration:
    - `predictions`: `245 resolved`, `0 excluded` (was `245 resolved`, `324 excluded`)
    - `rapid_fire`: `476 resolved`, `15 excluded` (was `8 resolved`, `318 excluded`)
    - `quan_engine`: `93 resolved`, `0 excluded` (was `91 resolved`, `52 excluded`)
    - overall `auto_expired_excluded` fell from `1049` to `576`
- Remaining-issues pass:
  - verified `fix(audit): align multi-asset closed lineage` had landed on `main` under commit `48c7250161`
  - validated in-memory that bridge-managed forex/non-crypto closes from `alpha_engine/data/closed_picks.json` now normalize back to their logical dashboard systems instead of being mislabeled as `alpha_engine`
  - observed current normalized forex closed counts in the loader:
    - `multi_asset`: 17 closed forex rows (mostly CTA/non-crypto bridge lineage)
    - `multi_asset_copytrader`: 9 closed forex rows
    - `alpha_engine`: 0 closed forex rows after remap
  - normalized multi-asset forex closed WR from the loader now computes to `47.1%` (`8W / 9L / 0F`)
  - pushed follow-up UI clarity fix to `origin/main` as commit `b3691ee6a0`:
    - Smart WR card in `audit_dashboard/index.html` now reads `Smart Snapshot` when the embedded metric is snapshot-based and explicitly says it is not execution-accurate
  - could not regenerate `dashboard_payload.json` locally because a live `audit_trail.dashboard_generator` process held `.generator.lock`; avoided killing it

- Polymarket whale tracker follow-on work completed:
  - repaired `prediction_market_agents/polymarket_whale_tracker.py` to consume the newer shared-scanner metadata instead of the older raw-WR path
  - direct position inference now prefers adjusted WR (`crypto_win_rate_bayes` / `history_wr_bayes`), carries raw WR separately, and includes archetype / recent-form / concentration metadata in `whale_data`
  - shared vetted picks now propagate `leaderboard_source`, crypto profit/volume ranks, adjusted history WR, profile score, sample tier, recent-form scores, archetype, and top-symbol concentration into downstream whale signals
  - `scan_whales()` backfill logic restored: if shared scanner returns fewer than `top_n`, it fills from trade-discovered crypto wallets, then direct-fetches only wallets without shared-scanner signals
  - re-created `tests/test_polymarket_scraper_scoring.py` (missing on disk) with focused coverage for Bayesian WR, rank sorting, recent form, concentration, and archetype classification
- Verification for the PM follow-on pass:
  - `python -m py_compile copy_trader_intel/polymarket_scraper.py prediction_market_agents/polymarket_whale_tracker.py tests/test_polymarket_scraper_scoring.py` passed
  - `pytest -q tests/test_polymarket_scraper_scoring.py` passed (`6 passed`)
  - live `scan_polymarket_traders(max_traders=30)` returned `20` qualified crypto wallets and `16` live picks
  - live `scan_whales(top_n=15)` returned `15` traders and `20` signals (`10` vetted wallet-copy + `10` direct-position signals)
  - live selected wallets now expose archetypes such as `high_prob_bond`, `milestone_fade`, `hft_micro`, `market_maker_systematic`, `directional_generalist`
  - notable live names in the top cohort included `justdance`, `coinman2`, `BoneReader`, `0x8dxd`, and `0xf705fa045201391d9632b7f3cde06a5e24453ca7`
- PM follow-on status after this pass:
  - DONE: Bayesian WR shrink + minimum-sample gate
  - DONE: wallet archetype classifier
  - DONE: 30d/90d recent-form scoring + single-symbol concentration metrics wired into rankings and whale signals
  - NEXT: latency/arb detector, liquidity/entry-quality filter, PM strategy audit notebook

- Follow-up after Claude review of the PM path:
  - validated that Claude was right about two source-side issues:
    - non-copyable archetypes (`hft_micro`, `market_maker_systematic`) were still emitting raw PM picks/signals because `copyable_archetype` was only a confidence penalty, not a hard gate
    - raw PM JSON outputs still contained opposite-direction picks on the same symbol even though downstream PM merge logic later resolves conflicts
  - validated that one Claude claim was incorrect:
    - `profile_crypto_recent_score_30d` was not all zero; raw PM picks had non-zero values such as `0.016`, `0.2718`, and `0.8831`
  - found an extra bug Claude did not call out:
    - `prediction_market_agents/polymarket_whale_tracker.py` had been polluted with research seed-wallet injection from Kimi/POLY notes, including politics/sports wallets and strategy boosts, which contaminated the crypto-only whale output
  - fixes shipped:
    - `copy_trader_intel/polymarket_scraper.py` now hard-blocks non-copyable archetypes from emitted picks
    - `copy_trader_intel/polymarket_scraper.py` now resolves raw symbol direction conflicts before saving `polymarket_picks.json`
    - `prediction_market_agents/polymarket_whale_tracker.py` no longer overrides live scanner profiles with research seed wallets / seed boosts
    - `prediction_market_agents/polymarket_whale_tracker.py` now filters to copyable profiles and resolves direction conflicts before saving whale signals
    - trade-discovery fallback wallets now default to `copyable_archetype=True` / `wallet_archetype='unknown'` so backfill still functions after the hard gate
    - re-added `tests/test_polymarket_scraper_scoring.py` again after it disappeared from disk and extended it with a direction-conflict test
  - post-fix verification:
    - `py_compile` passed for `copy_trader_intel/polymarket_scraper.py`, `prediction_market_agents/polymarket_whale_tracker.py`, and `tests/test_polymarket_scraper_scoring.py`
    - `pytest -q tests/test_polymarket_scraper_scoring.py` passed (`7 passed`)
    - live `scan_polymarket_traders(max_traders=30)` now returns `20` qualified profiles but only `5` emitted raw PM picks after non-copyable gating + conflict resolution
    - live emitted pick archetypes are now only `high_prob_bond` and `milestone_fade`; no `hft_micro` / `market_maker_systematic` picks remain
    - live `scan_whales(top_n=15)` now returns `15` traders and `12` conflict-resolved whale signals with `0` non-copyable signals

- Completed the next PM item: explicit latency / arbitrage detector.
  - `copy_trader_intel/polymarket_scraper.py` now computes:
    - `latency_arb_flag`
    - `latency_arb_score`
    - `latency_arb_title_share`
    - `latency_arb_short_expiry_share`
    - `latency_arb_ultrashort_share`
    - `latency_arb_midprice_share`
    - `latency_arb_avg_hours_to_expiry`
    - `latency_arb_tracked_market_share`
  - detection combines explicit micro-market title patterns (`up or down`, `5/10/15/30 min`, `hourly`), short expiry windows, ultrashort expiry share, mid-priced contracts, and wallet volume into a bounded latency-arb score
  - final copyability now uses a dedicated gate layer:
    - `archetype_copyable` preserves the archetype-only decision
    - `copyable_archetype` is the final downstream gate
    - `copyability_gate_reason` distinguishes `hft_micro`, `market_maker_systematic`, and `latency_arb`
  - downstream whale outputs now carry latency metadata through vetted picks, direct whale signals, and summary trader rows
  - extended `tests/test_polymarket_scraper_scoring.py` with a dedicated latency-arb regression
- Verification for the latency-arb pass:
  - `python -m py_compile copy_trader_intel/polymarket_scraper.py prediction_market_agents/polymarket_whale_tracker.py tests/test_polymarket_scraper_scoring.py` passed
  - `pytest -q tests/test_polymarket_scraper_scoring.py` passed (`8 passed`)
  - live `scan_polymarket_traders(max_traders=30)` still returns `20` qualified profiles, but now only `4` emitted raw PM picks after archetype + latency gating + conflict resolution
  - final profile gate reasons on the live run:
    - `9` gated as `hft_micro`
    - `2` gated as `market_maker_systematic`
    - `1` gated explicitly as `latency_arb`
    - `8` remained `ok`
  - notable explicit latency-arb catch on the live run: `coinman2` was no longer copyable because it scored `latency_arb`
  - live `scan_whales(top_n=15)` now returns `15` traders and `9` conflict-resolved whale signals (`4` vetted + `7` direct before conflict cleanup)
  - live selected trader gate reasons are all `ok`; latency-arb / HFT / MM wallets are filtered before emission

- Completed the next PM item: liquidity / entry-quality filter.
  - `copy_trader_intel/polymarket_scraper.py` now has a shared `_evaluate_entry_quality(...)` gate built from:
    - directional notional
    - lead-market notional
    - contract price quality
    - expiry horizon
    - a small breadth component
  - vetted PM symbol aggregation now tracks lead market per winning direction instead of using a possibly opposite-side lead title
  - emitted PM picks now carry:
    - `entry_quality_score`
    - `entry_quality_gate_reason`
    - `entry_quality_contract_price`
    - `entry_quality_directional_notional`
    - `entry_quality_lead_notional`
  - `prediction_market_agents/polymarket_whale_tracker.py` now:
    - imports the shared entry-quality helper instead of maintaining separate copyability logic
    - sorts direct positions by value before selecting one per symbol
    - skips direct positions that fail expiry / contract / notional quality
    - carries entry-quality metadata into direct and vetted whale outputs
  - removed a stale Kimi seed-wallet / strategy-boost block from `prediction_market_agents/polymarket_whale_tracker.py`; it was dead code, polluted the file, and currently contained invalid Python syntax
- Verification for the entry-quality pass:
  - `python -m py_compile copy_trader_intel/polymarket_scraper.py prediction_market_agents/polymarket_whale_tracker.py tests/test_polymarket_scraper_scoring.py` passed
  - `pytest -q tests/test_polymarket_scraper_scoring.py` passed (`10 passed`)
  - live `scan_polymarket_traders(max_traders=30)` returned `20` qualified profiles and `4` emitted raw PM picks
  - live raw PM pick entry-quality scores were approximately `0.9175`, `0.9175`, `0.9667`, and `1.0`
  - live `scan_whales(top_n=15)` returned `15` traders and `5` conflict-resolved signals:
    - `4` vetted wallet-copy signals
    - `1` direct-position fallback signal
  - the direct fallback is now materially cleaner:
    - micro `up or down` signals were filtered out
    - a far-dated December 2026 BTC contract was filtered out on expiry horizon
    - the surviving direct signal was a same-day BTC milestone contract (`Will Bitcoin dip to $66,000 on March 26?`) with entry-quality score `0.6906`

- Audited handoff commits `32e89efa2d` and `9572caf9b1` against current repo/runtime instead of trusting the prose.
  - Both commits are docs-only; several claims were stale by end of day.
  - Verified live MySQL schema currently includes `FUTURES`, `ETF`, and `COMMODITY` on the audit DB ENUM columns, including `at_discord_sent` and `at_sqlite_imports`.
  - Verified current PM status is not blocked by the old handoff items:
    - `scan_polymarket_traders(max_traders=30)` currently returns `20` traders and `16` picks.
    - `scan_whales(top_n=15)` currently returns `31` traders and `21` signals, though a few research-seed names still 400 on direct-position fetch.
    - `scan_kalshi()` currently returns `36` analyses and `3` live signals; the “9 events, 0 markets” note is stale.
  - Verified `adaptive_stops` is already wired inside `production_scanner.py`; that TODO was stale.
  - Quan status is narrower than the older handoff claimed:
    - `alpha_engine/data/closed_picks.json` now has `486` `quan_engine` rows with `0` missing `strategy` and `0` missing `elite_score`.
    - Current isolated-source intake has `3` Quan picks; `TAOUSDT / quan_engine_swing` passes current tier gates, while `MATICUSDT` and `RENDERUSDT` are paused as `MUTATION-PENDING`.
    - `alpha_engine/data/active_picks.json` still has `0` live Quan picks, so the old “verify Quan appears in active_picks with elite_score” monitor is not green yet.
  - Found one genuinely unfinished runtime bug in `alpha_engine/outcome_resolver.py`:
    - the resolver returned early when `closed_picks.json` had no unresolved rows, so it never reached the `claude_gainer_ml` zombie cleanup path on quiet cycles.
    - patched the resolver so source-specific cleanup still runs, and switched claude-gainer price lookup to use `pair` / `coin_id` instead of the truncated display ticker.
    - after rerun, `rapid_fire now_picks` resolved `5` more rows and `claude_gainer_ml/tracker/claude_live_picks.json` dropped from `65` ACTIVE to `9` ACTIVE (`191` resolved).
  - Monitoring reality after the audit:
    - dashboard recent-closed currently shows `44` forex rows and `0` TP hits, so the forex-calibration monitor is still red.
    - direct copy-trader closed rows in `alpha_engine/data/closed_picks.json` are currently `3W / 10L` (`23.1% WR`), worse than the older `31%` baseline.
    - trio bot currently has `0` active and `0` recent-closed rows, so that monitor has no sample yet.

- Audit dashboard data-quality pass on `findtorontoevents.ca/audit`:
  - patched `audit_trail/dashboard_generator.py` so active non-crypto picks with symbols like `GBPJPY=X` and `CL=F` enter the live-price enrichment path instead of being skipped as non-equity/non-crypto edge cases.
  - patched the Claude performance import to enrich `claude_gainer_ml_perf` rows from `claude_pick_history.json` / resolved tracker rows, instead of the thin `recent_10` summary that lacked entry/exit prices.
  - patched `baby_strats_forward` normalization to carry through `exit_price` and `exit_time`, and added generic closed-pick fallbacks so missing `exit_reason` can use `outcome/status` and missing `exit_price` can be derived from `entry_price + pnl_pct` when the source clearly represents a resolved trade.
  - regenerated the full dashboard payload/html after each patch and re-QA'd the live payload slices.
  - before the fixes:
    - active slice had missing live prices on several non-crypto rows
    - closed slice had `795` missing `exit_price`, `860` missing `exit_reason`, and `827` missing `closed_at`
  - after the fixes:
    - active slice is `15` rows with `0` missing `current_price`
    - `claude_gainer_ml_perf` is `10` rows with `0` missing `entry_price` and `0` missing `exit_price`
    - closed slice is down to `0` missing `exit_price`, `2` missing `exit_reason`, and `52` missing `closed_at`
  - remaining `closed_at` gaps are concentrated in `kimi_riseoftheclaw`, `ml_bg_system_f`, `kimi_signal_tracking`, and a few isolated rows where the source ledger does not expose a trustworthy close timestamp; I stopped there instead of fabricating close times.

- PM runtime follow-up:
  - `audit_trail/dashboard_generator.py` now backfills prediction-market labels during normalization, so PM rows in the final dashboard payload are tagged (`🔮 PM Whale`, `🔮 PM Vetted`, `🔮 PM Kalshi`, `🔮 PM Momentum`) even when the raw source JSON omits `type_label` / `trader_label`.
  - verified in `audit_dashboard/data/dashboard_data.json` that the surviving PM crypto rows now render with explicit PM badges instead of null labels.
  - `audit_trail/universal_pick_resolver.py` now includes PM source ledgers (`alpha_engine/data/polymarket_signals.json`, `prediction_market_agents/data/momentum_signals.json`, `prediction_market_agents/data/whale_signals.json`, `prediction_market_agents/data/kalshi_signals.json`) and snapshots zero-entry PM rows into tradeable entry/TP/SL levels before resolution.
  - `prediction_market_agents/kalshi_signal_agent.py` now emits live spot-backed entry / TP / SL, and the latest `prediction_market_agents/data/kalshi_signals.json` refreshed to `5` live tradeable rows with `0` zero-entry signals.
  - `prediction_market_agents/polymarket_momentum_agent.py` currently refreshed to `0` live rows rather than stale zero-entry placeholders.
  - `alpha_engine/data/polymarket_signals.json` can still emit signal-only rows with no entry snapshot on this machine when `api_failover.fetch_price()` comes back empty, but the new PM-aware resolver fallback now covers that case downstream.
  - full `python -m audit_trail.universal_pick_resolver` is too heavy to finish inside a short validation window here (timed out at ~5 minutes), so PM resolver validation this session was via unit tests plus refreshed source ledgers and dashboard rebuilds rather than a full end-to-end close cycle.

- Verified-alpha alignment pass for `/audit`:
  - `alpha_engine/prediction_market_consensus.py` now emits real `entry_price` / `take_profit` / `stop_loss` by inheriting trade frames from contributing wallet/market picks and falling back to live spot when needed; merged consensus rows are `OPEN`/`ACTIVE` instead of zero-entry `SIGNAL` ghosts.
  - `audit_trail/dashboard_generator.py` now snapshots live entry/TP/SL for signal-only PM rows during price enrichment, so PM consensus / Kalshi rows are no longer dropped by active-feed hygiene solely for having `entry_price=0`.
  - `audit_trail/quality_gates.py` and `alpha_engine/score_booster.py` now give stronger preference to verified PM / auditable pro-trader rows (`prediction_market_consensus`, `pm_whale_signals`, `polymarket_signals`, curated `multi_asset_copytrader`) instead of treating them like generic crypto signals.
  - relaxed Binance MTF / ensemble penalties for verified-alpha rows in `score_booster.py`; Binance TA no longer fully vetoes already-verified PM/copy ideas.
  - verified with `py_compile` + `pytest` (`14 passed`) and a full `python audit_trail/dashboard_generator.py` rebuild.
  - latest local payload after rebuild:
    - `28` active picks, `15` smart picks, `43.0%` overall WR
    - active book: `9/28` verified PM / curated copy (`32.1%`)
    - smart book: `8/15` verified PM / curated copy (`53.3%`)
    - `0` active or smart picks with score `<50`, score `0`, or missing core trade fields
  - visible winners from the pass:
    - `prediction_market_consensus` now has a live `DOGEUSDT` smart pick on the final payload
    - `pm_whale_signals` now has `3` active and `3` smart picks on the final payload

- Root-cause fix for `/audit` headline mismatch:
  - added a dedicated `verified_alpha` cohort summary in `audit_trail/dashboard_generator.py`, including:
    - active/smart counts
    - source mix
    - realized cohort WR/PnL
    - audited upstream WR using sample-shrunk PM / pro-trader history fields
  - added a dedicated `🔮 Verified Alpha` filter + tab + summary cards in `audit_dashboard/template.html` so PM / auditable pro-trader rows are no longer visually buried inside the all-systems headline.
  - also fixed preset filter hygiene in the dashboard UI so Best / In Profit / Proven / Smart / Verified Alpha presets fully reset conflicting state before applying their own view.
  - regression coverage:
    - `tests/test_dashboard_generator.py` now asserts verified-alpha summary segmentation works
    - full generator/quality test slice passed: `15 passed`
  - operational caveat:
    - a long-running `python audit_trail/dashboard_generator.py` process hung after the code change, so I backfilled the new `verified_alpha` block directly into `audit_trail/data/dashboard_payload.json` and rebuilt `audit_dashboard/index.html` with `dashboard_generator.build_html(payload)` instead of waiting on the stuck full run.
  - resulting local artifact state:
    - `verified_alpha_active_picks = 9`
    - `verified_alpha_smart_picks = 8`
    - `verified_alpha_audited_wr = 68.8%`
    - `verified_alpha_realized_wr = null` (no resolved verified-alpha forward trades yet)

- Verified-alpha audited WR propagation fix:
  - `audit_trail/dashboard_generator.py` was dropping `history_wr_bayes` / profile audit fields during `_normalize_pick`, which made PM/pro-trader systems look like `0.0%` WR even when upstream audited history existed.
  - fixed normalization to preserve `history_wr_bayes`, `profile_crypto_wr_bayes`, and `profile_crypto_wr`.
  - added `_refresh_verified_alpha_system_stats(...)` so final enriched active picks can backfill audited system WR after strategy-forward stats are attached later in the pipeline.
  - rebuilt payload + HTML successfully with `generate()` and `build_html(payload)`; current local artifact now shows:
    - `polymarket_signals`: `win_rate_basis=audited`, `display_win_rate_pct=56.3`, `audited_wr_coverage=1`
    - `pm_whale_signals`: `win_rate_basis=audited`, `display_win_rate_pct=61.3`, `audited_wr_coverage=2`
    - `multi_asset_copytrader`: `win_rate_basis=audited`, `display_win_rate_pct=71.0`, `audited_wr_coverage=3`
    - `prediction_market_consensus` still has no audit/forward history propagated, so it remains `win_rate_basis=none`
  - Systems tab cards in `audit_dashboard/template.html` now show audited WR with `WR Basis` / `Audit Coverage` rows instead of misleading `0%` realized WR for verified-alpha feeds.
  - regression coverage now includes the forward-WR fallback case; `pytest -q tests\\test_dashboard_generator.py tests\\test_quality_gates.py` passed `17` tests.
