# 2026-03-09

- User clarified that MySQL `ejaguiar1_stocks` should be the primary source for DNA strategy performance.
- Added `genome/mutate_top_performers.py` with source priority: MySQL `at_signal_outcomes` first, CSV fallback second.
- Verified MySQL connectivity and discovered `dna_genome` currently has no closed outcomes; script now supports all `source_system` values by default and optional filtering.
- Ran full MySQL-driven mutation batch:
  - `top_n=10`, `mutations_per_parent=8`
  - Output: `genome/results/top_performer_mutations_20260309_205001.json`
  - Registered 80 new mutations in `genome/strategy_registry.db`.
