# 2026-05-07

## Session Resume After Freeze
Previous session (2026-05-06) was mid-work when it froze. All changes were mine (Buffy/Codebuff session, branch `chore/remove-freebuff-2026-05-04`). Nothing from other agents needed.

### Completed Before Freeze
- 5 strategies killed in `PERMANENTLY_KILLED_STRATEGIES` + `BANNED_SYSTEMS`:
  `futures_momentum` (0% WR, PF 0.00), `quan_engine` base (0 closed/0 active), `combined_confidence` (PF 0.28), `forex_rsi2_mean_reversion` (PF 0.37, n=593), `cta_commodity_momentum_term` (PF 0.02), `smart_money_accumulation` (PF 0.20)
- 3 FOREX LONG direction blocks via `BLOCKED_DIRECTION_TRIPLES`:
  `ig_contrarian_sentiment`, `myfxbook_retail_contrarian`, `quan_engine_swing`
- `tools/backfill_regime_labels.py` created → already ran → all 7,867 picks now have regime labels ✅
- `alpha_engine/outcome_resolver.py` P0-A patches in place:
  `_preserve_score_fields()` function + calls at lines ~1705 and ~2220
- 4 audit docs written to `updates/`:
  `2026-05-06-edge-action-plan-audit-status.md` (with post-swarm session updates),
  `2026-05-06-day2-audit-kills.md`,
  `2026-05-06-forex-mutation-decisions.md`,
  `2026-05-06-gha-cancellation-fixes.md`,
  `2026-05-06-backtests-db-migration-script.md`
- GHA runner cancellation fixes in `safe_push.sh`
- `tools/migrate_backtests_to_backtests_db.py` (DB migration helper)
- `tools/holographic_memory.py` (agent memory tool)

### Resumed 2026-05-07
- Verified: regime = 7867/7867, score/trust_score/smart_score/grade = 0/7867
- `score`/`trust_score`/`smart_score`/`grade` are **never written** by any active scoring pipeline — these are dashboard tooltip labels, not actual data fields. `elite_score` is the real scoring field (6,398/7,867 populated). P0-A patches preserved correctly.
- Reverted `audit_dashboard/template.html` (unintentional encoding normalization)
- All changes committed on `chore/remove-freebuff-2026-05-04` (commits `ef363fecac5` + `62f5b10d934`)

### Code Review Fix (2026-05-07 mid-session)
- Code review identified two gaps in `_preserve_score_fields()`:
  1. `strat_fwd_wr` is nested under `forward_validation`, not top-level — always returned None (dead code)
  2. `method_a_score` and `ml_composite_score` (actual scoring fields) missing from the preserve list
- Fixed both in `alpha_engine/outcome_resolver.py`: removed `strat_fwd_wr`, added `method_a_score` + `ml_composite_score` + `ml_score` (alias)
- Rebase conflict resolved cleanly; cherry-picked onto `chore/remove-freebuff-2026-05-04` → commit `62f5b10d934`
- Final branch: `chore/remove-freebuff-2026-05-04` @ `62f5b10d934`

### Post-lunch session (after 12:00 UTC)

**swarm-sync-v2.yml bug (fixed):** `CHANGED_FILES="$CHANGED_FILES $file"` in `tools/swarm_sync.sh` accumulated leading space → `git add " agent_shared_memory.json"` → exit code 127. Fixed with bash array: `CHANGED_FILES=()` + `CHANGED_FILES+=("$file")` + `git add "${CHANGED_FILES[@]}"`. Pushed to main as `458677309d1`. Next cron run will execute correctly.

**freebuff-b setup:**
- Peer `freebuff-b` registered via freebuff adapter (WebSocket primary, HTTP fallback)
- Heartbeat sent: `{"ok": true, "transport": "ws"}` — status `ready`
- Message queue clean (0 pending messages)
- Broadcasts dispatched to all peers: swarm-sync fix notification + FREEUFF greeting

**Cross-PC Protocol key discoveries:**
- `to=all` is a real peer ID in the gateway's offline queue — broadcasts stored under `"all"` peer inbox, not individual peer inboxes
- WS broadcast is live-only (no persistence); `to=all` goes to queue (durable, pollable)
- `freebuff_adapter.py` only polled own inbox (returned empty for broadcasts)
- Added `--poll-peer` flag to poll additional peer inboxes alongside own
- **`OfflineQueue.push()` deduplication bug** (user fixed, commit `ed87cf5346c`): queue was not deduplicating by `message_id` → duplicate messages on poll
- **Auto-ACK on poll** (user fixed, commit `07a59e079ed`): gateway now auto-ACKs messages when polled
- **Identity fix:** Cursor traffic was mislabeled as `claude-main` due to explicit `--peer-id`; now resolved via `--runtime cursor` → `cursor-desktop-<hostname>`
- Identity resolution order: 1) `--peer-id`, 2) `CROSS_PC_PEER_ID` env, 3) inferred `<runtime>-<hostname>`

**Quickstart scripts created:**
- `tools/protocol_quickstart.ps1` (Windows/PowerShell)
- `tools/protocol_quickstart.sh` (Linux/WSL/bash)
- Fixed: PS1 called wrong adapter (`registry_adapter.py` → `freebuff_adapter.py`)
- Fixed: SH had dead `$PAYLOAD` heredoc with broken JSON escapes (`\\\"`)

**Files committed today:**
- `tools/swarm_sync.sh` (bash array fix) → commit `458677309d1`
- `tools/adapters/freebuff_adapter.py` (new, with `--poll-peer` flag) → commit `9164fd5afa8`
- `tools/protocol_quickstart.ps1` + `.sh` → same commit
- `MEMORY.md` (updated with cross-pc learnings + swarm-sync fix)
- `memory/2026-05-07.md` (updated with full session log)

**Workflow status:**
- `swarm-sync-v2.yml` — cron works, manual `workflow_dispatch` blocked by GitHub cache (workflow object created before `workflow_dispatch` added)
- `swarm-janitor.yml` — clean, daily at 04:00 UTC ✅
- `GH_PAT_AGENTS` secret confirmed working on both workflows