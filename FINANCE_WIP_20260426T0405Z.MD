# Finance / Audit-Engine WIP — 2026-04-26T04:05Z

Snapshot of the Kimi-audit hardening cycle (started 2026-04-25). Captures **what's merged**, **what's queued behind a remote agent**, and **what's still on the backlog** so the next session (or a different operator) can pick up cleanly.

---

## 1. Completed in this cycle (5 PRs merged to `main`)

| PR | Merge SHA | Scope |
|---|---|---|
| **[#411](https://github.com/eltonaguiar/findtorontoevents_antigravity.ca/pull/411)** | `0452fed72d` | **ML-2** — apply isotonic calibrator at `score_signal` inference (was fitted at train time but never `.predict()`'d, leaving the meta-label gate operating on raw `predict_proba`; root cause of Kimi's headline Cohen's d=0.011). **W1-T7** — post-clamp R:R re-validation in `compute_adaptive_tp_sl`; falls back to category defaults when `tp_pct/sl_pct < MIN_TP_SL_RATIO`. |
| **[#416](https://github.com/eltonaguiar/findtorontoevents_antigravity.ca/pull/416)** | (squash) | 12 unit tests for ML-2 + W1-T7. New Playwright spec `tests/audit_js_errors_kimi411.spec.ts` against live `https://findtorontoevents.ca/audit` with JSON artifact baseline. Surfaced 3 console errors caused by `api.allorigins.win` failing as the client-side CORS proxy for HYPEUSDT klines. |
| **[#418](https://github.com/eltonaguiar/findtorontoevents_antigravity.ca/pull/418)** | `115615cb7e` | Skip the allorigins step entirely for `_NOT_ON_BINANCE` symbols (HYPE et al.) — they were burning ~10s on a guaranteed-empty proxy hop before CryptoCompare picked it up. Drop allorigins timeout 10s → 4s. (Acts on Copilot's review of #416.) |
| **[#421](https://github.com/eltonaguiar/findtorontoevents_antigravity.ca/pull/421)** | (squash) | New `fetchViaCorsProxy(url, 5000)` helper tries `api.allorigins.win` then `corsproxy.io`. Wired into the 4 yfinance / Google Finance call sites in `fetchStockPrices`. New **KuCoin OHLCV tier** added to `fetchBinanceKlinesWithFailover` between the proxy block and CryptoCompare — restores true 3-host parity on the client side, mirroring `crypto_data_failover.py`. |
| **[#427](https://github.com/eltonaguiar/findtorontoevents_antigravity.ca/pull/427)** | `6db8e97c9e` | **W1-T5** — `TP_PERCENTILE = 60.0` constant replaces three magic 75.0 literals; lowers TP target from p75 to p60 of winner MFE because winners only reached p75 ~25% of the time before reversing (audit's TIME_EXIT root cause). **ML-1** — module logger + `logger.warning(...)` replaces the bare `except: pass` that was silently dropping ML prediction errors and falling through to the heuristic scorer. |

### Tests now in place
- `tests/test_isotonic_calibrator_branch.py` (8 tests) — ML-2 logic incl. real `IsotonicRegression` end-to-end
- `tests/test_post_clamp_rr_guard.py` (4 tests) — W1-T7 fallback in 4 scenarios
- `tests/test_tp_percentile_w1t5.py` (4 tests) — W1-T5 constant pin + operative-path proof + diagnostic field retention
- `tests/audit_js_errors_kimi411.spec.ts` — Playwright pageerror/console.error/network-failure capture against production `/audit`
- All 16 Python tests pass locally; Playwright spec passes (with 3 known console errors documented in baseline)

### Independent reviews
- **Copilot** reviewed #416 — verdict "ship it", recommended four follow-ups (all later landed in #418 and #421).
- **General-purpose review agent** caught a real BLOCKER on #427: the first push silently shipped the wrong commit (auto-sync had switched HEAD between checkout and push). After cherry-picking onto `origin/main` and force-pushing, the second review verdict was **ship**.

### Scheduled remote agent
- **`trig_01TGKX6sWsFf6mCBuwSgk82M`** — fires once at **2026-04-27T03:33:00Z** (≈23h after merge of #421). Re-runs `tests/audit_js_errors_kimi411.spec.ts` against live production. Three deterministic outcomes:
  1. **0 console errors** → opens `chore(audit): refresh JS-error baseline (3 → 0)` PR with refreshed artifact.
  2. **1–2 errors remain** → opens PR with diff + per-error hypothesis.
  3. **≥3 errors** → opens GitHub issue (deploy may have lagged), does NOT refresh baseline; verifies #418 / #421 are actually on `origin/main` and that the regenerated `index.html` reflects `fetchViaCorsProxy`.

---

## 2. Production deploy state at the moment of writing

Direct `curl https://findtorontoevents.ca/audit | grep …` confirms the static-site bundle has **not yet** picked up #418/#421/#427:

| Token | Should be | Actual on live |
|---|---|---|
| `fetchViaCorsProxy` | present (#421) | absent |
| `corsproxy.io` | present (#421) | absent |
| `api.kucoin.com` | present (#421) | absent |
| `TP_PERCENTILE` | present (#427) — server-side, not in HTML | n/a |

This is normal deploy lag for the static-site regenerator; the scheduled agent above is the verification mechanism.

---

## 3. Remaining work (prioritised)

All references trace back to `kimi_swarm/PICKS_SCORING_IMPROVEMENT_PLAN.md` and `kimi_swarm/SECOND_PASS_FINDINGS.md`.

### Tier 1 — Week 1 leftovers (highest leverage, smallest blast radius)

| ID | File:Line | Change | Why |
|---|---|---|---|
| **W1-T1** | `quality_gates.py:4449-4459` (also 2698-2711) | Drop the duplicate `strategy_wr` component in `calculate_smart_score` | Strategy WR is currently double-counted — adds ~15-27pts and the penalty is also doubled. PROVEN-but-`weak_edge` losers retain elevated scores. |
| **W1-T2** | `quality_gates.py:1479-1490` (and replicated at 1280, 2688, 4305) | `_effective_forward_wr_ratio`: replace `max(strat_fwd_wr, strategy_fwd_wr, forward_wr)` with **weighted-MIN** with `forward_trades >= 10` precondition | r = **−0.49** correlation between current `forward_wr` and realized PnL (per `quality_gates.py:2635` comment). The MAX is the single biggest overfit accelerant in the scorer. |
| **W1-T3** | `scanner.py:3264` | Final `signals.sort(key=ml_score)` → composite `0.4*ml_score + 0.4*smart_score/100 + 0.2*forward_wr` | Today the final ranking ignores both `smart_score` and `forward_wr` — they're computed and discarded at the cut. |
| **W1-T4** | `db.open_pick` / `db.close_pick` + offline backfill script | Mirror `extra.ml_score → root.ml_score` on close; backfill last 90d | `ml_score` is currently 0% populated on closed picks → ML gate `SMART_PICKS_MIN_ML_SCORE` is forced to 0.0 → strongest discriminator unused; Cohen's d = 0.011 today. **Note:** `signal["ml_score"]` is already at root in `scanner.py:3661` for *new* picks; the bug is in the close path. |
| **W1-T6** | `adaptive_tp_sl.py` (+ apply step) | Add `BREAKEVEN_TRIGGER_R = 1.0`; move SL to entry once MFE ≥ 1R | No breakeven moves today. Converts late-time-exit losers into 0R scratches. |

### Tier 2 — Week 2 (calibration & policy)

- **W2-T1** `scanner.py:3113-3237` — convert chained multiplicative ml_score penalties (×0.70 × 0.80 × 0.40 × 0.50) to **additive log-odds** on a calibrated scale.
- **W2-T2** `quality_gates.py:505` — re-enable `SMART_PICKS_MIN_ML_SCORE >= 0.50` *after* W1-T4 fill-rate ≥ 90%.
- **W2-T3** `quality_gates.py:2638-2640` — tiered overfit penalty (`wr>0.65 & n<15: -8`, `wr>0.75 & n<20: -15`, `wr>0.85 & n<30: -25`).
- **W2-T4** Dashboard truthfulness in `audit_dashboard/template.html` + `dashboard_generator.py` — require `forward_trades >= 10` to display WR; raise Track% min N from 3 → 30; render NO_DATA as `—` not `0%`.
- **W2-T5** Per `kimi_swarm/reports/asset_class_edge_validation_report.md`: **disable FOREX, COMMODITY, BOND, FUTURES** (statistically negative-EV per Bonferroni). Document via `MUTATION_THREE_AXIS_PROTOCOL.md`.
- **W2-T6** Reconcile SHORT crypto policy 3-way conflict between `quality_gates.py:534`, `scanner.py:3231`, `quality_gates.py:2666`.
- **W2-T7** Persist immutable `regime_at_entry` tuple `(label, source, ts)` in `db.open_pick`; reject when unresolved (current chained `.get()` falls through to "UNKNOWN").

### Tier 3 — Week 3 (ML rebuild)

- **ML-3** `ml_ranker.py:2415, 2448` — `btc_24h_change` is currently a feature *and* a sub-component of `direction_market_alignment`. Phase-20 comment admits it's "kept for feature count alignment". Drop the standalone column on the next retrain.
- **ML-4** `ml_ranker.py:1740-1745` — champion failure auto-promotes challenger with no challenger validation. Add try/except around challenger scoring; only promote on success.
- **ML-7** `ml_ranker.py:1647-1672` — drift threshold `accuracy < 45%` allows 55% error rate (near-random). Use `AUROC < 0.52` or `accuracy < 50.5%`.
- **ML-9 / W3-T1** Feature-name SHA-256 contract in model artifact; assert match at predict time; hard-fail rather than fall back to heuristic.
- **W3-T2** Replace Boruta with LASSO / Elastic Net (deterministic given seed).
- **W3-T3** Ternary classification (WIN / LOSS / TIME_EXIT) + Beta or Platt calibration; track Brier and log-loss.
- **W3-T4** Microstructure / cross-asset / macro features: OBI slope, VPIN, BTC β, SPX β, DXY β, MOVE index, Fed RRP, M2 z-score (audit gap #24 — flip `OBI_SHADOW_MODE` first).
- **W3-T5** Bonferroni / Deflated Sharpe gate on new strategy promotion (`p < 0.00025` for ~200 strategies tested).

### Tier 4 — Week 4 (TP/SL ladder + sizing)

- **W4-T1** Wire the existing-but-dead `TRAILING_STOP` config dict (`config.py:190-194`).
- **W4-T2** Partial-TP ladder (TP1 = p50(MFE) for 50% size, TP2 = p60(MFE) for runner with breakeven SL).
- **W4-T3** Per-asset R:R sweep — every category currently sits at R:R 1.5–2.33; the engine's own audit calls 1.0–1.5 the WR-optimal band. Diff table at `kimi_swarm/PICKS_SCORING_IMPROVEMENT_PLAN.md` §5.2.
- **W4-T4** ATR vol-time scaling: `* hold_days` → `* sqrt(hold_days)` (proper √t).
- **W4-T5** Bayesian symbol/strategy blend `1/sqrt(n)` instead of fixed 60/40.
- **W4-T6** Per-asset score floors data-driven from rolling 30d top-30% quantile.
- **W4-T7** `MIN_COPY_TRADER_PF` 10.01 → 2.0 (current threshold is effectively-off).
- **W4-T8** `STRATEGY_WEIGHT_OVERRIDES` + `BLACKLISTED_STRATEGIES` data-driven from rolling 90d Sharpe.

### Data-flow / observability

- **DF-1** `forward_validator.py` — atomic JSON writes for `closed_picks.json` and `active_picks.json` via existing `alpha_engine/atomic_json.py:atomic_write_json`. (My earlier edit got reverted by the auto-sync; needs to be done with care or via a different operator/branch.)
- **DF-2** Same file — try/except around `json.load()` readers; return `[]` + `logger.warning` on malformed JSON.
- **DF-3** `tools/data/snapshots/` is **dead since 2026-04-07** (per Subagent X). Either resurrect the writer or delete the directory.
- **DF-4** Reconcile DB ↔ JSON: `alpha.db` last modified 2026-04-15; JSONs updating continuously; no startup reconciliation.
- **OBS-1** Persist `forward_wr_gross` and `forward_wr_net` separately on every new pick (`scanner.py:3100-3105`).
- **OBS-2** Decay alert: rolling 30d PF < 70% of 90d PF on any whitelisted strategy (would have caught Equity's documented −27% PF drop).
- **OBS-7** Hide `forward_wr` columns / replace with `—` until W1-T2 lands AND replay confirms r > 0 on a 90d holdout. **Today the metric is anti-predictive** (r = −0.49) — actively misleading.

### Risk-quality (cross-cutting)

- **R-1** `risk_controls.py:363` — `is_daily_blocked()` was patched to `return False`. (My S2 logger swap got reverted by auto-sync; the underlying bypass is the real concern. Polluting the closed-pick distribution that feeds ML retraining is the silent damage.)
- **R-2** Drawdown computed as `sum(pnl_pct)` without exposure weighting (`risk_controls.py:102-116, 268-283`) — every DD-based gate is currently broken. Fix: `sum(pnl_pct * size) / total_capital`.

---

## 4. Open questions / known unknowns

1. **Auto-sync interference.** Several edits in this cycle were silently reverted before the next turn (full local rollback of `risk_controls.py`, `forward_validator.py`, and various `kimi_swarm/` docs). Working around it via direct PR-and-push works, but anything that needs an iterative local edit + smoke loop needs a different mechanism (worktree, dedicated CI job, or human pair-driving).
2. **Static-site deploy cadence.** Manual verification is hard because the deploy doesn't happen on every `main` push. The scheduled remote agent at 2026-04-27T03:33Z is the canary. If it reports outcome (b) or (c), revisit.
3. **Bonferroni gate side-effect.** Disabling FOREX/COMMODITY/BOND/FUTURES + applying `p < 0.00025` to new strategies will likely create a **pick-volume drought** during the Week-3 ML rebuild. Need an `OBS-6` tripwire (`forward 30d pick volume drops > 40%` → relax to `p < 0.001` and alert operator).
4. **TIME_EXIT measurement.** W1-T5 (TP 75 → 60) is expected to drop TIME_EXIT share by 3–5pp; W1-T6 (breakeven trigger) by another ~3pp. Cumulative effect needs to be measured against a frozen pre-fix baseline of `closed_picks.json` (current outcome resolver would mix pre and post).
5. **`ml_ranker` provenance.** ML Ranker is itself blocklisted at PF 0.58 (`quality_gates.py:989`), yet its `ml_score` still drives ranking. Should this be hard-zeroed pending ML-3/ML-4/W3-T1 rebuild? Currently #411 just calibrates the (still-trusted) probabilities.

---

## 5. Reference index

| Doc | Purpose |
|---|---|
| `kimi_swarm/PICKS_SCORING_IMPROVEMENT_PLAN.md` | Narrative master plan, 7 sections |
| `kimi_swarm/PICKS_TODOS.md` | 30+ tactical PR-sized todos with file:line + acceptance tests |
| `kimi_swarm/ACTION_ITEMS.md` | Original 35-item priority list (incl. risk/ops) |
| `kimi_swarm/FURTHER_ENHANCEMENTS.md` | Tier S/A/B/C code-health backlog |
| `kimi_swarm/SECOND_PASS_FINDINGS.md` | Live-code findings beyond Kimi snapshot (ML-1..ML-12, DF-1..7, 30 missed-nuance gaps) |
| `kimi_swarm/BENEFITS_SUMMARY.md` | Projected impact: WR +3-6pp, PF +0.10-0.20, TIME_EXIT 25.8% → 15-18% |
| `kimi_swarm/notes/C_codebase_findings.md` | Full quality_gates / scanner / risk audit (25 issues) |
| `kimi_swarm/notes/E_tpsl_config_findings.md` | Full TP/SL + config audit (23-row diff table) |
| `kimi_swarm/notes/SECOND_PASS_GAPS.md` | 30 nuances first synthesis dropped |
| `kimi_swarm/reports/` | Original Kimi swarm deliverables (FINAL, ML, asset-class, HC filter, plan) |
| `tests/audit_js_errors_kimi411.spec.ts` | Live-production JS-error scan |
| `tests/test_isotonic_calibrator_branch.py` | ML-2 regression tests |
| `tests/test_post_clamp_rr_guard.py` | W1-T7 regression tests |
| `tests/test_tp_percentile_w1t5.py` | W1-T5 regression tests |

---

## 6. How to pick this up next session

1. Start with **W1-T2** (`_effective_forward_wr_ratio` MAX → weighted-MIN). Single biggest leak. Same low-blast-radius pattern as W1-T7 / W1-T5. Touches `quality_gates.py` only. Companion test: synthetic strategy with disagreeing WR fields, assert the conservative blend.
2. Then **W1-T1** (drop strategy-WR double-count). Lock with a fixture pinning the score for a known input before/after.
3. Then **W1-T3** (composite final ranking) — single-line in `scanner.py:3264`, but needs a fixture asserting top-N ordering on a 100-pick sample.
4. Pick up the deferred items from this cycle (DF-1/2/3 atomic writes + reader guards + dead-snapshot cleanup) when an operator can prevent the auto-sync from clobbering local edits.
5. Do **not** start Week 2 until the verification agent at `trig_01TGKX6sWsFf6mCBuwSgk82M` has confirmed the live `/audit` is at 0 console errors.
