# Swarm worker outputs — per-run, large, regenerable. Keep curated docs.
swarm_runs/*
!swarm_runs/SESSION_SUMMARY.md
!swarm_runs/briefing_asset_class_audit.md
!swarm_runs/CONSENSUS.md
!swarm_runs/CONSENSUS_v2.md
!swarm_runs/DISAGREEMENT_RESOLUTION.md
!swarm_runs/KIMI_VS_OURS_MERGE_PLAN.md
!swarm_runs/FOREX_DEEP_DIVE.md
!swarm_runs/RUNS_LOG.md
!swarm_runs/_janitor.log
!swarm_runs/SWARM_ENHANCEMENT_ROADMAP.md
!swarm_runs/PR_REVIEW_ABORTED.md
!swarm_runs/SWARM_SELF_REVIEW.md
!swarm_runs/P0_BLACKLIST_FIX_REPORT.md
!swarm_runs/PR_TRIAGE_2026_05_03_MANUAL.md
!swarm_runs/SELF_REVIEW_PENDING_AUDIT_2026_05_03.md
!swarm_runs/NEW_ENGINES_HALLUCINATION_AUDIT_*.md
!swarm_runs/PR_ACTION_COMMANDS_2026_05_03.md
!reports/DASHBOARD_3_ISSUES_2026_05_03.md
!reports/CI_TEST_311_312_DIAGNOSIS_2026_05_03.md
!swarm_runs/PR_VALIDATION_RESULTS_2026_05_03.md
!swarm_runs/COPILOT_NON_CRYPTO_VALIDATION_2026_05_03.md
!swarm_runs/PR_MERGE_ORDER_2026_05_03.md
!swarm_runs/PR_QUEUE_EXECUTION_2026_05_03.md
!swarm_runs/KIMI_AUDIT_RESPONSE.md
!swarm_runs/kimi_audit_2026_05_03/
!swarm_runs/kimi_audit_2026_05_03/**
!swarm_runs/kimi_prediction_edge_audit_2026_05_03/
!swarm_runs/kimi_prediction_edge_audit_2026_05_03/**
!swarm_runs/KIMI_PREDICTION_EDGE_AUDIT_RESPONSE.md
!swarm_runs/PR_REBASE_REMERGE_2026_05_03.md
!swarm_runs/ACTION_PLAN_SWARM_CHECK_2026_05_03.md
!swarm_runs/PR_CI_REMEDIATION_BATCH_2026_05_03.md
!swarm_runs/PR_COMMENT_AUDIT_2026_05_03.md
!swarm_runs/PR_751_5MODEL_3ROUND_REVIEW.md
!swarm_runs/QUANT_FIX_BLUEPRINTS_2026_05_03.md
!swarm_runs/OPEN_PR_SWARM_REVIEW_2026_05_03_v2.md

# Backtest cache (parquet files from yfinance)
alpha_engine/data/bt_cache_*.parquet

# FRED data cache (rebuildable from API; see alpha_engine/fred_data_fetcher.py)
alpha_engine/data/fred_cache/

# Runtime cron output: Pinnacle moneyline snapshots (regenerated every 5 min
# by tools/pinnacle_anchor_scrape.py; consumed by sports_value_load_pinnacle_snapshot)
data/pinnacle_snapshots/

# Ignore generated data files
ALPHA_ENGINE/data/hmm_regime.json
node_modules

# Python cache
__pycache__/
*.pyc

# SQLite databases (binary files cause merge conflicts in git)
*.db
*.db-journal
*.db-wal
*.db-shm
*.db.bak
# Exception: DBs committed by GH Actions for state persistence
!sandbox/data/opposite_day.db
!paper_trading/data/paper.db
!coinglass_strategies/data/coinglass.db
!quan_engine/data/quan_engine.db

# Environment files
.env
*.env
# Local spend ledger (sensitive usage/cost telemetry — FreeLLM)
.local/spend/
# Never commit tokens / PATs (use OS keychain or GH_TOKEN locally only)
*.pat
github_token.txt
**/secrets/
nul
ftp_script.txt
KIMI_RISEOFTHECLAW/data/kimi_trading.db
KIMI_FEB172026/data/kimi_trading.db

# Strategy Registry inbox (envelopes are transient)
incoming_strategies/*.json
failed_strategies/*.json
audit_trail/backfill_import.sql
alpha_engine/data/mega_training_data.json
alpha_engine/data/continuous_improvement_report.json
alpha_engine/data/continuous_improvement_report.md
alpha_engine/data/continuous_improvement_history.json

# ML crypto predictor cached klines (stale parquet in git breaks CI)
ml_crypto_predictor/enhanced_models/data/klines/

# Playwright test results (local CI artifacts)
test-results/

# Local mutation_analysis.py outputs (CI uploads via workflow artifact)
mutation_artifacts/

# Temporary helper scripts (underscore-prefixed at project root)
/_*.py
/_*.txt
!__init__.py
run_migration_temp.py
audit_trail/data/.generator.lock
audit_trail/data/hf_gate_telemetry.json
scanner_lifecycle.log
.pytest_cache/

# Generated strategy bundles (large, auto-generated)
alpha_engine/generated_v2_bundle.py
alpha_engine/new_strategies/multi_asset_report.json

# Temp files
docs/*.tmp

# Scratch scripts (one-off analysis/diagnostic scripts)
**/tmp_*
**/apply_godlike_*

# ── Repository Hygiene Review 2026-05-02 ──
# Agent worktrees (ephemeral branch checkouts)
.claude/worktrees/

# Editor/tool directories (belt-and-suspenders)
.kilo/
.kilocode/

# Temporary research clones
.tmp_research/

# Nested external projects — accidental inclusions, not submodules
AutoHedge/
HedgeFundData/

# Large DB files (>100MB GitHub limit)
genome/strategy_registry.db
genome/phoenix_registry.db

# SQL dumps (contain internal IPs and DB schemas) — added 2026-04-11
data/*.sql
data/10_123_0_33*.txt
*.sql.gz

# Trust audit exports (daily JSONL logs, append-only)
alpha_engine/data/trust_audit_exports/
tradingview-mcp/

# Equity quote/marketcap on-disk cache (1h TTL, regenerated per workflow run).
# See alpha_engine/equity_price_failover.py — these files are write-only by
# the runner; we keep the dir checked in via data/equity_quote_cache/.gitkeep
# but ignore the per-ticker JSON snapshots themselves.
data/equity_quote_cache/*.json
tmp/super_swarm/.envkeys
# Ephemeral notification logs
logs/notifications/
# Market data — generated by scripts, not versioned
data/dividends/

# tools/ scratch JS scripts (leading underscore = scratch by convention)
tools/_*.js
# Cross-PC protocol gateway durable log (large, regenerable)
logs/cross_pc_protocol/events.jsonl
# Hermes "I give up" failure marker (transient, not work product)
COMMIT_FALLBACK_*.md

# graphify knowledge-graph outputs — regenerable via `graphify update <path>`
# (AST-only, ~30s) or `graphify extract <path>` (semantic LLM, minutes).
# .claude/skills/graphify-intel/SKILL.md documents how to rebuild.
graphify-out/
**/graphify-out/
!swarm_runs/run_20260510T003836Z/GHA_FIX_PLAN_2026-05-09.md
!swarm_runs/run_20260510T005925Z/P5_FOREX_POLICY_CONSENSUS.md
!swarm_runs/10round_20260510T043424Z/
!swarm_runs/10round_20260510T043424Z/**
!swarm_runs/metaq_v02_20260510T045213Z/
!swarm_runs/metaq_v02_20260510T045213Z/**
# Research orchestrator P1/P2/P5 swarm output (small JSON, valuable as
# evidence; orchestrator reads from these dirs)
!swarm_runs/briefing_research_*.md
!swarm_runs/research_*_p1_*/
!swarm_runs/research_*_p1_*/**
!swarm_runs/research_*_p2_*/
!swarm_runs/research_*_p2_*/**
!swarm_runs/research_*_p5_*/
!swarm_runs/research_*_p5_*/**

# Live market regime cache (regenerated hourly by tools/live_market_fetcher.py)
audit_dashboard/data/live_market_regime.json

# Meta-labeler SHADOW gate log (A1 scaffold — runtime artifact, regenerated by meta_label_gate)
audit_dashboard/data/meta_label_shadow_log.json

# /goal skill runtime state (per-session, not shared)
.claude/goal_state.json

# Per-class ML trainer model bundles (regeneratable — regenerate with: python ml_gatekeeper/per_class_trainer.py)
ml_gatekeeper/models/per_class/*.joblib
tools/cache/
.grok_session/

# Orphan submodule placeholders (no .gitmodules URLs — caused GHA checkout post-step exit 128)
openclaude/
openclaude-vscode/

# Scratch/temp files
deleteme.txt
tmp.txt

# 2026-05-22 repo-bloat fix: these are regenerated every hour by
# audit-dashboard.yml and FTP-deployed to findtorontoevents.ca/audit/data/.
# Committing them added ~100MB/hr to .git history (repo hit 19GB → checkout
# hung). They must NOT be tracked. See reports/AUDIT_STAT_VALIDATION_2026-05-22.md.
audit_dashboard/data/dashboard_data.json
audit_trail/data/dashboard_payload.json

# DB password remediation — never commit (see docs/db_remediation.md)
.env.dbpw

# 2026-05-25 bloat fix v2 — files missed by the 2026-05-23 stripped reset
# (commit 4aaa6ff84). Each was still adding ~1GB/day to .git history.
# All regenerable from training / scraping / CI. See verify_all_keys.py
# pattern: production-deployed files don't need to be in git.
ml_crypto_predictor/production_models/*.pkl
audit/data/dashboard_data.json
events.json
next/events.json
edge_analysis_results.json
data/live_picks.db
tmp_*.json
actionlint_dir/actionlint.exe
Kimi_Agent_*.zip
Kimi_Agent_*(*).zip

# 2026-05-25 bloat fix v3 — second-pass cleanup after audit. Each was found
# in the top 30 history blobs (>20 MB). Production audit confirmed:
#   closed_picks.json           — heavily used (8 readers, 5 writers) — keep current, regenerable
#   closed_picks_enriched.json  — pure derivative from closed_picks via backfill_pick_features.py
#   audit_edge_review_live.json — single consumer, no in-repo writer
#   tools/data/snapshots/*.json — archived dashboard snapshots, only audit_smart_gate_funnel.py reads
alpha_engine/data/closed_picks.json
alpha_engine/data/closed_picks_enriched.json
tools/data/audit_edge_review_live.json
tools/data/snapshots/dashboard_data_*.json

# 2026-05-25 bloat fix v4 — found by post-scrub blob audit
# enhanced_models/models/*.joblib: 1,746 files, 3.6 GB on disk, retrained periodically
# closed_picks.archive.jsonl: 17 MB rotation archive; consumers can read from current closed_picks.json or regenerate
ml_crypto_predictor/enhanced_models/models/*.joblib
alpha_engine/data/closed_picks.archive.jsonl

# 2026-05-25 bloat fix v5 — Round 3 after audit of remaining tracked files >1MB.
# Each pattern below has confirmed production consumers (still loaded at runtime
# from disk) — gitignoring just stops re-commits, files stay in place.

# Tmp / scratch (gitignore doesn't retroactively untrack — must git-rm-cached too)
tmp/
tmp_*.json

# Backups (suffix-based)
*.bak
**/index_backup_v*.html
events_backup.json

# Logs (selective — only repo-root + .log under common log dirs)
*.log
**/logs/*.log

# SQLite + DB files (regenerated by scripts that need them)
**/*.db
!tests/**/*.db
!.claude/**/*.db

# Additional ML models the v3/v4 patterns missed
parallel_agent/data/guess_models.pkl
ml_gatekeeper/models/gatekeeper_*.joblib
KIMI_RISEOFTHECLAW/data/rf_model.pkl
ml_crypto_predictor/enhanced_models/feedback_data/aggregated_closed_picks.json

# Dated/timestamped one-shot result files
quant_lab/combo_results/combo_trades_*.json
incubator/agents/*/mega_permutation_results.json

# Audit history / wrong-guess logs (regenerable)
KIMI_RISEOFTHECLAW/data/audit_log.json
alpha_engine/data/wrong_guesses_log.json
alpha_engine/data/expired_picks_archive.json
parallel_agent/data/guess_history.json

# Duplicate dashboard JSONs (incubator + battleground both read from disk)
incubator/config/baby_strats_dashboard.json
battleground/data/baby_strats_dashboard.json

# Dated reports (kimi_edge_audit_*) — committed once for reference, not iterated
reports/kimi_edge_audit_*/raw_picks_clean.csv

# Regenerated HTML dump artifacts (not the live template/index)
audit_full.html

# =========================================================================
# 2026-05-25 bloat fix v6 — Round 4 after multi-agent + multi-AI audit
# =========================================================================
#
# Biggest single win: alpha_engine/data/hindsight/ has 2,305 tracked timestamped
# JSONs (71 MB) auto-committed by evaluation loops.
#
# Live-state JSONs across many modules are touched 155+ times in 3 days by
# auto-scan workflows. Production code reads them from disk; stopping commits
# does not affect runtime.
#
# Universal Python/ML/JS hygiene patterns are added defensively in case any
# slip into commits via `git add -A` etc.

# --- Biggest single source: hindsight evaluations ---
alpha_engine/data/hindsight/**/*.json

# --- Live-state JSONs (high commit churn, runtime read from disk) ---
**/data/active_picks.json
**/data/live_*.json
**/data/*_signals.json
!cross_aggregation/data/super_signals.json  # needed by Deploy Competition workflow
# Re-track other deploy-source live JSONs (CI fresh-checkout needs them)
!KIMI_RISEOFTHECLAW/data/live_competition.json
!KIMI_RISEOFTHECLAW/data/live_signals_now.json
!KIMI_RISEOFTHECLAW/data/active_picks.json
!regime_terminal/data/active_signals.json
!crypto_ml_edge/data/active_picks.json
!crypto_signal_engine/data/active_picks.json
!quan_engine/data/active_signals.json
**/data/*_signals_now.json
**/data/scan_*.json
**/data/scan_log.json
**/data/elimination_state.json
**/data/tournament.json
**/data/live_competition.json
**/data/ml_weights.json
**/data/winner_history.json
**/data/regime_report.json
**/data/outcome_resolver_log.json

# --- Universal ML / data file extensions (regenerable training outputs) ---
*.npy
*.npz
*.h5
*.hdf5
*.pt
*.pth
*.onnx
*.ckpt
*.parquet
*.feather
*.tflite

# --- Specific cache + binary dirs called out by agent audit ---
cli_memory_store/**/*.bin
genome/data/ohlcv_cache/
data/parquet/
data_lake/**/*.parquet

# --- Additional model dirs (each has production consumer reading from disk) ---
claude_gainer_ml/models/*.joblib
crypto_ml_edge/models/*.joblib
ml_battleground/**/models/*.pt
ml_battleground/**/models/*.joblib
mercury2/models/*.joblib

# --- Universal Python/JS hygiene (safety net against future leaks) ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.dist-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
node_modules/
*.db-shm
*.db-wal

# --- IDE / editor temp ---
.idea/
*.swp
*.swo
*.tmp

# =========================================================================
# 2026-05-25 bloat fix v7 — Large tracked files still in working tree
# =========================================================================
# These files are regenerable outputs, test artifacts, or deployment
# artifacts that don't need version control. Stopping commits prevents
# ~50+ MB/month of unnecessary history growth.

# Backtest results and analysis outputs
alpha_engine/data/unique_edge_backtest_results.json
alpha_engine/data/dna_mutations.json
backtest_results/**/*.sql
incubator/backtest_results/**/*.json
quant_lab/combo_results/**/*.json

# Test artifacts (screenshots, proof images)
tests/screenshots/**/*.png
tests/artifacts/**/*.png

# Backup files (HTML, JSON, etc.)
updates/index.html.bak*
audit_dashboard/data/claudes_test_*.json
audit_dashboard/data/claudes_test_*.json.bak

# Live logs and state files (regenerated hourly/daily)
ml_crypto_predictor/enhanced_models/live_picks/
alpha_engine/data/missed_gainers_log.json
claude_gainer_ml/tracker/
copy_trader_intel/data/highscore_pick_history.json
copy_trader_intel/data/okx_trader_database.json

# Training data and feature matrices (regenerable)
claude_gainer_ml/data/
ml_crypto_predictor/models/swing/

# Quarantine and archive artifacts
audit_trail/data/quarantine/
audit_trail/data/universal_resolved_picks.json

# Deployment and build artifacts
deploy_riseoftheclaw/
TORONTOEVENTS_ANTIGRAVITY/audit/*.docx

# Competition and tournament data
# (removed bare-dir ignore — replaced with file-glob below)
data/ai_tournament/picks_2026*.json
!data/ai_tournament/picks_latest.json
audit_dashboard/data/ai_tournament_picks_latest.json
battleground/data/luxalgo_closed_picks.json

# Database snapshots and exports
database/*/
reports/h006_funding_cache_*.json

# Attachments and archives
kimi_attachments_*/
ejaguiar1_sportsbet.zip

# Misc regenerable artifacts
tools/data/audit_edge_review_live.json

# =========================================================================
# 2026-05-25 bloat fix v7 — manual audit by Roo on ELTONSVLLM_SERVER
# (paid-mode-fast routing via the LiteLLM proxy). 35+ files >1MB still tracked.
# Findings cross-checked against my v3/v4/v5/v6 — these are genuinely new
# additions. Original work credit: copilot/identify-git-bloat-sources branch.
# Doc: updates/2026-05-25-gitignore-bloat-audit.md
# =========================================================================

# Backtest / analysis outputs
alpha_engine/data/unique_edge_backtest_results.json
alpha_engine/data/dna_mutations.json
backtest_results/**/*.sql
incubator/backtest_results/**/*.json

# Test artifacts (screenshots, regeneratable by re-running tests)
tests/screenshots/**/*.png
tests/artifacts/**/*.png

# Backup files with timestamp suffixes (my v3 *.bak only catches `.bak`, not `.bak-<date>`)
updates/index.html.bak*
audit_dashboard/data/claudes_test_*.json
audit_dashboard/data/claudes_test_*.json.bak

# Live state / training output / log files
ml_crypto_predictor/enhanced_models/live_picks/
alpha_engine/data/missed_gainers_log.json
claude_gainer_ml/tracker/
copy_trader_intel/data/highscore_pick_history.json
copy_trader_intel/data/okx_trader_database.json
claude_gainer_ml/data/feature_matrix.csv
claude_gainer_ml/data/raw_market_data.json

# Swing ML models (missed by enhanced_models/models pattern — different dir)
ml_crypto_predictor/models/swing/*.pkl

# Audit quarantine / archive
audit_trail/data/quarantine/
audit_trail/data/universal_resolved_picks.json
battleground/data/luxalgo_closed_picks.json
reports/h006_funding_cache_*.json

# Attachment archives + DB snapshots + competition data
kimi_attachments_*/
ejaguiar1_sportsbet.zip
database/*/
STOCKS/competition/competition-stocks.json
!STOCKS/competition/competition-stocks.json  # needed by Deploy Competition workflow

# Deploy artifacts (regenerated each deploy run)
deploy_riseoftheclaw/audit_dashboard/index.html

# Dated AI tournament picks (current snapshot lives in *_latest.json which is tracked)
data/ai_tournament/picks_2026*.json

# =========================================================================
# 2026-05-25 bloat lockdown v8 — comprehensive fix per subagent audit
# Every broad pattern below has explicit !negations for deploy-required files.
# =========================================================================

# --- ML model artifacts (each dir specifically; broad **/*.pt|.pth|.h5|.npz) ---
**/*.pt
**/*.pth
**/*.h5
**/*.npz

# --- Document/archive bloat ---
**/*.docx
**/*.pdf
!docs/**/*.pdf
**/*.tar
**/*.tar.gz
**/*.tgz
**/*.7z
**/*.sql.gz
**/*.dump
**/*.sqlite
**/*.sqlite3
!alpha_engine/db/schema.sql

# --- Test artifacts (regenerated by Playwright) ---
tests/screenshots/**
tests/artifacts/**
!docs/screenshots/**

# --- Backup / dated / log files (catch-all) ---
**/*_backup.json
**/*_backup.*
**/*.orig
**/*.old
**/*~
*.bak.[0-9]*

# --- Bulky tracked files without deploy refs ---
unique_edge_backtest_results.json
alpha_engine/data/unique_edge_backtest_results.json
alpha_engine/data/augmented_training.json
incubator/backtest_results/real_data_directional_*.json
tools/cache/
reports/session_log_*.md
!reports/session_log_summary_*.md
GITHUB_STRATS.MD

# --- audit_log.json variants (multi-dir, 11MB+ each, regenerable) ---
alpha_engine/data/model_audit/audit_log.json
alpha_engine/data/pick_audit_log.json
riseoftheclaw/data/audit_log.json
deploy_riseoftheclaw/riseoftheclaw/data/audit_log.json

# --- STOCKS/competition: broad with explicit negations for ALL deploy files ---
STOCKS/competition/*.json
!STOCKS/competition/competition-crypto.json
!STOCKS/competition/competition-forex.json
!STOCKS/competition/competition-meme_coins.json
!STOCKS/competition/competition-penny_stocks.json
!STOCKS/competition/competition-slim.json
!STOCKS/competition/competition-stocks.json
!STOCKS/competition/forward_picks.json

# --- KIMI RISEOFTHECLAW: deploy-required state files (negation) ---
!KIMI_RISEOFTHECLAW/data/live_competition.json
!KIMI_RISEOFTHECLAW/data/signal_tracking.json
!KIMI_RISEOFTHECLAW/data/closed_picks.json
!riseoftheclaw/data/signal_tracking.json
!riseoftheclaw/data/live_competition.json
!audit_dashboard/data/ai_tournament_picks_latest.json

# --- cross_aggregation: caches/logs ignored, super_signals already negated ---
cross_aggregation/data/*_cache.json
cross_aggregation/data/*_log.json
cross_aggregation/data/*_archive.json
!cross_aggregation/data/consensus_outcomes.json
!cross_aggregation/data/contested_picks_tracker.json
!cross_aggregation/data/conflict_lessons_learned.json

# --- Session state / IDE leakage (was making it into commits) ---
.claude/settings.local.json
.claude/scheduled_tasks.lock
.kilo/
.kilocode/
.commandcode/
.cursor/gha_health_state.json
.github/gh_monitor_state.json
.qwen/settings.json.bak.*
session-ses_*.md
transcripts/
repro_*.py
___HELL_*.MD

# =========================================================================
# 2026-05-25 bloat fix v9 — ml_crypto_predictor/models/*.pkl (70 files, 53 MB)
# Roo on free-mode-fast flagged. Subagent had warned NOT to ignore because
# train_crypto_models.yml is the canonical writer. Workflow updated in same
# commit to use `git add -f` so the gitignore + CI both work.
# =========================================================================
ml_crypto_predictor/models/*.pkl
ml_crypto_predictor/models/**/*.pkl

# =========================================================================
# 2026-05-25 bloat fix v10 — preemptive ML experiment-tracking patterns
# Verified by peer review (nvidia/moonshot/kimi-k2.6 audit): no current files
# in repo, but flagged as common bloat sources if anyone adds wandb/mlflow/
# DVC tracking. Adding now to prevent future leaks (low risk, no false-pos).
# =========================================================================
wandb/
mlruns/
**/wandb/
**/mlruns/
.dvc/cache/
.dvc/tmp/
**/checkpoints/
!docs/**/checkpoints/
**/runs/events.out.tfevents.*
**/tb_events/

# =========================================================================
# 2026-05-25 bloat fix v11 — Cursor on this PC audit (reports/2026-05-25-large-files-gitignore-audit.md)
# Adds .venv/ at root level (was only protected by nested .venv/.gitignore)
# + ~30 more stale-tracked live-state JSONs that v6-v8 patterns named the
# class but didn't catch the specific paths.
# =========================================================================

# --- .venv at top level (defense in depth; .venv/.gitignore is the only other guard) ---
/.venv/
**/.venv/

# --- alpha_engine live-state JSONs (v6 broad patterns missed these specific names) ---
alpha_engine/data/closed_picks_fast.json
alpha_engine/data/forex_walk_forward_results.json
alpha_engine/data/precursor_history.json
alpha_engine/data/prediction_quality_history.json
alpha_engine/data/momentum_tracker_picks.json
alpha_engine/data/theory_portfolios_history.json
alpha_engine/data/strategy_performance.json
alpha_engine/data/extensive_backtest_results.json

# --- audit_dashboard pick_funnel snapshots (frequently re-committed) ---
audit_dashboard/data/pick_funnel_*.json

# --- backtest_results trade_logs (per-strategy JSONs accumulate) ---
backtest_results/futures_comparison/trade_logs/*.json

# --- ML models (skyrocket_detector dir was missed by v6/v7) ---
skyrocket_detector/data/*.joblib

# --- HTML backups + OLD-suffix files (v3 *.bak missed *.backup variant) ---
**/*.backup
**/*_OLD.html

# --- One-off archive docs (Mar-12 pre-archive snapshot) ---
docs/CHATWITHIT_ARCHIVE_PRE_*.md

# --- crypto_signal_engine price cache (regenerated each scan) ---
crypto_signal_engine/data/price_cache.json

# --- Windows case-conflict ghost files (case-insensitive FS creates duplicates of lowercase names) ---
.claude/commands/RESTARTVLLMP.md
.claude/commands/STARTVLLMP.md
.blackboxcli/

# A/B observability (per-runner local telemetry; print line in CI logs is the durable record)
audit_dashboard/data/ab_router_observability.jsonl

# mlflow local SQLite backend (no cloud) — added 2026-06-04
mlflow.db
mlruns_artifacts/
