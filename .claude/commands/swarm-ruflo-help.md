---
description: Show /swarm-ruflo command reference, agent list, and paid key status.
---

Run `/swarm-ruflo help`. Output the usage card from `.claude/commands/swarm-ruflo.md` (the help block).

Then run:
```
python3 .ruflo/orchestrator.py --list-agents --no-verify
```

This shows registered agents AND paid key status in one shot.
