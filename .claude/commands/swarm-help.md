---
description: Show /swarm command reference and engine status.
---

Run `/swarm help`. Output the usage card from `.claude/commands/swarm.md` (the help block).

Then run `python tools/swarm/swarm_stats.py` and append a line: "Recent stats above — ok% column tells you which engines are reliable."
