# /parallel-swarm — alias of /ParallelSwarm

Thin alias. Invoke the **`ParallelSwarm`** skill (`.claude/skills/ParallelSwarm/SKILL.md`)
and follow its steps: probe live workers → parallel implement (1 item → 1 file →
1 distinct worker) → validate → code-swarm review → test. See
`.claude/commands/ParallelSwarm.md` for the condensed steps. Aliases: pswarm,
distributed-swarm, code-swarm-distributed.
