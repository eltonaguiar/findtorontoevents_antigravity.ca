# DISABLED 2026-05-23 — Morning goal follow-up DMs are out of project scope.
# EVENT_NOTIFY_API_KEY remains in repo secrets for send-accountability-reminders and PHP APIs.
# To re-enable: restore workflow from git history (pre-2026-05-23).

name: Send Morning Goal Follow-Ups (disabled)

on:
  workflow_dispatch:
    inputs:
      acknowledge_disabled:
        description: 'This workflow is disabled. Leave default.'
        required: false
        default: 'disabled'
        type: string

jobs:
  disabled-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "::notice::Send Morning Goal Follow-Ups is disabled (2026-05-23). Not in project scope."
          echo "Use send-accountability-reminders or fc/api endpoints if needed."
