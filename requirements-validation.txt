# Optional anti-overfitting validation dependencies.
#
# These power alpha_engine/anti_overfit_validator.py and
# tools/run_anti_overfit_audit.py. They are intentionally split out
# of the main requirements.txt so production never depends on them.
#
# License notes:
#   arch         -- NCSA (BSD-style, free for commercial use)
#   timeseriescv -- MIT (López de Prado CPCV / PurgedKFold reimplementation)
#
# We deliberately AVOID mlfinlab (AGPL) which would contaminate the repo.

arch>=6.0
timeseriescv>=0.1.4
