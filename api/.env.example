# Events API (api/events/) — copy to api/events/.env
# MYSQL_HOST=localhost
# MYSQL_USER=ejaguiar1_events
# MYSQL_PASSWORD=...
# MYSQL_DATABASE=ejaguiar1_events

# Auth (api/google_callback.php) — optional; only if FavCreators db_config is not available
# Copy to api/.env if you deploy api/ without ../favcreators/public/api/
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# FC_MYSQL_HOST=localhost
# FC_MYSQL_USER=ejaguiar1_favcreators
# FC_MYSQL_PASSWORD=...
# FC_MYSQL_DATABASE=ejaguiar1_favcreators
