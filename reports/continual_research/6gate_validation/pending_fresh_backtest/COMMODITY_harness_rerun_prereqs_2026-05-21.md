# COMMODITY Harness Rerun Prerequisites (2026-05-21 Firing 2)

**Context:** COMMODITY 6-gate validation (firing 2) completed with B_failed marker. All 5-7 named (cot_positioning variants, commodity_carry_momo_double_sort, seasonal_momentum, oil_inventory_momentum, commodity_tsmom_12m, metals_mean_reversion, dxy_inverse_commodities) FAIL or INSUFFICIENT on clean data per COT leakage, small n, low sharpe, no WF/MC/FDR power. Flagship post-clean negative. Requires fixes before fresh statistical_validation_framework + commodity_strategy_harness + edge_stability_harness run (cf. FOREX pending and 6GATES MD:282-292).

**10 Prerequisites (P0-P2, modeled on FOREX + 90day_plan_COMMODITY Phase 1-2 + registry M-095/021/022):**
1. **COT re-aggregation + dedup enforcement:** Update dashboard_generator.py, audit_trail/quality_gates.py, MySQL trading_picks views, edge_stability_COMMODITY.json, cot_paper_pilot_status.json, cot_*.json to apply strict 1-per-(symbol, COT_release_week, direction) + 3d publication lag (_is_cot_row_public) retroactively on historicals. Verify 0 over-emission (ledger cot_emitted_releases.json full seed). Target: n>=20 independent weekly cycles for CT=F + other ags (KC/SB/CC/ZC/ZS/ZW).
2. **Lag + lookahead guards:** Confirm M-095 fixes in copy_trader_intel/multi_asset_copytrader_scraper.py and alpha_engine/cot_positioning.py (scrape_cftc + _last_cot_friday pre-3:30ET logic) applied everywhere (including kimi/commodity_signal_generator, multi_asset). Audit for any remaining bypass in signal_tracker / resolver / dashboard.
3. **Freshness + data quality gates:** Enforce data_quality_gates.yaml commodity rules (symbols_min, cot_release_day friday, roll_yield_check, pnl_window) + max_emission_per_cot_release:1, min_independent_cycles:20, liquidity_min_avg_daily_vol. Re-audit yfinance =F for all 18-25 COMMODITY_SYMBOLS (config.py:780) roll dates/volume/survivorship; add explicit checks for GC/HG/NG/CT etc.
4. **Concentration + symbol blocks:** Hard cap single symbol <=25-30% class PnL (CT=F explicit); kill or heavy penalty on low-liquidity (no-micro CT=F at live sizing stress $1500 limits, margin). Update quality_gates.py COMMODITY_NON_BLACKLIST + hedge_fund_quality_gate / commodity_kill_switch.
5. **Daily PnL series generation:** For realistic (non per-trade annualized) Sharpe per 6GATES MD rec 2026-05-21:290. Mark-to-market daily for resolved + active COMMODITY picks (esp. futures rolls, contango drag on proxies USO/UNG/DBA).
6. **Carry_momo + non-COT wiring validation:** Confirm commodity_carry_momo_double_sort fully wired in dashboard_generator::JSON_PICK_SOURCES + smart_picks path (per json 2026-05-20 WIRED); test quintile=3 long/short on 18 sym with realistic futures costs/rolls/ATR stops from config. Expand to seasonal_momentum, tsmom_12m, clean inventory (H-027 style if any) as production emitters with pre-reg.
7. **Harness execution:** Run full pipeline from tools/kimi_research_2026_05_20/statistical_validation_framework.py + six_gate_validated_strategy.py (G1-8 + bootstrap/FDR/MC/WF) + edge_stability_harness.py + alpha_engine/commodity_strategy_harness.py (150+ cand design, 25 futures, 12 cats incl COT/CARRY/SEAS/INV/MOM) on post-clean resolved (n>100 target per class/strat) + synthetic + paper pilot. Include costs/spreads, WF rolling (e.g. 6mo/3mo or COT-cycle aligned), 1000 MC, BH-FDR or SPA. Use COMMODITY-tuned thresholds (G1>=0.5, G8>=0.8, MDD<20, min n per window ~20-30). Output per-strat + ensemble gates + A/B markers. Pre-register any new variant (hypothesis-registry skill M-107) BEFORE.
8. **Regime / external / liquidity:** Add DXY/term-structure (contango/backwardation) / weather/USDA/EIA side features if not in; compare basket to KMLM/DBMF/GSCI/BCOM passive; stress CT=F full contract sizing (no micro). CPCV/DSR/PSR where contract cycles allow (Lopez de Prado).
9. **Dashboard / resolved refresh:** Force fresh dashboard_data.json + edge + effective_n_report + asset_class_health post re-agg; update smart_picks_by_asset.COMMODITY (raise minTrades); verify sizing_allowed + graduation_gate only on clean diversified (5+ symbols, CT share<30%).
10. **Documentation + loop:** Update 90day_plan_COMMODITY, hypothesis_registry (new H- for cleaned carry/seasonal), MASTER_ACTION_PLAN M-008/021/022/050, CYCLE summary, LOOP_STATUS. Run only after all P0 hygiene; otherwise defer again.

**Blockers / Citations:** Matches 6GATES MD:160-169,282 (COMMODITY sparse like FOREX, n limits WF); 90day:84-97 (over-emission primary), 122-174 (phases: re-agg P0, wire carry P1); registry H-001 operator_note "NOT salvageable"; cot_paper_pilot_status.json + overemission report (n=5 collapse); edge_stability_COMMODITY + commodity_carry_momo.json (current state); FOREX pending/FOREX_harness_rerun_prereqs_2026-05-21.md (parallel); CYCLE_2026-05-21_01_SUMMARY (COMMODITY ready for firing 3 post-prereqs).

**Success Target for Rerun:** Clean diversified COMMODITY basket (carry_momo + seasonal + tsmom + 1-2 clean COT ags) meeting T2: PF>=1.5 / real WR>=52% / sharpe>=0.4 / MDD<18% / independent n>100 / SPA pass / no conc >30%. Then shadow micro (all-classes-first) per codex.

*Generated 2026-05-21 firing 2 COMMODITY subagent. Parallel to FOREX pending. Do not rerun harness until P0-P1 complete.*
