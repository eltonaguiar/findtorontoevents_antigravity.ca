# EAGLE2 brainstorm — ollama-cloud-large

Mode is LIVE (smoke test returned "Claude: 2+2 equals 4") but the grounded
brainstorm call returned EMPTY content via the proxy chat route (reasoning-model
wrapper appears to consume the token budget on hidden thinking). ollama-cloud (Qwen)
and ollama-cloud-local (Gemma) returned usable text — see sibling files.
