# Weekly Real-Money Filter — 2026-05-17

> **RETRACTED 2026-05-17** — the CRYPTO confidence>=0.70 filter below is a selection artifact: every conf>=0.70 pick is an `ml_enhanced` per-symbol curve-fit (family PF 0.63). See `reports/deep_dive_crypto_ml_enhanced_artifact_2026-05-17.md`. No asset class has a proven filter.


Produced by `/money-maker-readyv2`. Numbers are the **canonical net-of-slippage**
view (`pf_registry.json::by_asset_class_policy_clean_net`), which the /audit
dashboard now reads directly (M-067 flip, PR #1179, confirmed live this run).

## Honest headline

**No asset class is real-money ready.** Net PF per class — COMMODITY 1.17,
CRYPTO 1.28, EQUITY 0.72, FOREX 0.33 — all sub-T2 (T2 = PF≥1.5). This filter
identifies the ONE proven edge filter that exists; it does not greenlight
live capital.

## CRYPTO — proven filter: confidence ≥ 0.70

The single statistically-defensible edge filter in the repo. Policy-clean
(killed/blocked strategies excluded), net-of-slippage, by confidence bucket:

| Confidence bucket | n | WR | PF |
|---|---|---|---|
| **HI ≥ 0.70** | 119 | 63.0% | **6.67** |
| MID 0.50–0.70 | 123 | 58.5% | 2.26 |
| LO < 0.50 | 536 | 47.8% | 0.21 |
| NONE (no conf) | 108 | 30.6% | 0.33 |

- Criteria: `asset_class=CRYPTO`, `confidence≥0.70`, status closed.
- Historical analog: **WR 63%, PF 6.67, n=119** — clears the v2 bar
  (PF≥1.5, WR≥50%, n≥100).
- avg_win 15.71% / avg_loss 4.01%.
- **Kelly size:** quarter-Kelly fraction 0.134 → $1,339/pick at $10k. That is
  too concentrated for real capital — **cap at 2–3%/pick** ($200–$300 at
  $10k) per the charter per-trade risk limit. Use `compute_position_size()`
  with the Hyro overlay, which applies the prop-safe vol-scaled cap.

**CAVEAT — the filter needs two more conditions.** Of the 36 current OPEN
crypto picks, only 3 pass `confidence≥0.70`, and all 3 are `ml_enhanced_*`
single-symbol curve-fits (one on TRXUSDT, a BLOCKED symbol). The proven
historical edge sits in the carrier strategies (`ensemble` PF 1.47,
`mega_mutation` PF 2.19, `st_fear_greed_contrarian` PF 7.87) — NOT the
ml_enhanced family. The real-money CRYPTO filter is therefore:

> `confidence≥0.70` **AND** `strategy ∈ {ensemble, mega_mutation,
> st_fear_greed_contrarian}` **AND** `symbol ∉ BLOCKED_SYMBOLS`

Today that yields ~0 actionable OPEN picks → CRYPTO has a *proven filter
awaiting qualifying live picks*, not a deployable pick list this week.

## COMMODITY / EQUITY / FOREX / ETF / BOND / FUTURES — no filter

| Class | n | net PF | Why no filter |
|---|---|---|---|
| COMMODITY | 160 | 1.17 | ~49% of trades are CT=F — COT-publication look-ahead leakage, on probation. Strip CT=F → PF ~0.35. No clean filter. |
| EQUITY | 31 | 0.72 | Below every charter floor (n≥100). Non-functional, not tradeable. |
| FOREX | 392 | 0.33 | Hard-disabled (`FOREX_HARD_DISABLE=1`), correctly. Only `cta_cross_asset_tsmom` USDJPY-SHORT shows edge — single-pair, single-direction, regime-conditional; not a class filter. |
| ETF | ~1 | — | Canonical n≈1 (ledger leak — see §27 M-101). |
| BOND | 1 | 0.0 | The one row is a mis-tagged USDJPY=X FOREX pick (§27 M-102). |
| FUTURES | 12 | 0.96 | The dashboard's n=203 is a resolver replay artifact (§27 M-103). |

## How to apply (CRYPTO only — paper/monitor)

1. Open findtorontoevents.ca/audit, CRYPTO filter.
2. Filter to `confidence≥0.70`; then keep only `ensemble` /
   `mega_mutation` / `st_fear_greed_contrarian` strategies; drop any
   `BLOCKED_SYMBOLS` symbol (TRXUSDT etc.).
3. Size per `compute_position_size()` — cap 2–3%/pick, NOT raw quarter-Kelly.
4. Exit on the pick's TP/SL.
5. **This remains paper/monitor.** CRYPTO net PF 1.28 is sub-T2; the conf≥0.70
   *subset* is the only thing clearing PF≥1.5, and it is not yet emitting
   qualifying live picks.

## Risk controls

- Max per-pick: **2–3%** of account (raw quarter-Kelly 13.4% is rejected as
  over-concentrated).
- Daily soft-stop: −2% total PnL → pause (Hyro overlay).
- DD halt: rolling-30d drawdown > 30% → `compute_position_size()` returns 0
  (`KELLY_DD_HALT_ENABLED=1`).

## v2 success-criteria status

| Criterion | Status |
|---|---|
| CRYPTO sub-class filter, PF≥1.5 @ n≥100 | ✅ confidence≥0.70: PF 6.67, n=119 — identified; not yet emitting qualifying live picks |
| EQUITY weekly filter ≥5 picks elite≥60 WR≥55% | ❌ n=31, non-functional |
| COMMODITY top strategy PF≥1.5 post-dedup | ❌ edge is CT=F leakage |
| ETF n≥150 | ❌ canonical n≈1 (ledger leak) |
| FOREX directional filter WR≥50% | ❌ only single-pair USDJPY-SHORT; hard-disabled |
| BOND top strategy at n≥20 | ❌ n=1 |
| Kelly sizing on filter picks | ✅ computed (capped) |

**Verdict: 1 of 7 criteria met.** The CRYPTO confidence≥0.70 filter is the
only proven edge. Real-money readiness is gated on §27 (M-088 confidence
exec gate, M-104 kill-ratchet) + forward accumulation — calendar-bound.
