# Peer validate: edge_stability auto-refresh wiring (2026-05-31)

## Pre-expectation
edge_stability JSONs would be auto-regenerated by some workflow but had drifted; HTML would have an EST stamp via its data file.

## Findings (methodology-proof)

### 1. Existing workflow coverage
```
$ grep -ln "edge_stability\|edge-stability" .github/workflows/*.yml
.github/workflows/audit-dashboard.yml
```
Raw matches in that file (lines 1071, 1182, 1232) — **all three are FTP-deploy lists** that ship `edge_stability.html` to `/audit/`. None of them invoke `tools/edge/edge_stability.py`.

Verdict: **NOT wired for regeneration** (refutes "fully auto"). Page is deployed hourly but the JSONs it consumes are static-since-2026-05-12.

### 2. Staleness proof
```
$ python3 -c "import json; d=json.load(open('audit_dashboard/data/edge_stability/edge_stability_index.json')); print(d['as_of'])"
2026-05-12T21:53:04.229451+00:00
```
19 days stale at 2026-05-31.

### 3. Script entry point
`tools/edge/edge_stability.py:8` — usage `python -m tools.edge.edge_stability --all`
`tools/edge/edge_stability.py:387` — `def main()`
`tools/edge/edge_stability.py:391` — `ap.add_argument("--all", action="store_true", ...)`
`tools/edge/edge_stability.py:50` — `PAYLOAD_PATH = REPO_ROOT / "audit_trail" / "data" / "dashboard_payload.json"` (file-only input; **no DB creds needed**).

### 4. EST stamp on the page
`audit_dashboard/edge_stability.html:97`:
```
document.getElementById('meta').textContent = `Last updated: ${toEST(idx.as_of)} EST · schema ${idx.schema_version}`;
```
Verdict: **stamps EST already** (matches); refreshing `as_of` server-side automatically updates the displayed EST string.

### 5. Live payload availability
```
$ curl -sI https://findtorontoevents.ca/audit/data/dashboard_payload.json | head -3
HTTP/2 200
last-modified: Sun, 31 May 2026 20:59:05 GMT
content-length: 18639244
```
Workflow can fetch via curl — avoids re-running the 30-min MySQL pipeline.

## Action shipped

- Branch: `ci/edge-stability-daily-refresh`
- File: `.github/workflows/edge-stability-refresh.yml` (33 lines)
- Cron: `30 0 * * *` (daily 00:30 UTC)
- PR: **#285** (draft)
- Workflow: curl live `dashboard_payload.json` → `python -m tools.edge.edge_stability --all` → commit JSONs with `[skip ci]`.

## Open question (post-merge)
- First `workflow_dispatch` run will validate the commit-back path; if `GH_PAT` is required for write, fallback is `github.token` (already in checkout step).
