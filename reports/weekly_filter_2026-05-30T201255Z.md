# Weekly Real-Money Filter — 2026-05-30 (UTC)

**Audit run:** `/money-maker-readyv2` · author `claude-gx10-c9b9` · timestamp `2026-05-30T20:12:55Z`
**Canonical sources (re-derived live, not carried forward):**
- `audit_dashboard/data/money_ready_verdict.json` — generated `2026-05-30T01:46:17Z` (≈18.5h old, FRESH)
- `audit_dashboard/data/pf_registry.json` — `by_asset_class_policy_clean_net` (canonical) + `by_asset_class_strategy_policy_clean_net`
- `audit_dashboard/data/dashboard_data.json` — **STALE 46.67h** (warn, not used for verdict — verdict file is authoritative)
- Live DB: `ejaguiar1_stocks.at_raw_picks` (raw signals) + `closed_picks.json` snapshots (alpha_engine, battleground, mercury2, paper_trading, ml_battleground)

## TL;DR — 0/8 classes pass any success criterion today

> **No class is money-ready.** Only **1** strategy across all classes meets the **PF≥1.5 / WR≥50 / n≥30** floor (`crypto_liquidity_wick_reversal_v1`) — and it **fails the vetted-stats single-source check** (100% concentration in `battleground`, 0 picks resolved in the production DB). No weekly filter can be emitted that would pass a hedge-fund manager's edge bar.

Skill snapshot drift (the in-skill 2026-05-28 table is now stale — re-derived above): CRYPTO degraded from n=652 / PF=1.31 (skill snapshot) → **n=325 / PF=0.886** (today). The previous "closest to viable" class has slipped below break-even PF.

---

## Per-class baseline (re-derived 2026-05-30)

| Class | n | WR% | PF | MDD% | Verdict (money_ready) | Skill criterion | Result |
|---|---:|---:|---:|---:|---|---|---|
| **CRYPTO** | 325 | 36.9 | 0.886 | 1.0 | NOT_READY | #2 sub-class WR≥50% / PF≥1.5 / n≥100 | **FAIL** (no sub-class survivor passes vetted discipline) |
| **EQUITY** | 38 | 28.9 | 0.116 | 1.0 | INSUFFICIENT_DATA | #1 5 picks elite_score≥60 / WR≥55% / n≥30 / PF≥1.5 | **FAIL** (n<100, PF≪1.5) |
| **COMMODITY** | 9 | 44.4 | 1.812 | 0.1 | INSUFFICIENT_DATA | #3 n≥50 post-dedup + PF≥1.5 | **FAIL** (n=9; PF appears OK but n far below 50) |
| **ETF** | 3 | 33.3 | 0.193 | 0.1 | INSUFFICIENT_DATA | #4 n≥150 + PF≥1.3 | **FAIL** (n=3, way below 150) |
| **FOREX** | 26 | 26.9 | 0.033 | 0.8 | INSUFFICIENT_DATA | #5 directional WR≥50% | **FAIL** (n<100, PF≪1) |
| **BOND** | 0 | — | — | — | INSUFFICIENT_DATA | #6 top strategy if n≥20 | **FAIL** (no closed bonds) |
| **FUTURES** | 11 | 9.1 | 0.475 | 0.2 | INSUFFICIENT_DATA | (no specific criterion) | n far below floor |
| **PENNY_STOCK** | 1 | 0.0 | 0.000 | 0.0 | INSUFFICIENT_DATA | (no specific criterion) | single sample |
| **UNKNOWN** | 5 | 60.0 | 1.995 | 0.0 | INSUFFICIENT_DATA | — | unclassified, do not size |

---

## Per-strategy edge discovery (PF≥1.5 & n≥30 from `pf_registry::by_asset_class_strategy_policy_clean_net`)

Only **3** strategies cleared the PF≥1.5 / n≥30 cell across all 71 tracked strategy/class rows:

| Class | Strategy | n | WR% | PF | Skill criterion 2 (WR≥50%) | Vetted survivor? |
|---|---|---:|---:|---:|:---:|:---:|
| CRYPTO | `UNKNOWN` (placeholder) | 34 | 26.5 | 5.81 | ✗ | ✗ outlier-suspect (skewed PF) |
| CRYPTO | `copy_trader_intel` | 34 | 47.1 | 1.66 | ✗ (47.1 < 50) | ✗ |
| **CRYPTO** | **`crypto_liquidity_wick_reversal_v1`** | **30** | **60.0** | **1.55** | ✓ | **see vetted-discipline below** |

The single survivor: `crypto_liquidity_wick_reversal_v1`. Now applied the 6 vetted-stats checks (skill rules):

1. **Dedup** on `dedup_hash` — no duplicates in the 366 `at_raw_picks` rows for this strategy.
2. **Single-source check** — `at_raw_picks` source breakdown: `battleground` 190 + `Battleground_Main` 176 = **366 / 366 = 100% one source family**. The pf_registry's "n=30" came entirely from `battleground/data/closed_picks.json` (the same engine's snapshot). **VIOLATES skill rule #2: ">60% single-source = concentration, not edge."**
3. **Outlier cap** — N/A (vetted-data set is empty due to #2).
4. **Min-n** — pf_registry n=30 ≥ skill floor; raw DB has 366 — but every one of those 366 is `status=OPEN` (never resolved in production).
5. **OOS / window stability** — N/A (no resolved rows in raw DB across 7d / 30d / 90d windows).
6. **Anomaly exclusion** (`is_resolution_trustworthy`) — N/A (no resolved rows).

**Verdict: REJECT.** The `crypto_liquidity_wick_reversal_v1` "edge" is a **single-source snapshot artifact** — exactly the pattern the skill's FUTURES worked example warned about. The pf_registry surfaces 30 picks from one engine's `closed_picks.json`; the live DB shows the same strategy producing 366 OPEN-only picks, none ever resolved. Without multi-source corroboration + actual resolution in production, this is **NOT an edge.**

---

## Path-forward per class (what would unblock each criterion)

| Class | Blocker | Action to reach money-ready |
|---|---|---|
| **CRYPTO** | Single-source artifacts; PF=0.886 < break-even on policy-clean cohort | Resolve some of the 39,699 OPEN raw picks → diversify source mix; re-validate `crypto_liquidity_wick_reversal_v1` against ≥2 independent engines |
| **EQUITY** | n=38 (well below n≥100); only `stocks_rsi2_pullback` is the proven sleeve per skill PART 2 | Wire more EQUITY emitters into production scoring; resolve the 228 OPEN EQUITY picks |
| **COMMODITY** | n=9; COT over-emission contamination (skill warning) | Apply COT dedup before claiming PF=1.81 is real; need n≥50 post-dedup |
| **ETF** | n=3 vs criterion n≥150 | Wire ETF emitters; resolve the 55 OPEN ETF picks |
| **FOREX** | PF=0.033 (catastrophic); LONG-direction block evaluation pending per criterion 5 | Run mutation-analysis on the 908 OPEN FOREX picks; identify a directional subset with WR≥50% |
| **BOND** | n=0 (no closed bonds in 90d window) | Confirm BOND emitters are firing; investigate why none reach resolution |
| **FUTURES** | n=11 / WR=9.1% | Skill warns FUTURES "zombie tile, real futures hidden under COMMODITY" — needs taxonomy clean-up |
| **PENNY_STOCK** | n=1 | Need volume; skill notes "Deep oversold BLOCKED by Gate 0" |

---

## Kelly sizing — illustrative only (no eligible filter to size today)

Skill criterion #7 requires Kelly sizing on every weekly-filter pick. **There are no eligible picks to size.** For completeness, the canonical sizer + parameters:

```python
from alpha_engine.kelly_position_sizer import compute_position_size
# signature: compute_position_size(pick: dict, stats: dict,
#                                  active_picks: list|None=None,
#                                  portfolio_value: float = 10_000.0) -> float (USD)
# stats: {"win_rate": 0.55, "avg_win_pct": 3.0, "avg_loss_pct": 2.0}
# kelly_fraction is internally 0.25× (quarter-Kelly) — same as audit charter.
# DD-halt:  KELLY_DD_HALT_ENABLED=1 + rolling_dd_30d > KELLY_DD_HALT_MAX → 0
# Hyro:     HYRO_RISK_SIZER_ENABLED=1 → prop-firm-style vol-scaled cap
```

For reference, if `crypto_liquidity_wick_reversal_v1` had been vet-cleared (WR=60%, assume avg_win=3.0%, avg_loss=2.0%):
- `kelly_fraction(p=0.6, win=3, loss=2, fraction=0.25)` ≈ 0.0833 (8.3% of NAV)
- At $10k → ~$833/pick. **Not authorized** until single-source + resolution gaps closed.

---

## Current OPEN-pick census (Step 6 — `at_raw_picks` live)

| Class | OPEN | Note |
|---|---:|---|
| CRYPTO | 39,699 | massive backlog; resolving these is the leverage point |
| (blank) | 2,320 | unclassified — needs `asset_class` backfill |
| UNKNOWN | 1,669 | same |
| FOREX | 908 | needs directional filter analysis |
| MEMECOIN | 548 | not in money_ready_verdict yet |
| EQUITY | 228 | |
| FUTURES | 130 | |
| PENNY_STOCK | 124 | |
| ETF | 55 | |

**Skill criterion 6 verification: filter coverage is N/A** since no weekly filter is emitted.

---

## Risk controls (would apply if any filter passed)

- Max per-pick: **0.25× Kelly** (quarter-Kelly), enforced by `compute_position_size`.
- Daily soft-stop: `HYRO_TODAY_PNL_PCT` env var sets the Hyro overlay; -2% triggers pause.
- DD halt: `KELLY_DD_HALT_ENABLED=1` + rolling_dd_30d > `KELLY_DD_HALT_MAX` (default 0.30) → size=0.
- Source diversification: any filter must show ≤60% single-source share (skill rule #2).
- Min-n: 100 decisive per class verdict, 30 per strategy.

---

## Decisions made + known limitations

| Decision | Rationale |
|---|---|
| Used `money_ready_verdict.json` as canonical despite `dashboard_data.json` 46.67h stale | Verdict file is fresh (≈18.5h) and skill names it "policy-clean verdict" canonical |
| Rejected the single PF≥1.5/n≥30 strategy survivor | Single-source artifact (skill rule #2); zero production resolutions |
| Did NOT emit a Kelly-sized weekly filter | Skill rule #6: "NO PLACEHOLDERS — real components + real states" — no eligible filter exists, sizing would be theatre |
| Did NOT call any class "ready" | Skill criterion-7 requires *7* gates simultaneously; 0/8 classes clear them |
| Did NOT push or FTP | Skill rules forbid push-without-pull + no live deploy without operator gate |

**Known limitations carrying forward:**
- `crypto_liquidity_wick_reversal_v1` deserves a follow-up: why do all 366 raw-DB picks sit OPEN? Either the strategy emits unrealistic TP/SL that never trigger, or the resolver is mis-classifying. Worth a finding.
- `pf_registry` pulls from `closed_picks.json` snapshots (not the live raw DB). When a snapshot is single-engine, the pf_registry's per-class numbers carry that concentration without flagging it. Tighter dedup pre-aggregation upstream would help.

---

## Final deliverable check (per skill "CHECK SUCCESS BEFORE STOPPING")

| Success criterion | Met? | Why |
|---|:---:|---|
| 1. EQUITY weekly filter ≥5 picks, elite_score≥60, WR≥55%, n≥30, PF≥1.5 | ✗ | EQUITY n=38, WR=28.9%, PF=0.116 — no filter cleared |
| 2. CRYPTO sub-class WR≥50%, PF≥1.5, n≥100 | ✗ | only `crypto_liquidity_wick_reversal_v1` cleared the n=30/WR=60%/PF=1.55 cell — fails single-source check; n far below 100 |
| 3. COMMODITY post-COT-dedup n≥50, PF≥1.5 | ✗ | n=9 raw, no COT dedup performed (insufficient signal first) |
| 4. ETF n≥150, PF≥1.3 | ✗ | n=3 — 50× below floor |
| 5. FOREX directional filter WR≥50% | ✗ | PF=0.033 across all directions; mutation-analysis not yet run |
| 6. BOND top strategy if n≥20 | ✗ | n=0; no decisive bonds in window |
| 7. Kelly sizing applied to filter picks | ✗ (vacuous) | no filter picks exist to size |

**0/7 criteria met.** Honest result preserved over a fabricated "edge".

---

*Generated by `/money-maker-readyv2` skill execution. Re-derive before reuse — never carry a typed table forward.*
