# Weekly Real-Money Filter — 2026-05-16

**Generated:** 2026-05-16T05:54:23Z · **Skill:** `/money-maker-readyv2`
**Data source:** `audit_dashboard/data/dashboard_data.json` (`generated_at` 2026-05-16T05:24:41Z, age 0.46h — fresh, passes <2h gate)
**Account model:** $10,000 · Kelly 0.25-fraction · sizing via `alpha_engine/kelly_position_sizer.py::compute_position_size()`

## Per-Class Baseline (verdict-grade `asset_class_health`, resolved_n)

| Class | resolved_n | WR % | PF | OOS WR % | Verdict |
|-------|-----------|------|-----|----------|---------|
| EQUITY | 393 | 53.2 | 1.65 | 62.2 | T2 PF✓ WR✓ — tradeable |
| ETF | 75 | 66.7 | 2.25 | 75.0 | T1-grade PF/WR, n<150 — on path |
| CRYPTO | 7565 | 47.2 | 1.35 | 45.7 | sub-T2 — needs sub-class filter |
| FOREX | 251 | 57.8 | 0.85 | — | WR>50 but PF<1 — losers bigger |
| BOND | 11 | 54.5 | 0.66 | 56.2 | n<20 — not measurable |
| COMMODITY | 0 | — | — | — | 0 resolved post-COT-dedup — N/A |
| FUTURES | 2 | 100 | — | — | n<20 — not measurable |

## EQUITY Top Picks Filter

- **Criteria:** `asset_class=EQUITY`, `status=OPEN`, `direction=LONG`, `elite_score≥60`, strategy ∈ {`multi_asset_copytrader`, `aggregated_picks`}
- **Expected WR (historical analog):** 74–76% (`multi_asset_copytrader` 74.2% n=132 · `aggregated_picks` 76.2% n=386)
- **Expected PF (historical analog):** 4.56–5.32 (class-level resolved PF 1.65, n=393)
- **Kelly size:** `multi_asset_copytrader` 14.5% / pick ($1,447 @ $10k); cap at 10% (Kelly 0.25-fraction recommends 14.5% but per-pick hard cap = 10%)
- **⚠ LIVE GAP:** `alpha_engine/data/active_picks.json` currently holds **2 EQUITY picks, both `elite_score<60`** (YM=F 57.0 — also a mislabeled future; JNJ 13.8). The filter is statistically sound but the emission pipeline is **not currently producing qualifying EQUITY picks.** Action item below.

## CRYPTO Sub-Class Filter ✓ (criterion met)

- **Criteria:** `asset_class=CRYPTO`, `status=OPEN`, strategy ∈ {`mega_mutation`, `claude_gainer`, `claude_gainer_st`}
- **Expected WR:** 56–59% (`mega_mutation` 58.8% n=165 · `claude_gainer` 56.2% n=32 · `claude_gainer_st` 57.5% n=113)
- **Expected PF:** 1.36–2.43 — `mega_mutation` is the standout: **PF 2.43, WR 58.8%, resolved_n=165 ≥100** ✓
- **Kelly size:** `mega_mutation` 8.6% / pick ($864 @ $10k); `claude_gainer_st` 3.75% ($375)
- **AVOID (drag):** `quan_engine` (PF 1.22 / WR 33.2), `luxalgo_filters` (PF 1.12), `copy_trader_highscore` (PF 1.02), `super_signals` (PF 0.98)
- **Live status:** `mega_mutation`/`claude_gainer_st` currently `status=monitoring`, 0 active picks — historically proven but not emitting this week. `aggregated_picks` (14 active) and `multi_asset_copytrader` (16 active) carry the live CRYPTO book.

## ETF Filter (on path to OOS_READY)

- **Criteria:** `asset_class=ETF`, `status=OPEN` — class-wide; resolved PF 2.25, WR 66.7%, OOS WR 75%
- **n=75 of 150 target** — keep accumulating; do NOT promote to "proven real-money" until resolved_n≥150
- **PF≥1.3 maintained** ✓ (2.25)
- **Kelly size:** use class WR 66.7% — defer per-pick sizing until n≥150 and a single dominant ETF strategy is isolated

## FOREX (mutation in progress — no real-money filter yet)

- resolved PF **0.85 < 1.0** — WR 57.8% is positive but average loss exceeds average win → no positive-expectancy filter
- `FOREX_DIRECTIONAL_GATE_ENABLED` (default ON) blocks low-conviction FOREX LONGs (`elite<75 or conf<0.75`); `passes_forex_symbol_gate` blocks autopsy-killed pairs (NZDUSD/EURJPY/USDCHF)
- **Filter when going live:** FOREX SHORT only, or LONG with `elite≥75 AND conf≥0.75`. Re-evaluate after the gates accumulate 30+ post-gate resolved picks.

## BOND / COMMODITY / FUTURES — not measurable

- COMMODITY: 0 resolved post-COT-dedup (the headline PF 2.37 was falsified — see `reports/commodity_cot_post_dedup_rederivation_2026-05-16.md`). `multi_asset_cot` shows PF 4.72 but is `status=monitoring`/pre-dedup — do **not** trade on it.
- BOND n=11, FUTURES n=2 — below the n≥20 measurability floor. Accumulate.

## How to Apply

1. Open `findtorontoevents.ca/audit`.
2. **CRYPTO:** asset-class filter = CRYPTO; strategy filter = `mega_mutation` (or `claude_gainer_st`); status = OPEN.
3. **EQUITY:** asset-class filter = EQUITY; strategy = `multi_asset_copytrader`; require `elite_score≥60` — *currently returns 0; wait for pipeline.*
4. Size each pick per the Kelly % above, hard-capped at 10% of account.
5. Exit: follow the TP/SL set on the pick.

## Risk Controls

- **Max per-pick:** 10% of account (Kelly 0.25-fraction caps recommend higher for `multi_asset_copytrader`, but enforce a 10% hard ceiling)
- **Daily soft-stop:** −2% total PnL → pause new sizing (Hyro overlay, `HYRO_RISK_SIZER_ENABLED=1`)
- **DD halt:** rolling 30d drawdown > 30% → `compute_position_size` returns $0 (`KELLY_DD_HALT_ENABLED=1`, `KELLY_DD_HALT_MAX=0.30`)

## Success Criteria Status

| # | Criterion | Status |
|---|-----------|--------|
| 1 | EQUITY ≥5 picks elite≥60 WR≥55 PF≥1.5 | ✗ filter sound, **0 qualifying live picks** — emission gap |
| 2 | CRYPTO sub-class filter WR≥50 PF≥1.5 resolved_n≥100 | ✓ `mega_mutation` PF 2.43 WR 58.8 n=165 |
| 3 | COMMODITY top strategy once n≥50 post-dedup | N/A — n=0 post-dedup |
| 4 | ETF n≥150 path, PF≥1.3 | ◐ on path (n=75, PF 2.25) |
| 5 | FOREX directional filter if WR≥50 exists | ◐ gates shipped; PF<1, no positive-EV filter yet |
| 6 | BOND top strategy once n≥20 | N/A — n=11 |
| 7 | Kelly sizing on all weekly-filter picks | ✓ computed per filter above |

**Verdict:** 2/7 fully met (CRYPTO filter, Kelly sizing). 2/7 on-path (ETF, FOREX). 3/7 N/A — blocked by insufficient resolved sample (COMMODITY/BOND/FUTURES).

## Action Items

1. **EQUITY emission gap (P1)** — resolved EQUITY edge is genuine (PF 1.65, n=393, OOS WR 62.2) but `active_picks.json` carries only 2 EQUITY picks, both `elite<60`. The `multi_asset_copytrader`/`aggregated_picks` strategies that produce EQUITY edge are emitting CRYPTO-heavy. Investigate why EQUITY-side emission is starved.
2. **YM=F mislabel** — `YM=F` (E-mini Dow future) is tagged `asset_class=EQUITY` in `active_picks.json`. The `contract_type`→FUTURES routing (shipped in `dashboard_generator._derive_asset_class`) is not applied at the `active_picks.json` writer. Wire `_classify_contract_safe` into that emitter.
3. **COMMODITY rebuild** — 0 resolved post-dedup. The COT pipeline needs to accumulate clean post-dedup picks before any COMMODITY filter is trustworthy.
