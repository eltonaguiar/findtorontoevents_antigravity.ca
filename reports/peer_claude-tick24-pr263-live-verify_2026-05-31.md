# PR #263 Live-Verify (Tick 24) — CRYPTO 0.8-bucket dampen

**Date:** 2026-05-31
**Verifier:** claude-opus-4-7 (subagent)
**PR:** https://github.com/eltonaguiar/findtorontoevents_antigravity.ca/pull/263 (MERGED 2026-05-31T20:18:37Z)
**Verdict:** WORKING (code-level proven) — PARTIAL on production observability (low CRYPTO volume).

## TL;DR

- PR #263 dampen code path is mathematically correct and active in production code.
- Direct unit test of `_compute_ml_composite()` produces score×0.5 for CRYPTO conf∈[0.75, 0.85) and 1.0× for all other buckets / asset classes.
- `alpha_engine/data/smart_picks.json` was regenerated at **2026-05-31T20:19:23Z** (+46s after merge) — confirms the new code is loaded.
- BUT: only **1 CRYPTO pick was processed in that run** (ETHUSDT, `confidence: None` → outside the dampen band), and the production scanner has produced **0 new CRYPTO trading_picks** since 19:00Z. So a side-by-side bucket distribution comparison is not yet feasible from production data.

## Evidence

### 1. PR #263 code (alpha_engine/smart_picks_engine.py:146-153)

```python
if ml is not None and float(ml) > 0:
    ml_val = float(ml)
    score = ml_val * _w_ml + conf * _w_conf + fwd_wr * _w_fwd
    if _raw_ac == "CRYPTO" and 0.75 <= conf < 0.85:
        score *= 0.5
    score *= _claude_penalty * _tier_penalty
```

### 2. Unit test of `_compute_ml_composite` (live code, no mocks)

```
asset_class | conf  | score   | tag
CRYPTO     | 0.70  | 0.5000  | ml_composite
CRYPTO     | 0.75  | 0.2500  | ml_composite   <- dampen ON
CRYPTO     | 0.80  | 0.2500  | ml_composite   <- dampen ON
CRYPTO     | 0.84  | 0.2500  | ml_composite   <- dampen ON
CRYPTO     | 0.85  | 0.5000  | ml_composite   <- dampen OFF
CRYPTO     | 0.90  | 0.5000  | ml_composite
EQUITY     | 0.80  | 0.5300  | ml_composite   <- control, no dampen

0.8/0.7 ratio = 0.5000 (expected 0.5)
0.8/0.9 ratio = 0.5000 (expected 0.5)
```

**Mathematical proof: dampen fires exclusively for CRYPTO conf∈[0.75, 0.85).**

### 3. Production smart_picks.json regenerated post-merge

| field | value |
|---|---|
| file mtime | 2026-05-31 20:21:20Z |
| `generated_at` in payload | 2026-05-31T20:19:23.959628+00:00 |
| `total_scored` | 1 |
| `crypto_scored` | 1 |
| picks[].symbol | ETHUSDT |
| picks[].confidence | None |
| picks[].asset_class | CRYPTO |
| picks[].ranking_method | (not ml_composite — no ml_score path) |

The single CRYPTO pick has `confidence=None` → conf coerced to 0.0 → outside [0.75, 0.85) → dampen correctly does NOT fire. Behaviour matches code.

### 4. Production volume context (ejaguiar1_stocks.trading_picks)

CRYPTO pick creation by hour (UTC):

```
19:00 → 2
18:00 → 20
17:00 → 26
16:00 → 36
15:00 → 15
...
20:00 → 0 (PR #263 merged 20:18)
21:00 → 0
```

The upstream signal generators have stalled in the 20:00Z window — this is independent of PR #263 (which is downstream ranking, not pick generation).

### 5. Pre-merge baseline (CRYPTO 24h pre-merge bucket distribution)

```
bucket | n
None   | 10
0.5    | 65
0.6    | 46
0.7    | 124
0.8    | 62    <- target bucket
0.9    | 44
1.0    | 9
```

Confirms the 0.8 bucket is normally well-populated (~62 picks/day) — once upstream generators resume we will be able to verify dampen visibility in production rankings.

## Verdict Mapping

- **WORKING (code path)**: ✅ Unit test of `_compute_ml_composite` returns 0.5× score for CRYPTO 0.8-bucket. Smart_picks.json regenerated by post-merge code at 20:19Z. No code-reload issue.
- **NOT YET OBSERVABLE (production)**: ⚠ Only 1 CRYPTO pick processed since merge (n=1, confidence=None) — insufficient to show bucket-level avg_score drop in the JSON surface.
- **Recommendation**: re-verify in ~6-12h once the production_scanner resumes normal CRYPTO volume. Expected post-merge: bucket-0.8 avg smart_score ~50% of buckets 0.7 / 0.9.

## Follow-up actions

1. Mark **PR263_VERIFIED_CODE_LEVEL** in operator queue (PR #259 TL;DR).
2. Keep CONFIDENCE_INVERT incident in **DEPLOYED-PENDING-LIVE-OBS** state (not full RESOLVED) until production bucket data confirms 0.5× ratio.
3. Investigate the upstream CRYPTO signal-generator stall (20:00Z onwards, separate from PR #263 — likely scheduler / scanner issue).

## Reproducer

```bash
python3 -c "
from alpha_engine.smart_picks_engine import _compute_ml_composite
for c in (0.70, 0.75, 0.80, 0.84, 0.85, 0.90):
    print('CRYPTO', c, _compute_ml_composite({'asset_class':'CRYPTO','confidence':c,'ml_score':0.5,'strat_fwd_wr':0.5}))
print('EQUITY 0.80', _compute_ml_composite({'asset_class':'EQUITY','confidence':0.80,'ml_score':0.5,'strat_fwd_wr':0.5}))
"
```
