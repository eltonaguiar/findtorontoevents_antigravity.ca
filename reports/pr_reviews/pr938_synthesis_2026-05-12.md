# PR #938 Multi-Agent Swarm Review Synthesis — 2026-05-12

**PR:** #938 — `QUANT_EDGE_ANALYSIS_PER_ASSET_CLASS.md` (docs-only, +349/-0, already merged)
**Author attribution:** "External Quant Review (OpenClaw Agent)"
**Reviewers (3 parallel agents):** security / architecture / code-quality
**Aggregate verdict:** **HOLD-EQUIVALENT — REQUIRES CORRECTION COMMIT** (PR already merged, so fixes go in follow-up).

---

## 1. Aggregate severity board

| Source | Verdict | Top severity | Confidence |
|---|---|---|---|
| Security (pr-reviewer) | approved_with_concerns | medium | HIGH |
| Architecture (general-purpose) | HOLD | major | HIGH |
| Code quality (pr-reviewer) | COMMENT_ONLY | **blocking ×5** | HIGH |

**Aggregate fabrication risk: HIGH.** Code-quality agent verified 8 quantitative claims as wrong against live `dashboard_data.json`. Numbers appear to come from `.claude/worktrees/agent-a5ce407f/audit/data/dashboard_data.json` (2026-04-11 snapshot) not the live file.

---

## 2. Blocking factual errors (code-quality agent, all verified against live dashboard)

| Claim in PR | Real value (live `dashboard_data.json::performance.asset_class_health`) | Damage if acted on |
|---|---|---|
| Net Sharpe = **-8.27 annualized** | **+1.25** | False catastrophic narrative; would trigger emergency policy |
| BOND PF = **25.9**, n=8 | PF **0.66**, n=11 | Inflates BOND ranking 39× |
| COMMODITY PF = **1.08** "Breakeven" | PF **3.94**, WR 67.8%, n=425, **Tier-1 exceeds PF>2/WR>55** | Misclassifies system's BEST class as dead, would discard top alpha |
| FOREX PF = **0.49**, n=685 | PF **0.29**, n=1,357 | Understates problem; doubles the trade count |
| CRYPTO PF = **0.49**, n=15,749 | PF **1.36**, n=7,797 | Halves real performance; doubles fake trade count |
| EQUITY PF = **0.70** | PF **1.55**, WR 53.2%, n=447, sizing-allowed | Misclassifies Tier-2 candidate as broken |
| ETF "**Dead** (PF 0.19, n=20)" | PF **1.34**, WR 56.1%, n=107, sizing-allowed | Recommends discarding working infra |
| FUTURES PF=0.08, n=18 | n=**0**, status=insufficient_data | Wrong premise for action plan |

**Source of the bad numbers:** `.claude/worktrees/agent-a5ce407f/audit/data/dashboard_data.json` (generated 2026-04-11). The agent producing PR #938 had a stale worktree mounted and read from it instead of the live file at `audit_dashboard/data/dashboard_data.json`.

---

## 3. Policy / process violations

### CLAUDE.md mutate-before-kill violation (major, code-quality + architecture)
PR §5.3 recommends "Block forex picks until resolver fix is confirmed clean (P0)" with no reference to:
- `docs/MUTATION_THREE_AXIS_PROTOCOL.md` (CLAUDE.md cites as mandatory)
- `docs/STRATEGY_INVESTIGATION_BEFORE_KILL.md`
- Existing `reports/asset_class_deep_dive_FOREX_2026-05-12.md` already covers approved path

### Wire-Up Rule violation (major, architecture)
PR enumerates 6 follow-up code PRs (TRX block, per-asset scoring, WFE wire to quality_gates.py, swing resolver deploy, FUTURES reclass, CI consolidation) without a `## Wiring Plan` section naming target files + functions.

### Placement convention (minor, architecture + code-quality)
File at repo root. Convention is `reports/<name>_YYYY_MM_DD.md`. All comparable analyses live there (`reports/SYNTHESIS_*`, `reports/deep_dive_FOREX_*`, `reports/hedge_fund_performance_*`).

### Test-suite false-negative (major, code-quality)
PR §3.2 claims "No test suite — I see no pytest or unit test workflows." Verified false: `.github/workflows/ci-tests.yml:59` runs `python -m pytest tests/ paper_trading/tests/ alpha_engine/tests/` covering ~747 tests.

### Workflow + file-count undercounts (major, code-quality)
- Claimed 48+ workflows. Actual `ls .github/workflows/*.yml | wc -l = 314` (6.5× higher).
- Claimed 1,093 files. Actual `git ls-files | wc -l = 19,590` (18× higher).
- Suggests agent only saw first page of GitHub API listing, not full traversal.

---

## 4. Security exposure (security agent)

### SEC-001 (MEDIUM) — DB creds disclosure in committed doc
Document text contains:
- `eajaguiar1_stocks` (DB username)
- `ejaguiar1_backtests` (DB username)
- `mysql.50webs.com` (production host)

These are now permanently in git history. Username + host → brute-force/credential-stuffing barrier lowered. No passwords leaked, but recon data is actionable.

**Recommended fix:**
1. **Rotate MySQL passwords for both database users** as precaution.
2. Genericize doc text — replace with `<db_user>` placeholders.
3. Future analysis docs that touch production schema: keep in private gist or strip table names + row counts.

### SEC-002 (LOW) — Architecture disclosure
Doc enumerates table names, row counts (28,705,218 trades), CI workflow count, system count — complete schema/topology blueprint. Low individual risk, high aggregate recon value.

### SEC-003 (LOW) — Performance figures
-23,187% PnL and -8.27 Sharpe (both stale) published. Public repo exposes financial state. Future analyses: round to ranges or keep private.

### SEC-004 (QUESTION) — OpenClaw Agent provenance
Unclear whether "OpenClaw Agent" had direct DB access using stored credentials. If yes, rotate those credentials — they passed through an untrusted third-party system.

---

## 5. Strengths confirmed across agents

1. **TRXUSDT concentration warning correct** — `alpha_engine/data/active_picks.json:3091` confirms `"WARNING: 100.0% of total PnL comes from TRXUSDT trades..."`. 2 active TRX picks remain.
2. **swing_resolver.py orphan-status correct** — grep finds only `tests/test_swing_resolver.py` as caller. No production wire-up.
3. **walk_forward_validator.py not wired to quality_gates.py** — confirmed via grep, zero matches.
4. **Document structure + framing direction-correct** — trust_score as cross-asset filter, regime-conditioning, per-asset deep-dive layout all sensible.

---

## 6. Future action items (ranked by leverage)

### Immediate (this week)

1. **Open follow-up PR `pr938-corrections`** that:
   - Moves file to `reports/quant_edge_analysis_per_asset_class_2026_05_12.md`
   - Re-derives every quantitative claim from current `audit_dashboard/data/dashboard_data.json::performance.asset_class_health`
   - Replaces forex-kill recommendation with pointer to `docs/MUTATION_THREE_AXIS_PROTOCOL.md` + existing `reports/asset_class_deep_dive_FOREX_2026-05-12.md`
   - Adds `## Wiring Plan` section naming caller files + functions for each proposed code change
   - Genericizes DB usernames + host references
2. **Rotate MySQL passwords** for `eajaguiar1_stocks` and `ejaguiar1_backtests` (SEC-001). Then rotate again if OpenClaw Agent had direct DB access (SEC-004).
3. **Verify OpenClaw Agent provenance** — what credentials did it use? If production DB creds, those leaked through a third-party agent and need rotation.

### Short-term (next 2 weeks)

4. **Wire `swing_resolver.py` to production** — pick one entry point: `quality_gates.py` smart_gate or `dashboard_generator.py` outcome resolver. Currently dead code per Wire-Up Rule.
5. **Wire `walk_forward_validator.py` into `quality_gates.py::smart_gate`** — adds WFE filter referenced in PR §2.5 / §5.6.
6. **TRX concentration block** — Add `TRXUSDT` to a blocked-symbol list with min-n quarantine. Currently 100% of system PnL is one symbol; that's not edge, that's variance.
7. **Add stale-worktree guard** — pre-commit hook OR CI step that errors if a doc cites `dashboard_data.json` numbers older than 7 days. Prevents future PR-938-style fabrications.

### Medium-term (next month)

8. **CI consolidation** — 314 workflow files is unmanageable. Audit which actually fire vs which are dead. PR's §3.1 framing wrong on the count but right on the principle.
9. **Per-asset scoring weights** — PR §2.4 proposes splitting smart_score into per-class weight vectors. Worth investigating once correct class baselines are reset.
10. **FUTURES reclass** — n=0 currently. Either populate from micro-futures picks already on `theswarm` (MES1!, MCL1!) or remove from `asset_class_health` to avoid skewing tier targets.

---

## 7. Areas to check (places affected by this PR but not edited)

1. **`CLAUDE.md`** — does it need a new rule: "Analysis docs must cite `dashboard_data.json` snapshot date + verify against live file before commit"?
2. **`docs/MUTATION_THREE_AXIS_PROTOCOL.md`** — was this protocol invoked for the FOREX block claim? If not, document violation in a feedback memory.
3. **`audit_trail/dashboard_generator.py`** — does it need a `_last_generated_at` watermark exported so downstream consumers can flag stale reads?
4. **`memory/MEMORY.md`** — should pin a `feedback_stale_worktree_fabrication` entry so future agents don't repeat the pattern.
5. **`tools/`** — is there a `scan_for_stale_dashboard_refs.py` worth building? Walks markdown for `dashboard_data.json` references + diffs against live.
6. **`.gitignore` / pre-commit** — should `.claude/worktrees/*/audit/data/dashboard_data.json` be off-limits to read? Or at minimum, flag reads from it as suspicious.
7. **`reports/asset_class_deep_dive_FOREX_2026-05-12.md`** — verify it actually contains the mutate-before-kill analysis PR #938 should have referenced.
8. **GitHub Actions audit** — actual count is 314, not 48. Run `gh workflow list --all --limit 500` to enumerate + flag dead workflows.

---

## 8. Recommendation

**Action:** open a corrections PR within 48 hours to repair the factual record. Do **not** allow follow-up code PRs to cite PR #938's numbers (a downstream PR built on "COMMODITY is dead" or "ETF needs new strategies" would discard real alpha).

**Top-3 fix-before-acting:**
1. Replace every quantitative claim with live `dashboard_data.json` value + cite the timestamp.
2. Rotate the two DB usernames' passwords + verify OpenClaw Agent didn't have production DB access.
3. Add `## Wiring Plan` to every proposed follow-up code change before opening those PRs.

---

## 9. Files referenced (3 source reviews)

- `reports/pr_reviews/pr938_arch_2026-05-12.md` (architecture agent, full 1500-word report)
- Security agent: returned as inline JSON, archived above in §4
- Code-quality agent: returned as inline JSON, archived above in §2, §3

All three confirmed the fabrication risk. Aggregate evidence below.
