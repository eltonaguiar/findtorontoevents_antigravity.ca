# PR #904 Review — 2026-05-12

**Engine:** claude-sonnet-4-6 (via pr-reviewer subagent)
**Verdict:** **HOLD** (high confidence)
**Fabrication risk:** LOW (all claims verified against diff / CI logs / git ls-tree)

## Summary

PR #904 ships nav pills + 21 swarm YAML configs + `audit_dashboard/edge_stability.html` + 16 smoke tests + `research-orchestrator.yml` workflow. **Core Python modules** (`tools/research/*.py`, `tools/edge/edge_stability.py`) are **NOT new in this diff** — they were already on main before the branch diverged. The genuinely new artifacts are the swarm configs, the edge_stability HTML page, the smoke tests, the orchestrator workflow, and template.html nav-pill additions.

## Blocking issues (2)

### B1 — CI red (4 tests fail)

CI run **25709677529** failures (both Py 3.11 + 3.12):
1. `tests/test_orphan_emitter_wire.py::test_collect_all_picks_reads_orphan_emitters` — *"BOND pick missing; saw active=[SPY, ES=F]"*
2. `tests/test_quality_gates.py::test_smart_gate_uses_strategy_score_overrides_for_proven_non_crypto` — *"assert 40 == 30"*
3. `alpha_engine/tests/test_confluence.py::TestStrategyFamiliesCoverage::test_covers_all_forex_strategies` — *"Forex strategies missing family assignment: {inverse_carry_contrarian}"*
4. `alpha_engine/tests/test_confluence.py::TestStrategyFamiliesCoverage::test_covers_all_strategies_combined`

**All 4 are pre-existing on origin/main**, not caused by this PR's diff. Still — branch must be rebased onto a green-main commit OR the pre-existing failures fixed before merging.

**Fix:** rebase or fix `inverse_carry_contrarian` STRATEGY_FAMILIES gap + orphan-emitter BOND pick + smart_gate threshold mismatch.

### B2 — Missing edge_stability_index.json, no .catch() fallback

`audit_dashboard/edge_stability.html` does:

```js
fetch("data/edge_stability/edge_stability_index.json?cb=" + Date.now()).then(r => r.json()).then(idx => { ... });
```

No `.catch()`. The file does **not exist** on PR branch or main (confirmed `git ls-tree`). Page will silently show blank grid in production.

**Fix:**
1. `python -m tools.edge.edge_stability --all` → commit resulting `edge_stability_index.json` to `audit_dashboard/data/edge_stability/`
2. Add `.catch(err => { document.getElementById('cards').textContent = 'Edge stability data not yet generated.'; })` to fetch chain.

## Major issues (2)

### M1 — Wire-Up Rule violation

PR body labels `tools/edge/edge_stability.py` as "sidecar" but contains **no `## Wiring Plan` section** as required by CLAUDE.md Wire-Up Rule.

**Fix:** add `## Wiring Plan` section naming the target caller file + function + expected PR/date.

### M2 — Workflow doesn't refresh edge_stability data

`research-orchestrator.yml`:
- Does NOT invoke `python -m tools.edge.edge_stability --all`
- Does NOT `git add audit_dashboard/data/edge_stability/`

**Result:** edge stability data goes stale after first weekly run.

**Fix:** add `python -m tools.edge.edge_stability --all` step and extend the git-add line in commit step.

## Minor issues (2)

### m1 — Hover-state regression

`audit_dashboard/template.html` onmouseover for "Jump to Active Picks" button:
- Before: `rgba(245,158,11,0.2)`
- After (via `-X theirs` cherry-pick): `rgba(245,158,11,0.1)`

Slightly darker hover. Cosmetic.

**Fix:** restore to `rgba(245,158,11,0.2)`.

### m2 — PR description inaccuracy

PR claims `tools/edge/edge_stability.py` is "stdlib only" but it imports `from alpha_engine.walk_forward_validator import (compute_window_metrics, parse_ts,)`.

**Fix:** update PR description to "stdlib + alpha_engine.walk_forward_validator".

## Open question

### Q1 — Direct push to main from orchestrator

`research-orchestrator.yml` pushes directly to `origin/main` (not a staging branch). Has 3-iteration retry with `git pull --rebase`. CLAUDE.md and memory `feedback_preserve_peer_changes.md` warn against high-velocity-repo contention.

**Recommend:** consider pushing to a dedicated `research-data` branch + auto-merge OR scheduled PR, OR confirm the direct-push pattern is intentional for this workflow.

## Strengths (verified)

1. 16 smoke tests in `tests/test_edge_stability_smoke.py` all PASS on both Py 3.11 + 3.12
2. SSRF guard in `tools/research/verify_citations.py:42-76` properly rejects loopback/IMDS/RFC-1918/link-local URLs via DNS resolution + `ipaddress.ip_address(addr).is_loopback / is_link_local / is_private` checks
3. XSS mitigations in `edge_stability.html`: `esc()` HTML-escapes user values, `escVerdict()` whitelists allowed verdicts before innerHTML insertion
4. `research-orchestrator.yml` retry logic correctly uses `pull --rebase --autostash` before push, with exponential backoff and `[skip ci]` to avoid workflow loops
5. Known ghost-artifact strips for MATICUSDT/quan_engine are coded directly into `GHOST_SYMBOL_STATS` with test coverage in `test_normalize_pick_strips_ghost`

## Next action

Author needs to address **B1 + B2** before merge can be considered. M1/M2 should be addressed in the same revision. Minor items are optional polish.

Posting the structured commentary inline on the PR (gh pr comment) is the next step — but per pr-reviewer agent contract, this review only writes the report; user decides whether to post.
