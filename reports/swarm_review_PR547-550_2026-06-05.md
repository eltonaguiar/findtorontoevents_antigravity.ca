# Swarm Review — PRs #547–#550 (2026-06-05)

## Verdict summary

| PR | Title | Swarm verdict | Outcome |
|----|-------|---------------|---------|
| #550 | docs real-money edge hunt | **APPROVE** | **Merged** to main (`d3a6e87802`) |
| #549 | crypto onchain + fundamental_macro_gates | **APPROVE** (rebase) | **Closed** — already on main (`be7721dc34`) |
| #547 | PEAD earnings cache | **CHANGES** | **Closed** — superseded by `be7721dc34` |
| #548 | PEAD probation | **CHANGES** | **Closed** — superseded by `be7721dc34` |

## Consensus issues (addressed post-review)

1. **`max_age_days` unused** — fixed in follow-up commit on `fix/pead-earnings-max-age-filter`.
2. **`assume_guidance_on_beat=True` fabricates guidance** — default now `False`; use `PEAD_REQUIRE_GUIDANCE_RAISE=0` for shadow when needed.
3. **CI failure on #547/#548** — `ModuleNotFoundError: fundamental_macro_gates`; resolved once module landed on main.

## Merge order used

1. #550 (docs) — merged first  
2. Code paths from #547/#548/#549 were already on main via money-ready blitz commit  
3. Duplicate PRs closed to avoid dirty merge  

## Ops reminder

Probation live emits require explicit:

`PEAD_EQUITY_ENABLED=1 PEAD_EQUITY_PROBATION=1` (max 2 via `PEAD_EQUITY_PROBATION_MAX`)

Do not enable until 2026-06-14 review gate unless operator override.