# M-067 — per-class sizing_allowed diff (registry-sourced vs recompute)

Gating artifact for flipping `AUDIT_HEALTH_SOURCE` default `recompute`→`registry`.
Red-team R8 requires: NO asset class may flip `blocked → allowed` unreviewed.

Generated 2026-05-17. Left = current dashboard (in-generator recompute);
right = registry-sourced (`pf_registry.json` net policy-clean view).

| Class | recompute sizing / pf | registry sizing / pf | flip |
|---|---|---|---|
| BOND | False / 0.66 | False / 0.0 | — |
| COMMODITY | False / 7.71 | False / 1.17 | — |
| CRYPTO | True / 1.46 | True / 1.28 | — (both allowed, both >1.0) |
| EQUITY | True / 2.04 | False / 0.72 | **allowed → blocked** |
| ETF | False / 2.49 | None (not in net view) | — |
| FOREX | False / 2.23 | False / 0.33 | — |
| FUTURES | False / — | False / 0.96 | — |
| PENNY_STOCK | None | False / 0.0 | — |

## Verdict: SAFE TO FLIP

- **blocked → allowed flips: NONE.** The dangerous direction (R8) does not occur.
- The single change — **EQUITY allowed → blocked** — is *protective*: the
  in-generator recompute reported EQUITY PF 2.04 (inflated) and permitted
  sizing; the canonical net policy-clean registry reports PF 0.72, correctly
  below the 1.0 sizing floor. Flipping to the registry **removes a wrongly-
  granted sizing permission** — strictly more conservative.
- CRYPTO stays allowed under both (recompute 1.46, registry net 1.28 — both
  above the 1.0 `sizing_allowed` floor, both still below the 1.5 T2 floor;
  no behaviour change).

Default flipped to `registry` in `dashboard_generator.py`
(`_registry_backed_ac_breakdown`). Rollback: `AUDIT_HEALTH_SOURCE=recompute`.
