# B11 Multi-AI Feedback — Self-Review #2 (DeepSeek proxy) — 2026-05-02

**Item:** B11 — ETF Source Diversification  
**Source:** `reports/REMAINING_ACTION_ITEMS_2026_04_30.md`  
**Review date:** 2026-05-02

---

## A. Confirmed assumptions

1. **Correct hook points identified.** `alpha-engine-etf.yml` run step at line ~40 is the sole location needing change. No other files require modification beyond the workflow and the updates doc.

2. **The emitter imports are correct.** `etf_strategies.etf_sector_momentum` exists at `alpha_engine/etf_strategies.py:181`. `ETF_SYMBOLS` is imported from `alpha_engine/config.py`. The emitter's dependency chain is intact.

3. **Existing tests adequately cover the change.** `tests/test_etf_sector_emitter.py` tests: schema correctness, short-signal handling, defaults, disabled-flag behavior, yfinance-unavailable graceful fallback, JSON_PICK_SOURCES registration, and file schema. All 7 pass. The workflow change doesn't add new code paths that need new tests.

4. **Risk classification LOW is accurate.** The workflow change is purely additive: the emitter already exists with safe fallbacks. The `picks: []` output when yfinance is unavailable means no dashboard pollution. The quality gates (score floor, forward_wr floor) will screen picks naturally.

---

## B. Surfaced contradictions / blockers

1. **Potential `etf_strategies` path issue in CI.** The emitter does `from etf_strategies import etf_sector_momentum, ETF_SYMBOLS`. The workflow sets `PYTHONPATH=${{ github.workspace }}` and the emitter adds `sys.path.insert(0, str(ROOT / "alpha_engine"))`. As long as `alpha_engine/etf_strategies.py` exists (confirmed), this should resolve. Low risk.

2. **Push contention is pre-handled.** The workflow already has a 10-retry push loop with exponential backoff. Adding one more `python` call before the commit doesn't introduce new contention.

3. **`active_picks_etf_draft.json` copy step.** The existing `cp alpha_engine/data/active_picks_etf_draft.json alpha_engine/data/active_picks_etf.json` step will still run. The spike output file may or may not exist if yfinance is slow. A `--if-exists` guard or `|| true` on the `cp` is cleaner but not critical since the file exists from prior runs.

---

## C. Recommended deltas to action-item doc

1. **Update B11 status** from 🔬 needs-investigation to 🔵 PR open once this PR is opened.

2. **Add a follow-up note**: Once the emitter populates picks, the `strat_fwd_wr` for `etf_sector_momentum` will build up. After n≥20 forward trades at ≥40% WR, picks from this strategy will pass the active gate. Until then, they're in active_raw only. Operator should check in ~2 weeks.

---

## D. Net verdict

**Ready-to-ship.** The 2-line workflow fix is clean, safe, and closes B11. The only monitoring needed is checking `alpha_engine/data/etf_sector_picks.json` after the first workflow run to confirm picks are generated.
