# B11 Multi-AI Feedback — Self-Review #1 (Claude Sonnet) — 2026-05-02

**Item:** B11 — ETF Source Diversification  
**Source:** `reports/REMAINING_ACTION_ITEMS_2026_04_30.md`  
**Review date:** 2026-05-02

---

## A. Confirmed assumptions

1. **File paths are correct.** `tools/etf_sector_emitter.py` exists at the expected path (5.7 KB, 2026-05-07 07:07). `alpha_engine/data/etf_sector_picks.json` exists (placeholder, 392 bytes, `picks: []`). The emitter imports `etf_sector_momentum` from `alpha_engine/etf_strategies.py` — confirmed at line 181.

2. **JSON_PICK_SOURCES entry is present.** Line 3848 of `audit_trail/dashboard_generator.py` shows `("etf_sector_rotation", "alpha_engine/data/etf_sector_picks.json", None)`. Wire-Up Rule satisfied — the production caller (JSON_PICK_SOURCES) exists.

3. **The emitter is never executed.** `.github/workflows/alpha-engine-etf.yml` calls `python tools/etf_emitter_spike.py` (proof-of-concept that writes `active_picks_etf_draft.json`) NOT `etf_sector_emitter.py`. The sector picks file remains at its placeholder `picks: []`.

4. **`active_picks_etf.json` (what the workflow writes) is NOT in JSON_PICK_SOURCES.** Only `etf_sector_picks.json` is registered. So the spike output never reaches the dashboard even when the spike succeeds.

5. **7 unit tests pass** against `tests/test_etf_sector_emitter.py` — all 7/7 green locally.

6. **ETF concentration confirmed.** `recent_closed` has 86 ETF picks, kimi_riseoftheclaw = 74/86 = 86.0%. By pnl_pct WR: kimi 52.7%, sum_pnl +25.78% — the concentration is real but kimi ETF picks are profitable.

7. **0 active ETF picks** — 4 in active_raw, all blocked: super_signals (strat_fwd_wr=37.1% < 40% ETF floor) and riseoftheclaw (score < 55 floor). ETF hard ban was removed 2026-04-19 per code comment.

---

## B. Surfaced contradictions / blockers

1. **The workflow writes the wrong file.** `etf_emitter_spike.py` → `active_picks_etf_draft.json` → copied to `active_picks_etf.json`. Neither file is in JSON_PICK_SOURCES. The workflow cycle is a no-op for dashboard purposes.

2. **No blocker on implementation.** The `etf_sector_emitter.py` is complete, tested, and registered. Only the workflow invocation is missing.

3. **yfinance may return empty data** — the emitter handles this gracefully (empty picks array, no crash). Not a blocker.

4. **Concentration risk is manageable.** kimi ETF WR=52.7% with positive sum_pnl (+25.78%). Adding sector rotation as a second source is additive diversification, not displacement.

---

## C. Recommended deltas to action-item doc

1. **Narrow the PR scope**: The fix is 2 lines in the workflow YAML — add `python tools/etf_sector_emitter.py` to the run step and `alpha_engine/data/etf_sector_picks.json` to the git add command. No new emitter needed.

2. **The `tests/test_etf_sector_emitter.py` already exists** (7 tests). No new test file needed — the existing tests cover the emitter. The action item doc says "New `tests/test_etf_*.py`" but this is already done.

3. **Wire-Up Rule is already satisfied** — JSON_PICK_SOURCES entry exists. PR can claim "wired" not "opt-in sidecar."

---

## D. Net verdict

**Ready-to-ship** — The emitter exists, is tested, and is registered. The only gap is the workflow invocation. A 2-line workflow change closes B11. No new code, no new tests required.
