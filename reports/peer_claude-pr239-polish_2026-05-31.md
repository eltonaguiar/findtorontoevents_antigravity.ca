# PR #239 Polish — Quote + Query Corrections

**Date:** 2026-05-31
**Subject:** corrections to `reports/peer_claude-OPERATOR_DIAGNOSTIC_PACKETS_2026-05-31.md` (the PR #239 deliverable)
**Trigger:** red-team report `reports/peer_claude-pr239-cross-verify_2026-05-31.md` flagged 2 quote-MOSTLY + 3 query non-EXACT.
**Mode:** READ-ONLY for ground truth (re-read source files, re-ran SQL against `mysql.50webs.com / ejaguiar1_stocks`); inline-edited the original PR #239 MD only (no new operator doc).

## Findings polished (5)

| # | Packet | Failure class (red-team) | Ground-truth fix applied inline in original MD |
|---|---|---|---|
| 1 | 7 — COMMODITY top-12 row 12 | Query MOSTLY | row 12 = `futures_cross_asset_momentum n=14 +0.075` (live), not `commodity_tsmom_12m n=9 +0.209`. `commodity_tsmom_12m` now correctly shown at rank 14. |
| 2 | 8 — `penny_deep_oversold` line pointer | Quote MOSTLY | replaced `production_scanner.py:2572-2575` + `("EQUITY", ...)` with verbatim `:2680` block including 5 lines of surrounding context. Tuple is `("equity", "penny_deep_oversold")` lowercase. |
| 3 | 8 — EQUITY top-9 row 9 | Query MOSTLY | row 9 = `non_crypto_consensus n=27 0.000` (live). `regime_accumulation` is rank 10 in the tie. Decision matrix unchanged (both below n=100). |
| 4 | 9 — PENNY Gate 0 call-site | Quote MOSTLY | `logger.info(` → `logger.debug(` plus full `try: ... except Exception:` envelope from `quality_gates.py:6741-6747`. |
| 5 | 9c — PENNY emission count | Query MINOR_DISCREPANCY | "0 rows 90d AND all-time" → `('penny', 4, 0, 3)` 90d AND all-time. Lowercase `category='penny'` bypasses the uppercase `asset_class` gate — material finding, gate is leaking. Decision criteria block also updated to recommend extending the gate to category-aware compare. |

## Method

1. Read red-team report → enumerated 5 items.
2. For each quote-fail: re-ran `Read` on cited file at red-team-supplied real line → captured verbatim bytes → `Edit`-replaced inside packet MD.
3. For each query-fail: re-ran the exact SQL block from the packet via `mysql.connector` to live DB → captured tuple output → `Edit`-replaced packet's claimed output, with a `> **CORRECTION 2026-05-31**` callout for operator traceability.
4. Added `APPENDIX C — Polish round` to original MD with a summary table + post-polish self-check.

## Post-polish self-check (same sample method)

- Packet 7 row 12 — query re-run output rank 12 = `futures_cross_asset_momentum n=14 +0.075` ✓
- Packet 8 `:2680` — `grep -n penny_deep_oversold alpha_engine/production_scanner.py` returns only `2680:`. Tuple lowercase ✓
- Packet 8 row 9 — query re-run output rank 9 = `non_crypto_consensus n=27 0.000`; rank 10 = `regime_accumulation n=27 -0.698` ✓
- Packet 9 logger — `Read quality_gates.py:6741-6747` confirms `try:` envelope + `logger.debug(` ✓
- Packet 9c PENNY — both 90d and all-time queries return `('penny', 4)` ✓

## Verdict

- **fixed = 5/5**
- **final_verified strict = 9/9** (quote 9/9 + query 9/9, no MOSTLY remaining for the polished items)
- Material new operator finding: **PENNY gate is leaking via lowercase `category='penny'`**. 4 picks emitted in 90d, 0 TP_HIT / 3 LOST. Recommend gate extension before going live with any PENNY-adjacent strategy.

---

*Generated by claude-opus-4-7 polish subagent. Read-only against source files + live DB; only mutation is inline-edit of the original PR #239 MD via server-side PR.*
