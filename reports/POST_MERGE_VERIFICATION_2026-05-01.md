# Post-Merge Verification Report — 2026-05-01

Generated by the autonomous hourly loop. Run started ~13:30 UTC.

## V1 — UEPS picks reach active_picks.json

**Status: FAIL** (B28 race condition confirmed)

Command: `python -c "import json; d=json.load(open('alpha_engine/data/active_picks.json')); picks = d if isinstance(d,list) else d.get('active_picks',[]); count = sum(1 for p in picks if (p.get('id','').startswith('ueps_') or p.get('pick_type')=='long_term_value')); print(f'UEPS/LTV picks: {count}')"`

Result: `0` UEPS/LTV picks in `active_picks.json` (total: 127 picks from other sources).

Root cause: `alpha-engine-live.yml` ("DARWIN ENGINE") rewrites `active_picks.json`
from scratch hourly. UEPS rows inserted by `sync_to_active_picks()` are overwritten
within ~4h. Fix: B28 (PR in review) registers `ueps_picks.json` directly in
`JSON_PICK_SOURCES`, eliminating the race.

## V2 — Existing closed equity picks reclassify to POSITION

**Status: PARTIAL FAIL** — EQUITY × POSITION row count = 0 in closed picks.

- Active EQUITY picks: AMD (INTRADAY), XOM (SWING) — no POSITION in active book
- Closed EQUITY × POSITION: 0 rows

Two LONG_TERM_VALUE equity picks (PEP + LLY from PR #545) should be appearing
as POSITION timeframe. Likely still pending dashboard rebuild after recent crons.
Needs re-verification after next hourly dashboard rebuild.

## V3 — TradingAgents emitter dormant when flag off

**Status: PASS ✅**

Command output: `flag TRADINGAGENTS_EMITTER_ENABLED: OFF` — zero file writes.
Dry-run exited after printing watchlist count + flag status. No output JSON written.

## V4 — Penny skyrocket cron wired

**Status: PASS ✅**

Workflow `.github/workflows/penny-skyrocket-runner.yml` exists and is scheduled:
`cron: '48 14 * * 1-5'` (weekdays 14:48 UTC). Workflow is not disabled.

## V5 — PEAD cache persists across runs

**Status: PASS ✅**

`git log --oneline -- data/earnings/` shows auto-commits touching the directory:
- `252fc9b0 DARWIN ENGINE: Hourly DNA evolution cycle 2026-05-01 12:30 UTC [skip ci]`
- `038485de Sustained Gainer scan: 2026-05-01 13:09 [AUTO] [skip ci]`

Two+ auto-commits have landed touching `data/earnings/` since PR #545 merged.
PEAD earnings cache is persisting across cron cycles.

## V6 — Concept taxonomy 100% coverage

**Status: PASS ✅**

Dashboard data at `/audit_dashboard/data/dashboard_data.json`:
`concept-tagged: 32/32` active picks have `concept_family` field.
PR #548's `assign_concept_fields()` is firing on all active picks.

## V7 — BOND credit-spread emits

**Status: DIAGNOSTIC** (not a fail per criterion)

`non_crypto_agent/data/bond_picks.json` contains 0 picks total (empty).
Criterion: "if 0, log diagnostic, don't fail".

Diagnostic: The bond agent workflow may not have run since PR #545 merged, or
the credit-spread signal conditions were not met during the last run. Check
bond-agent workflow run logs for the `strategy=bond_credit_spread_mean_reversion`
emitter path. Not blocking other queue items.

---

## Actions taken this run

- Ran V1-V7 verification and recorded findings above
- Identified B28 (UEPS race condition) as root cause of V1 failure
- Implemented B28: PR in review (`feat/ueps-json-pick-sources-2026-05-01`)
- V3 ✅, V4 ✅, V5 ✅, V6 ✅ marked as verified
- V1 still FAIL (will re-check after B28 merges + next dashboard rebuild)
- V2 partial fail, needs re-check
- V7 diagnostic (acceptable per criterion)
